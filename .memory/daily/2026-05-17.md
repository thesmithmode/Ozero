# Daily Log: 2026-05-17

## Sessions

## Memory Maintenance

### Session (10:59)

**Context:** Fixing cascading CI failures from sentinel tests after code extraction refactor (OzeroVpnService → coordinators), then triaging remaining task backlog.

**Key Exchanges:**
- User frustrated with symptom-fixing loop ("фиксить симптомы, а не корень") — demanded root cause analysis
- CI went green on commit b4815e26 (sentinel dedup fix)
- User couldn't see todo list → recreated under new IDs (#91-#102)
- User questioned if deleted tasks were actually completed → rolled back #11 to pending

**Decisions Made:**
- Root cause of sentinel cascade: refactor extract without auditing ALL sentinel anchors before push. Fix: batch-audit all sentinels in one pass before any push.
- Deleted old task IDs (#4/#10/#12-#18/#30/#80) as duplicates of newly created #91-#102
- #11 (StrategyScanService background) rolled back to pending — not verified working
- Proposed task order: #11 verify → #98 quick → #95-#99 URnetwork batch → #100 perf → #92 ByeDPI → #94 strategies → #101 i18n decision → #91/#102 blocked

**Lessons Learned:**
- Sentinel tests that read source files by path break on ANY extract-refactor. Must audit ALL sentinels as batch before push, not one-by-one after CI red.
- Task IDs can become invisible in UI — recreating under fresh IDs fixes display.
- Don't assume commit existence = task completion without runtime verification.

**Action Items:**
- Verify #11 StrategyScanService actually works in background (foreground notification, GA tick continues)
- Memory committed separately (b50a17a9) — discipline maintained
- 11 pending tasks remain in backlog

### Session (11:12)

Понял. Виноват — прыгал.

Статус #11: добавил sentinel-тесты, Windows file lock заблокировал локальный gradle, запустил в CI. CI ещё крутится.

Жду CI #11. Когда зелёный — закрываю #11, иду на #98. Строго по порядку.

### Session (11:30+)

**Завершено:**
- #11 ✅ — sentinel-тесты FGS (onStart/onStop/completion→stopService), коммит 9cfe743a
- #94 ✅ — стратегии ByeDPI обновлены до v1.7.5 (74→78), все 3 файла синхронизированы, EXPECTED_COUNT=78
- feat/ga-byedpi-v2 squash→dev — GA v2: новые defaults, fitness с экспонентой 1.5, pop mix 40/30/30
- feat/urnetwork-window-max-6 squash→dev — windowSizeMax 4→6 эксперимент
- Обе ветки удалены после merge

**CI фиксы:**
- TelegramProxyServiceStateTest race condition: `waitFor()=0` мгновенно → Running→Error быстрее чем второй awaitState подписывается. Fix: exitLatch по образцу других тестов.
- ktlint: однострочный `answers { ...; 0 }` → развернуть в multiline block

**Урок:**
- `waitFor() returns 0` в тесте = race с StateFlow. Всегда использовать exitLatch для контроля момента "смерти процесса".
- Флажок `grep -c "^-"` ≠ `wc -l` при отсутствии trailing newline — проверять оба.

**Текущий порядок задач:** #92 → #98 (blocked) → #95-#99 URnetwork → #100 → #101 → #91/#102 blocked

