# Daily Log: 2026-05-15

## Sessions

## Memory Maintenance

### Memory Flush (10:33)

FLUSH_ERROR: Exception: Control request timeout: initialize

### Session (11:19)

**Context:** Дебаг и фикс MTG генерации ключа (`engine-telegram`), затем ревью изменений агента.

**Key Exchanges:**
- Первоначально агент добавил только логирование — без реального диагноза. Юзер жёстко пресёк: "ты нихуя не починил".
- После `/gsd:debug` — найден реальный корень: `extractNativeLibs=false` (AGP default с 3.6) → `libmtg.so` не распаковывается при установке → `ProcessBuilder` падает с `binary.exists()=false` → UI молчит тихо.
- Фикс: `jniLibs.useLegacyPackaging=true` в `ozero.android.application.gradle.kts`.
- Коммит `2b99f40b` + 2 тест-файла.

**Decisions Made:**
- `useLegacyPackaging=true` в convention plugin (одно место для всех модулей) — правильный уровень фикса.
- Тест `MtgExtractNativeLibsSentinelTest` читает реальный buildscript с диска — нет мок-обмана.

**Lessons Learned:**
- `android:extractNativeLibs=false` — AGP default с 3.6. Любой Go-бинарь (`.so` как executable) в `jniLibs/` не распакуется без явного `useLegacyPackaging=true`. Паттерн применим и к будущим prebuilt-бинарям (`engine-telegram`, и другим subprocess-proxy).
- Shell-скрипты в тестах (`#!/bin/sh`) не запустятся на Windows — приемлемо, тесты Kotlin/Android только в CI (Linux).
- `locateRepoRoot()` в тесте обходит 5 уровней вверх — немного хрупко.

**Action Items:**
- P0 #1: split tunneling "все" ломает все движки → следующий дебаггер.
- P0 #2, #4 — после #1.
- P1 #7 — URnetwork не переключается (может быть связан с P0).

### Memory Flush (12:01)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (12:27)

**Context:** Расследование и фикс регрессии VPN — все движки перестали работать после изменения excludeSelf логики в OzeroVpnService.

**Key Exchanges:**
- Полный git-bisect анализ: commit 9cc8749a (рабочий, excludeSelf=true для всех) vs 5a8089dd (регрессия, excludeSelf=(engineId != WARP)) — разница 26 минут в один день
- 5 гипотез сформулированы, H1 подтверждена на 99%: WARP без self-исключения → трафик приложения летит в TUN → AWG не справляется → "запущен" но интернет мёртв
- Юзер напомнил: изначально пытались ускорить (как PORTAL WG), случайно сломали; модульность и низкая связанность — приоритет

**Decisions Made:**
- `excludeSelf = true` всегда в common-vpn (строка 401) — common-vpn не должен знать про EngineId.WARP, это нарушение модульности
- `EngineWarp.ipProbeRoute()` → `IpProbeRoute.StaticLocation("Cloudflare WARP", null)` вместо Unavailable — честное отображение без обмана
- Sentinel-тесты переписаны: удалены 2 теста защищавшие регрессию, добавлен тест запрещающий упоминание EngineId.WARP в common-vpn
- IP-probe через protected HTTP client для WARP — отложен как P2
- Верифицировано через PORTAL_WG reference: они тоже always-iterate disallow list (excludeSelf=true)

**Lessons Learned:**
- Sentinel-тест может стать антипаттерном: в 5a8089dd sentinel был написан чтобы защитить сломанное поведение, блокируя возврат к рабочему коду
- Модульная граница нарушена когда common-vpn проверяет `engineId != WARP` — движок-специфичная логика должна жить в engine-* модуле
- IP-probe через self-traffic в TUN конфликтует с архитектурой WARP (AWG в отдельном процессе) — дизайн изначально неверен
- Auto-mode не умеет детектить traffic-fail после старта (только preflight-fail) → WARP "запускается" но мёртв → auto-mode не переключается на следующий движок

**Action Items:**
- CI watcher запущен на pushed фикс excludeSelf
- P0 #2: расследование ByeDPI (read-only, без правок до зелёного CI)
- P2: реализовать IP-probe для WARP через WireGuard UAPI внутри engine-warp (отдельная задача)

### Memory Flush (12:50)

FLUSH_OK - Nothing worth saving from this session

### Session (13:30) — byedpi native correctness

**Context:** После зелёного CI на split tunnel fix (commit a891ef1b) — code review engine-byedpi (subagent), потом hardening native-lib.c race patterns.

**Key Exchanges:**
- Юзер: "если ты убежден что следует фиксить эти баги то фикси, надеюсь ты не допускаешь регрессий и не тупишь" → автономно
- Юзер ткнул на скип T-20/T-22: "не понимаю почему скип. Типа это хуйня и не надо трогать или что?" → пересмотрел T-22, объяснил T-20

**Decisions Made:**
- T-19 (commit 2cf9fa74): argv[0]=strdup("byedpi") NULL-check на OOM
- T-21 (commit 2cf9fa74): jniForceClose НЕ сбрасывает g_proxy_running — guard exclusively owned jniStartProxy. Premature release race window между cleanup main() и new start
- T-20 SKIP: server_fd race требует форка upstream byedpi submodule (extern int → _Atomic int). Локальный mutex полу-фикс — upstream main() пишет fd без наших локов. Документировано комментом в native-lib.c + memory
- T-23 (готов в tree): bounded retry в jniStartProxy (100×10ms=1s spin) — после Kotlin cancel() guard остаётся удержан, retry даёт upstream main() уйти естественно
- T-24 (готов в tree): proxyDispatcher = Executors.newSingleThreadExecutor("byedpi-proxy") вместо Dispatchers.IO. Изолирует leak при JNI блокировке после cancel()

**Lessons Learned:**
- Upstream ByeByeDPI 1.7.4 (Контекст/) имеет ТЕ ЖЕ race-баги: g_proxy_running без atomic, premature release в Stop и ForceClose. Наши fixes объективно лучше upstream
- При фиксе race с премaturной guard release — обязателен bounded retry в counterpart caller (jniStartProxy), иначе создаём cancel-edge регрессию
- @Singleton CoroutineScope на Dispatchers.IO с блокирующим JNI = thread leak risk при coroutine.cancel(); dedicated single-thread daemon-Executor изолирует

**Action Items:**
- Ждать CI зелёного на 2cf9fa74 → push T-23+T-20doc → CI → push T-24 → CI
- P0 #4 (SIGABRT) blocked: нужен tombstone от юзера

### Session (15:30) — code review + rework + hang recovery

**Context:** После push T-23+T-24 bundle (5b559c9b) спавнен code-reviewer subagent. Найдено 4 P0/P1 регрессии в моих свежих коммитах. CI на 5b559c9b упал (broken sentinel regex). Rework через ba2f74b5 + ktlint fix + hang recovery через 9de61de3.

**Key Findings (reviewer):**
- P0-1: 1s C spin retry блокировал single-thread dispatcher → +1s сериализация новых start()
- P0-2: jniStartProxy возвращал -1 на CAS fail неотличимо от real failure → ложные алерты
- P1-1: jniForceClose без guard release + single-thread = wedge = engine permanent dead
- P1-2: Executors.newSingleThreadExecutor + asCoroutineDispatcher не closes → leak в тестах
- P1-3: argv loop strdup без NULL-check (БЫЛО до меня, не починил)
- P1-4: JNI ExceptionCheck отсутствует после GetObjectArrayElement / GetStringUTFChars (БЫЛО до меня)
- P2-2: моя sentinel regex для retry loop broken — [^}]* не пересекает inner if-блок → false negative passed sentinel но не зацепил реальный код

**Decisions Made:**
- ba2f74b5: убран C retry; Executor → Dispatchers.IO.limitedParallelism(1); JNI_GUARD_BUSY = -2 distinct code; argv loop NULL-check; ExceptionCheck; sentinel-тесты переработаны
- 9de61de3: добавлен jniEmergencyReset (atomic_exchange) + ByeDpiEngine.startProxyWithRecovery() вызывает emergencyReset при JNI_GUARD_BUSY + retry. ktlint blank-line перед @OptIn annotation
- Trade-off с юзером: emergencyReset делает race window только при confirmed wedge, vs permanent dead engine = accept

**Lessons Learned:**
- ОБЯЗАН вызывать code-reviewer subagent ПОСЛЕ push коммита но ДО монитора CI — иначе bad code накапливается
- Sentinel regex `[^}]*` не пересекает inner `{...}` блоки — для multi-line patterns использовать `[\s\S]*?` или явные anchor patterns
- "Удаление feature" = регрессия требует обязательного компенсаторного механизма (emergencyReset для wedge recovery после T-21 удалил premature release)
- Bash в Claude Code НЕ поддерживает PowerShell @'...'@ heredoc — переключаться на PowerShell tool для русских commit messages
- Внутренние `"` в PowerShell here-string при передаче в native exe (git) могут вызывать pathspec split — избегать в commit messages

**Action Items:**
- CI watch на 9de61de3
- После green: memory commit отдельно
- P0 #4 (SIGABRT) blocked: нужен tombstone от юзера
- P2 #27 i18n аудит — следующая non-byedpi задача

### Session (13:33)

**Context:** ByeDPI engine correctness rework — fixing self-introduced regressions from T-21/T-23/T-24, plus JNI hardening and i18n gap analysis

**Key Exchanges:**
- User challenged "are we objectively better?" → honest audit revealed P0/P1 regressions I introduced (1s spin retry blocking single-thread dispatcher, Executor leak)
- Code reviewer (subagent) caught 7 issues in my own commits — P0-1 spin blocking, P0-2 indistinguishable return codes, P1-1 wedge deadlock, P1-2 Executor leak, P1-3 argv OOM, P1-4 missing JNI ExceptionCheck
- Explained wedge recovery trade-off to user in plain language (forceClose no longer releases guard → safer but risks permanent dead engine on wedge)
- User approved new behavior → led to T-28 emergency reset design (jniEmergencyReset as last resort when guard stuck after full cleanup)

**Decisions Made:**
- T-23 C-side retry REMOVED — my addition was a regression (1s spin blocks limitedParallelism(1) dispatcher)
- T-24 `Executors.newSingleThreadExecutor` replaced with `Dispatchers.IO.limitedParallelism(1)` — zero cleanup needed
- jniForceClose does NOT release guard (intentional trade-off) — queue serialization compensates; T-28 emergency reset added as wedge escape hatch
- `return -2` for guard-busy vs `return -1` for real failure — Kotlin can now distinguish
- argv loop OOM NULL-check + JNI ExceptionCheck after GetObjectArrayElement/GetStringUTFChars added (pre-existing gaps, not my regression)

**Lessons Learned:**
- Self-review insufficient — code reviewer subagent caught P0 regressions I missed in honest audit
- C-side spin retry in single-thread dispatcher = serialization bottleneck (non-obvious perf regression)
- `Executors.newSingleThreadExecutor().asCoroutineDispatcher()` leaks in tests — `limitedParallelism(1)` is strictly better
- Sentinel test regex `[^}]*` doesn't cross inner braces — caused CI fail on 5b559c9b
- PowerShell heredoc `@'...'@` breaks on inner double-quotes — need alternative quoting
- feedback_byedpi_native_guard_ownership: jniForceClose must NOT touch g_proxy_running — only jniStartProxy's main() exit path releases

**Action Items:**
- CI pending on 9de61de3 (rework ba2f74b5 + T-28 emergency reset + ktlint fix)
- T-27 i18n: 17 keys missing in es/pt (identical set), 180 missing in ar/de/fr/hi/ja/zh-rCN (stale partial translations)
- Memory commit needed separately after CI green
- Upstream byedpi 1.7.4 has same race bugs — our fix is better (documented in .claude/Контекст/)

