# Daily Log: 2026-05-26

## Sessions

## Memory Maintenance

### Memory Flush (08:12)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (08:21)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (12:21)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (13:02)

FLUSH_OK - Nothing worth saving from this session

### Session (13:59)

**Context:** Ozero — singbox engine UI/crash диагностика и несколько UI задач

**Key Exchanges:**
- Singbox crash требует tombstone с устройства: `adb pull /data/user/0/ru.ozero.app/files/debug/`

**Decisions Made:**
- `probeLatencyMs` десериализовать через `AbstractBean` (не `VLESSBean`) — иначе все не-VLESS серверы возвращают -1
- Кнопка пинга: `PlayArrow` → текстовая `TextButton("Тест")` + при активном пинге → `"Отмена"` с cancellation

**Lessons Learned:**
- Singbox ping deserializes only `VLESSBean` → -1 для других протоколов; fix через `AbstractBean.serverAddress/serverPort`
- Ping без `Job` reference = нельзя отменить; всегда хранить `pingJob` в ViewModel
- Строки можно не добавлять — "Отмена" уже была в strings.xml

**Action Items:**
- Tombstone с устройства для диагностики singbox crash (SIGSEGV/SIGABRT)
- Task #7: сортировка серверов по пингу
- Task #8: шестерёнка → отдельный settings экран
- Task #9: восстановить управление подписками
- Task #6 (бесконечное переподключение) — зависит от singbox crash fix

### Session (14:32)

GitHub Actions global incident (10:57 UTC 26 May 2026). Our code fine. CI triggers automatically when resolved.

### Memory Flush (14:48)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (14:48)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (16:22)

**Context:** Ozero — код-ревью фиксы SingboxServerListViewModel + URnetwork relay provider (0 bytes проблема)

**Key Exchanges:**
- Агент 2 вернул 3 finding по code review; лично перепроверено: 2 ложных (isPinging clear и LaunchedEffect race), 1 принят (error?.message fallback → добавить `?: "Unknown error"`)
- Агент 1 (relay) вернул пустоту — relay код верифицирован вручную: RelayLockManager, RelayNetworkMonitor, watchdog — всё корректно
- Обсуждение готовности кода: CI green + device test нужны для финального подтверждения relay fix

**Decisions Made:**
- `connectBestAvailable()` после `device.start()` — главная гипотеза fix'а 0 bytes (device создавался но не подключался к mesh)
- Subagent findings перепроверять лично перед принятием — 2 из 3 оказались ложными

**Lessons Learned:**
- `isPinging.clear()` перед новым pingJob — корректный паттерн, не race (старый job отменён, новый стартует чистым)
- `LaunchedEffect(key)` при смене key автоматически cancel'ит предыдущую — стандартный Compose, не race
- `Exception().message` может быть null → fallback обязателен для robustness

**Action Items:**
- Дождаться CI green (dev-fix)
- Device test: включить движок → лог `MeshSession` → `relayDiagnostics` → проверить `unpaidBytes>0`
- Если всё ещё 0: проверить `tunnelStarted` и `connectEnabled` в диагностике → след. итерация (возможно `setTunnelStarted(true)` без TUN fd)

### Session (16:52)

detekt threshold `>=20` не `>20` — нужно 19 функций в ViewModel.

**Context:** CI troubleshooting в ветке dev-fix: detekt TooManyFunctions + ktlint if/else без скобок.

**Key Exchanges:**
- detekt TooManyFunctions threshold 20 = `>=20` семантика, нужно строго < 20 (максимум 19)
- Объединили `onAddGroupDialogOpen` + `onAddGroupDialogDismiss` → `onAddGroupDialog(show: Boolean)` toggle-паттерн
- ktlint fail: if/else без фигурных скобок → зафиксили

**Lessons Learned:**
- detekt `TooManyFunctions=20` означает `functions >= 20` = FAIL; нужно 19 или меньше
- Watcher race: `gh run list --limit 1` может вернуть старый completed run если новый ещё не появился → фиксить через явный run ID в мониторинге

**Action Items:**
- CI run 26451747627 зафейлил (ktlint) — запущен новый, ожидаем результат

### Session (19:44)

**Context:** Расследование багов Ozero v0.2.8/v0.2.9 — singbox, WARP, URnetwork, VPN фон

**Key Exchanges:**
- Юзер сообщил ~10 проблем: после отключения VPN что-то работает в фоне (v0.2.8), URnetwork не восстанавливает провайдера, singbox не принимает URL подписок, "Лучший сервер" не работает, авто-выбор сломан, мёртвые подписки зависают UI без cancel, кнопки Тест/Обновить не на вкладке настроек, WARP не пускает трафик сразу, наложение цветов кнопки
- Агент нашёл в логах: 3 SIGABRT в `:engine_singbox`, `unknown transport type: splithttp`, `dns outbound deprecated in 1.13.0`, singbox Connected→fail race, WARP reconnect за 4ms (stale tunnel)
- Агент работал в ветке `win11` вместо `dev` — критическая ошибка, прервано юзером

**Decisions Made:**
- TODO сформирован от простого к сложному: T1 dns outbound → T2 splithttp → T3 Connected race → T4 UI кнопки → T5-T6 subscription UX → T7 авто-выбор → T8 Karing-style input → T9 URnetwork провайдер → T10 WARP handshake → T11 кнопка цвет → T12 VPN фон → T13 refresh блокировка UI

**Lessons Learned:**
- Перед работой ВСЕГДА проверять активную ветку — `git branch` первым шагом. Агент начал работу в `win11` (устаревший) вместо `dev`
- `dns outbound` deprecated в sing-box 1.13.0 — нельзя использовать как outbound type
- `splithttp` transport не поддерживается текущей libbox.so — профили с ним нужно фильтровать или skipped
- WARP `awgTurnOn dt=4ms` при reconnect = stale tunnel reuse без handshake verification

**Action Items:**
- Работа прервана на T1 (dns outbound fix) — возобновить с dev ветки
- Верифицировать полный список: юзер указал что агент мог пропустить баги — нужно cross-check prompta + логов + UI кода
- Karing reference: `C:\Soft\Projects\Ozero\.claude\Контекст\karing` — изучить для subscription input UI (URL/файл/clipboard)

### Memory Flush (20:16)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (20:23)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (20:34)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (21:05)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (21:06)

FLUSH_OK - Nothing worth saving from this session

### Session (21:41)

**Context:** Merge win11→dev (desktop multiplatform), WARP amber flash sentinels, singbox crash diagnosis

**Key Exchanges:**
- singbox: DeadObjectException + CRASH_NATIVE (SIGABRT) в libbox при `startWithConfig`, после `checkConfig passed`. configLen=868 и configLen=20448 — обе крашатся. Native crash в Go-библиотеке
- WARP amber flash fix (`cbbbab76`): `_stats.value = _stats.value.copy(activeConnections = 1)` при ReadyResult.Ready — исправляет кратковременный 0 в UI

**Decisions Made:**
- Memory-конфликты win11↔dev → берём dev-версии (позже обновлены, одна сессия актуальнее)
- Singbox расследование отложено до sentinels WARP — задача #23 pending

**Lessons Learned:**
- Amber flash fix не имел sentinel — новая категория regression: stats initialisation при Ready должна быть закреплена тестом немедленно после фикса
- win11→dev merge: 76 файлов, 0 кодовых конфликтов; shared-модули (UriCompat, Base64, PersistentLoggers) вошли чисто

**Action Items:**
- Singbox: достать tombstone (`adb pull /data/user/0/ru.ozero.app/files/debug/`) и анализировать SIGABRT в libbox — задача #23
- CI watcher на dev (desktop-модуль первый прогон)

### Memory Flush (23:27)

FLUSH_OK - Nothing worth saving from this session

