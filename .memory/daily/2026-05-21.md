# Daily Log: 2026-05-21

## Sessions

## Memory Maintenance

### Session (00:04)

**Context:** Диагностика красного CI на dev (ByeDPI applyUnderlying fix + killswitch startup)

**Key Exchanges:**
- User: "задачу выполнил и починил модуль ByeDPI?" → Да, прод-код починен. CI red только из-за устаревшего sentinel-теста, не из-за дефекта.
- Split-контракт: ByeDPI engine TUN `applyUnderlying=false` (QUIC parity с ByeByeDPI 1.7.5, YouTube fix). Killswitch startup TUN `applyUnderlying=true` (P37 lockdown при WiFi↔Mobile).
- Sentinel `OzeroVpnServiceLockdownKillswitchTest:136` не знает про новый параметр `applyUnderlying: Boolean = false` и второй call-site → падает.

**Decisions Made:**
- `applyUnderlying=false` → ByeDPI engine path (upstream parity)
- `applyUnderlying=true` → killswitch preliminary startup TUN (P37 invariant)
- Не патчить sentinel наскоро — переписать под оба call-site с явными аннотациями

**Lessons Learned:**
- Статус-запрос ≠ action: когда user просит "посмотри CI" — только репортить, не делать checkout/stash. Нарушение = раздражение.
- Stash cleanup падает на Windows из-за file locks (Permission denied `.kotlin/sessions`) — рабочий stash создаётся но working tree остаётся dirty.

**Action Items:**
- Переписать `OzeroVpnServiceLockdownKillswitchTest` под split-контракт (два call-site, оба с явными `applyUnderlying` значениями) → dev CI зелёный
- `stash@{0} wip-dns-tunnel-module` висит, `dns-tunnel-module` working tree dirty — разобрать при возврате к этой ветке

### Session (00:20)

Осталось:

**Context:** Доводка `engine-masterdns` модуля + фикс ByeDPI `applyUnderlying` split-контракта в ветке `dns-tunnel-module`.

**Key Exchanges:**
- Юзер спросил: починен ли ByeDPI fix? Ответ: прод-код живой в `dev` (коммиты `c7954861` + `563dab97`), CI красный только из-за устаревшего sentinel-теста.
- Split-контракт: ByeDPI engine TUN → `applyUnderlying=false`, killswitch startup TUN → `applyUnderlying=true`.

**Decisions Made:**
- Ревью masterdns модуля проведено, два серьёзных находки:
  - `MasterDnsEngine.kt:42` — `state.first { ... }` без timeout → hang risk.
  - `upstream` параметр ignored в `start()`.
- CI `dns-tunnel-module` упал → ktlint violations (незафиксированный ktlint прогон).

**Lessons Learned:**
- Юзер явно попросил: summary поменьше и проще. Длинный технический dump ревью = зря.
- При code review output → отбирать топ 2-3 критических, остальное в лог/задачи.

**Action Items:**
- Починить `state.first` timeout в MasterDnsEngine.kt.
- Проверить контракт `upstream` параметра Engine интерфейса для masterdns.
- Обновить sentinel `OzeroVpnServiceLockdownKillswitchTest:136` под split-параметр.
- Дождаться CI и добить зелёный dev.

### Session (01:30) — merge + 7 review fixes + sentinel split

**Context:** Squash merge `dns-tunnel-module` → `dev`, удалить ветку, починить 7 review-находок по engine-masterdns + sentinel split на dev.

**Actions:**
- Squash merge `dns-tunnel-module` → `dev` как `e469e252 FEAT: engine-masterdns модуль — subprocess-pattern EnginePlugin (libmdnsvpn.so)`. Ветка удалена (local + remote).
- Sentinel `OzeroVpnServiceLockdownKillswitchTest > buildTunBuilder applyUnderlying=false` переписан под текущий контракт: ассертит `applyUnderlying: Boolean = false` в сигнатуре + `applyUnderlying = applyUnderlying` pass-through (вместо литерала `applyUnderlying = false`, которого в коде нет — функция принимает параметр).
- 7 фиксов engine-masterdns в `284b87ab`:
  - `MasterDnsEngine.start`: `withTimeoutOrNull(startTimeoutMs=10s)` + service.stop() при timeout
  - `MasterDnsEngine`: `@Suppress("UNUSED_PARAMETER")` на `upstream` (намеренно ignored — `supportsUpstreamSocks=false`)
  - `MasterDnsConfigWriter`: фильтр LISTEN_IP/LISTEN_PORT/LOCAL_DNS_ENABLED case-insensitive (`startsWith ignoreCase=true`)
  - Новый `MasterDnsResolversCache` — `stateIn(SharingStarted.Eagerly)` snapshot. Заменил `runBlocking + withTimeoutOrNull` в Hilt provider (anti-pattern в DI).
  - `MasterDnsPreflight.parseHostPort`: поддержка `host:port` и `[ipv6]:port`. Раньше port был хардкод 53.
  - `MasterDnsClientService`: stdout reader limit MAX_LOG_LINES=200, далее `PersistentLoggers.warn` (не Log.w — LoggingContractTest).
  - `MasterDnsSettingsViewModel`: runCatching + PersistentLoggers.error в каждом setX (DataStore IO errors молча проглатывались).
- `.kotlin/` добавлен в `.gitignore` (build artefact от Kotlin Gradle plugin).
- Покрытие: 3 новых теста (MasterDnsConfigWriterTest case-insensitive, MasterDnsPreflightTest host:port + ipv6 + invalid port, MasterDnsEngineTest stuck-starting timeout, MasterDnsResolversCacheTest snapshot).

**Lessons Learned:**
- При squash merge на свежий dev с разошедшимися `.memory/` правками — stash pop конфликтует. Решение: `git checkout --theirs` на conflicted .memory + drop stash, daily вести из текущей сессии. Старые правки в стэше теряются — приемлемо если контент менее ценный чем чистый merge.
- Sentinel-тесты с substring assertions на литералы (`"applyUnderlying = false"`) ломаются когда default-параметр вытесняет литерал в сигнатуру. Лучше ассертить на сигнатуру (`"applyUnderlying: Boolean = false"`) + pass-through. Литерал не выживает рефакторинга.
- `@Suppress("UNUSED_PARAMETER")` на override функции с осознанно неиспользуемым параметром (по capabilities контракту) — корректнее чем переименование в `_param` или удаление, т.к. сигнатура определена интерфейсом.

**Action Items:**
- Дождаться CI на `284b87ab` → если зелёный, готово.
- `stash@{0} wip-dns-tunnel-module` висит без ветки — drop при возможности (ветка уже удалена).

### Session (00:43)

**Context:** Merge dns-tunnel-module branch, fix masterdns review bugs, fix ByeDPI sentinel/split-tunnel regression, prepare v0.1.10 release.

**Key Exchanges:**
- Юзер попросил объяснить прошлое ревью проще → краткий пересказ 7 находок masterdns (таймаут, upstream ignored, case TOML, runBlocking в DI, DNS port, log spam, silent error)
- Юзер скомандовал: squash в dev, удалить ветку, починить баги masterdns + ByeDPI CI

**Decisions Made:**
- Squash merge `dns-tunnel-module` → `dev`, удалить ветку
- Все 7 masterdns review-проблем зафикшены в dev
- После зелёного CI dev → squash dev→main → tag `v0.1.10`

**Action Items:**
- ✅ CI зелёный на `1843cf28`
- ✅ Squash `dev`→`main` как `b0d361e6 RELEASE: v0.1.10`, push, тег `v0.1.10` → release.yml in_progress (run 26192143270)

### Session (00:54) — релиз v0.1.10

**Actions:**
- Squash merge `dev` → `main` (3 коммита накопления с v0.1.8, 151 files, +6658/-765). Конфликты `.memory/*` + .kt — все разрешены `--theirs` (dev побеждает).
- Релиз-коммит `b0d361e6 RELEASE: v0.1.10` пушнут в main.
- Тег `v0.1.10` создан и пушнут — `release.yml` тригернулся.
- Возврат на `dev` + stash pop daily-memory (правки оставались в worktree до merge).

**Lessons Learned:**
- Bash here-doc для git commit message ломается на $-substitution в Windows; PowerShell `@'...'@` here-string работает корректно для многострочного русского текста.
- При squash merge dev→main с накопленной работой 100+ файлов конфликты ожидаемы (parallel histories через предыдущий squash). `git diff --name-only --diff-filter=U | xargs -I {} git checkout --theirs {}` — быстрое решение когда main устарел.

### Memory Flush (00:54)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (00:57)

**Context:** Knowledge base article compilation session — reviewer false-positive lessons, sentinel trap family, symptom-vs-removal pattern

**Key Exchanges:**
- Reviewer P1 "BC compatibility broken" claims on walletOverride/warpSlots были false positives — dead code in prod + intentional empty=preserve design. Human verification mandatory.

**Decisions Made:**
- Workflow зафиксирован: reviewer findings → author verifies против codebase + project memory → apply only confirmed bugs. "P1 severity" = hypothesis, not verdict.

**Lessons Learned:**
- False-positive rate rises when codebase has deliberate design choices that look like bugs without business context. Reviewers lack access to `.memory/knowledge/` and deployment status.
- Structural improvement: inject relevant `project_*.md` memory articles into reviewer system prompt when spawning on domain-specific code.
- Three sentinel trap types form completeness family: FQN desync (vacuous pass), protecting-bug (blocks correct fix), anchor-miss (searches full file). All three must be guarded.
- Consent system removal > auto-grant fix: when subsystem copied from ref impl with no business need in integrating app — remove, don't patch.

**Action Items:**
- Future: inject project memory articles into reviewer system prompt for domain-specific code reviews.

---

**WARP mirrors question:** Нет кода в контексте. Нужен `grep -r "mirror\|generator\|fallback\|retry" engine-warp/` чтобы ответить. Что конкретно сломано или что строишь?

### Memory Flush (01:33)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (01:56)

FLUSH_ERROR: Exception: Control request timeout: initialize

---

## Task14/15/16 — ByeDPI CMD verbatim + URnetwork picker fixes

### Task14 — URnetwork search LazyColumn crash
- Root: two Moscow entries с одинаковыми name+countryCode → composite key collision → IllegalArgumentException.
- Fix: `System.identityHashCode(it.location)` для всех 4 sections (bm/c/r/ct).
- Commit `b14f7c42`.

### Task15 — URnetwork picker без regions/cities
- Root: fresh users получают `localState.provideMode = ProvideModeNone (0)` → SDK скрывает regions/cities в filterLocations callback.
- Reference `DeviceManager.kt:105`: `provideMode = if (provideControlMode == ALWAYS) Sdk.ProvideModePublic else localState.provideMode` — override на ALWAYS.
- Fix: extracted `applyDeviceFields(device, localState)` helper, 13 device-полей в одном месте, override provideMode на ALWAYS, normalize provideControlMode через `UrnetworkProvideControlMode.fromRaw`.
- Sentinel'ы: applyDeviceFields helper, single source of truth для runStartOnMain + ensureDeviceOnMain, ProvideModePublic override.
- Commit `d953d56d`.

### Task16 — ByeDPI YouTube CMD broken
- Root: `ensureUdpDesync` (legacy 7da7e852) дописывал `-Ku -a1 -An` к CMD args без `-Ku`:
  - User strategy `-s1 -q1 -a1 -Y -At -a1 -S -f-1 -r1+s -a1 -As -d1+s -O1 -s29+s -a1` (без -Ku) → суффикс добавляет лишнюю `-a` секцию + переопределяет `-As` на `-An` → byedpi топология сломана → YouTube fail.
  - UI mode не использует ensureUdpDesync — частично работал.
  - В оригинальном ByeByeDPI идентичная стратегия работает идеально.
- Fix: CMD path → `settings.byedpiWinningArgs!!.trim()` verbatim. Удалены `ensureUdpDesync` и `UDP_DESYNC_SUFFIX`. Если эволюция нуждается в UDP — фиксить в `AutoStrategyPicker`, не в выходе.
- Sentinel'ы: `CMD mode winningArgs передаются verbatim`, `без -Ku не получает auto-suffix`, `с пробелами на краях trim`.

### Lessons
- "Те же args в оригинале работают" → корень всегда в pipeline/init/build, не в args. Применимо и к выходу — auto-mutation args тоже корень pipeline.
- 13-field parity URnetwork: localState reading отдельно от device writing. Reference patterns читать ВСЕГДА перед патчем (DeviceManager.kt:105 явно описывал override).
- Memory feedback `no_skip_no_defer`: диагностический Log ≠ фикс. Юзер прямо: "ты все время что то скипаешь или оставляешь на потом, НИКОГДА не делай так".

### Task17 — IP probe ECONNREFUSED fallback
- Симптом: UI показывает Error на IP-карточке при ECONNREFUSED к 127.0.0.1:<port> у ByeDPI (Socks proxy ещё не открыт или уже закрыт).
- Fix: `MainViewModel.resolveOnce` — после `fetchVia(host,port).isFailure` → `ipInfoProvider.fetch()` (direct). Покажет реальный device IP (без VPN), но не Error.
- Retry-loop сверху (3 attempts × 1500ms) сохранён — каждая попытка теперь Socks → direct fallback.
- Sentinel: `resolveOnce Socks-route после fetchVia failure делает direct fetch` в `MainViewModelIpInfoChannelTest`.
### Memory Flush (02:04)

FLUSH_OK - Nothing worth saving from this session

