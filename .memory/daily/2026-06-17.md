# Daily Log: 2026-06-17

## Sessions

## Memory Maintenance

### Session (10:37)

**Context:** Пользователь расследовал повторное падение MasterDNS при деплое и просил проверить сопутствующие проблемы в логах.

**Key Exchanges:**
- По trace и скриншоту выявлены два разных сбоя MasterDNS: ранний контейнер падал с `No Allowed Domains Configured`, а свежий запуск ломался раньше на `config_init` с `/bin/sh` ошибкой `expecting "fi"`.
- Обнаружен отдельный сопутствующий сбой sing-box: профиль с портом `4449499` приводил к ошибке `server_port ... uint16`; это отмечено как отдельный риск, не смешанный с MasterDNS-фиксом.
- Пользователю сказано, что на сервере пока ничего вводить не нужно; при повторном падении нужны `docker ps -a --filter name=masterdns-ozero`, `docker logs --tail 80 masterdns-ozero` и sanitized `/etc/masterdnsvpn/server_config.toml`.

**Decisions Made:**
- Исправить корень MasterDNS-сбоя в генерации `runContainer`: внутренний `sh -c '...'` нельзя резать одинарными кавычками внутри `grep -Eq '...'` и `sed '...'`; оба выражения переведены на двойные кавычки.
- Добавить регрессионный контрактный тест, что `config_init` не содержит внутренних single quotes и не может снова сломать внешний shell argument.
- Не смешивать sing-box порт `4449499` с MasterDNS-фиксом, потому что в текущем коде уже есть тесты на отбрасывание такого порта, а повтор на свежей сборке требует отдельного расследования пути запуска выбранного профиля.

**Lessons Learned:**
- Вложенные `sh -c`-скрипты в single-quoted shell argument нельзя генерировать с внутренними одинарными кавычками: shell обрезает скрипт и симптомы выглядят как незакрытый `if/fi`.
- `No Allowed Domains Configured` у MasterDNS связан с пустым `DOMAIN = []`; миграция в `DOMAIN = ["ozero.invalid"]` должна выполняться до старта контейнера, иначе сервер падает уже внутри контейнера.
- Перед push в Ozero локально разрешён и обязателен style gate `ktlintCheck detekt --continue`; полные Gradle tests локально не запускать по проектному правилу.

**Action Items:**
- После сборки с фиксом повторить деплой MasterDNS.
- Если MasterDNS снова упадёт, собрать `docker ps -a --filter name=masterdns-ozero`, `docker logs --tail 80 masterdns-ozero` и sanitized `server_config.toml` без ключа шифрования.
- Если sing-box с портом `4449499` повторится на свежей сборке, отдельно проверить путь запуска выбранного профиля, не только auto-probe.

### Session (10:48)

**Context:** Пользователь чистил и ревьюил MasterDNS после серии неудачных итераций запуска/деплоя.

**Key Exchanges:**
- Пользователь попросил удалить устаревший блок “Как поднять свой сервер за 15 минут” из параметров MasterDNS и досконально проверить код MasterDNS на ошибки.
- Было уточнено, что встроенный Android-инструмент есть: плагин `Test Android Apps`, применим для проверки APK на эмуляторе/устройстве, но для текущего кода сначала выполнялась локальная ревизия.

**Decisions Made:**
- Удалять нужно весь manual setup UI-блок, а не только заголовок: `ServerSetupCard`, `SetupStep`, команды copy/install/start и связанные строки локалей, потому что путь устарел и конфликтует с автодеплоем.
- Субагенты не запускались, потому что callable multi-agent инструмента в текущем наборе не было; ожидание несуществующего агента нарушило бы правило не простаивать.
- Для SSH timeout выбран консервативный фикс: сначала `join(timeout)`, при `exitStatus == null` закрывать command и возвращать структурный `ERR_TIMEOUT|timeoutMs=...`, затем читать stdout/stderr.

**Lessons Learned:**
- PowerShell-механическая правка XML может испортить UTF-8 ресурсы; безопаснее восстанавливать из git и удалять строки UTF-8-safe способом.
- `SshjTransport.exec()` имел критичный риск: чтение stdout до `cmd.join(timeout)` могло зависнуть навсегда, фактически отключая timeout для удалённых команд деплоя.
- MasterDNS `config_init` не должен полагаться только на миграцию точного `DOMAIN = []`: после старых неудачных запусков конфиг может быть без `DOMAIN` или с пустым значением, что снова приводит к `No Allowed Domains Configured`.
- Вложенный `sh -c` для MasterDNS нельзя снова ломать одинарными кавычками внутри `grep`/`sed`; shell-cutting уже приводил к `expecting "fi"`.

**Action Items:**
- После правок прогнать разрешённый style gate `ktlintCheck detekt --continue`.
- При следующей проверке MasterDNS повторить деплой и, если снова упадёт, собрать `docker ps`, `docker logs --tail 80 masterdns-ozero` и sanitized `server_config.toml` без ключа шифрования.

### Session (13:06)

**Context:** Добавлялись и доводились JVM behavior-тесты auto fallback для `StartSequenceCoordinator.run()` с реальным `ChainOrchestrator` и stateful fake-плагинами.

**Key Exchanges:**
- Был найден уже существующий `StartSequenceCoordinatorBehaviorTest.kt`, поэтому новые проверки добавлялись точечно вместо дублирования sentinel/source-тестов.
- Субагент провел ревью новых тестов и указал, что порядок `stop A -> start B` проверен, но TUN fd cleanup закреплен недостаточно.
- После ревью тесты доработаны: добавлены проверки `close()` для fd, порядок закрытия до старта следующего кандидата и явные проверки `tunnelGateway.start` с правильными `tunPfd`/`socksPort`.

**Decisions Made:**
- Не запускать локальные Gradle test/build, кроме разрешенного style-gate, потому что проектные правила Ozero запрещают локальные полные Gradle проверки.
- Перед коммитом запускать только `ktlintCheck detekt --continue`; обе проверки прошли до и после rebase.
- В коммит включены только правка тестов и dirty `.memory/daily/2026-06-17.md`; чужие untracked изменения оставлены нетронутыми.
- Push в `dev` делался вне песочницы. После reject из-за отставшего remote выполнен `git pull --rebase`, затем повторный style-gate и push.

**Lessons Learned:**
- Для auto fallback behavior-тестов недостаточно проверять только `plugin.stop` перед стартом следующего кандидата; нужно закреплять закрытие TUN fd и, при необходимости, порядок `fd.close()` до `start` следующего runtime-кандидата.
- `tunnelGateway.start` стоит проверять с конкретными `tunPfd` и `socksPort`, иначе тест может не поймать перепутанный fd/port между кандидатами.
- В Ozero CI лог Gradle может показывать только `N tests completed, 1 failed` без имени теста; для диагностики common-vpn failures надежнее скачивать artifact `test-reports-common-vpn` и читать XML/HTML отчет.

**Action Items:**
- Разобрать artifact `test-reports-common-vpn` для CI run на SHA `06a47676`, найти failing test и исправить причину падения `Tests — common-vpn`.
- После фикса снова прогнать `ktlintCheck detekt --continue`, закоммитить, проверить `git show HEAD`, push в `dev` и дождаться terminal CI status.

### Session (13:37)

**Context:** Работа над тестовым покрытием Ozero для runtime-пути VPN engines и применения настроек ByeDPI при запуске.

**Key Exchanges:**
- Саб-агент подтвердил, что `SettingsRepository`, `SettingsViewModel` и `EngineSettingsRestartObserver` уже хорошо покрыты; реальный gap находится в цепочке `settings -> EnginePlugin config -> StartSequence`.
- Был выбран app-level JVM integration test, потому что `common-vpn` не зависит от `engine-byedpi`, а `app` видит оба модуля.
- Тест построен так, чтобы проходить через `SettingsModel -> StartSequenceCoordinator -> ChainOrchestrator -> plugin.start(config)`, но не запускать native ByeDPI.
- Второй саб-агент оценил новый тест на 8/10 и указал, что нужно явно закрепить выбор `buildProxyConfig()` в `TrafficMode.PROXY`.

**Decisions Made:**
- Не добавлять новые тесты в `SettingsRepository`, `SettingsViewModel` и restart observer, потому что это дублирует существующее покрытие.
- Добавить интеграционный тест для ByeDPI CMD/UI settings на уровне `app/src/test`, чтобы проверить фактическое применение сохраненных настроек при старте runtime.
- Усилить тест счетчиками `buildProxyConfig`/`buildManualConfig` и проверкой `ByeDpiEngine.AUTO_ROTATE_PORT`, чтобы тест лучше фиксировал boundary coordinator/orchestrator/plugin.

**Lessons Learned:**
- Для проверки engine-specific runtime settings полезнее тестировать путь запуска через `StartSequenceCoordinator`, а не только repository/ViewModel round-trip.
- В `TrafficMode.PROXY` тест может ложно выглядеть как проверка proxy builder, если `buildProxyConfig()` делегирует default implementation; нужны явные счетчики вызовов.
- PROXY mode позволяет тестировать ByeDPI config propagation без Android `VpnService.Builder`, native tunnel и открытия портов, снижая риск flake.

**Action Items:**
- Следующая фича по очереди: ByeDPI strategy scan/apply.
- Остались чужие/не относящиеся untracked: `.codex/config.toml`, `.tmp/`, `_ozero_tmp/`, `singbox-core/libs/libbox.jar`.

### Session (13:39)

**Context:** Продолжение долгосрочной цели: покрыть бизнес-логику Ozero надежными тестами по фичам; завершены первые две тестовые фичи и начато расследование следующей.

**Key Exchanges:**
- Добавлен app-level JVM integration test `StartSequenceByeDpiSettingsIntegrationTest.kt` для пути `SettingsModel -> StartSequenceCoordinator -> ChainOrchestrator -> plugin.start(config)` в ByeDPI CMD/UI mode без запуска native.
- Саб-агент оценил новый тест на 8/10: тест полезен для runtime-config propagation, но изначально не доказывал выбор `buildProxyConfig()` в `TrafficMode.PROXY`.
- После ревью тест усилен счетчиками `buildProxyConfig` vs `buildManualConfig` и проверкой `ByeDpiEngine.AUTO_ROTATE_PORT`.
- Ранее в этом же цикле закрыта фича VPN lifecycle + auto fallback behavior tests.

**Decisions Made:**
- Интеграционный тест для ByeDPI settings положен в `app/src/test`, потому что только `app` видит одновременно `common-vpn` и `engine-byedpi` без циклических зависимостей.
- Тест не запускает native ByeDPI: используется real config builder через wrapper/plugin, а `start()` только записывает `EngineConfig`, чтобы проверять propagation через стартовую последовательность без Android/native flake.
- Для сценария выбран `TrafficMode.PROXY`, чтобы обойти `VpnService.Builder` и native tunnel, сохранив проверку `StartSequenceCoordinator` и `ChainOrchestrator`.

**Lessons Learned:**
- `EnginePlugin.buildProxyConfig()` default может делегировать в `buildManualConfig()`, поэтому sentinel на proxy mode должен явно считать вызовы builder-методов, иначе возможен ложноположительный тест.
- Проверка только аргументов ByeDPI частично дублирует `ByeDpiBuildManualConfigTest`; интеграционная ценность выше, когда закрепляется boundary coordinator/orchestrator/plugin и runtime transport details.
- Для `BYEDPI:PROXY` `mockk<TunBuilderHelper>(relaxed = true)` приемлем, но неожиданный TUN path лучше ловить через failing `HevTunnelGateway` или отдельные assertions.

**Action Items:**
- Следующая фича по очереди: ByeDPI strategy scan/apply.
- Для расследования ByeDPI strategy scan соблюдать порядок проекта: сначала `wiki-find`, затем `.codex/Контекст/*_ANALYSIS.md`, только потом новые гипотезы или саб-агенты.

### Session (14:16)

**Context:** Исправлялся пакет из трех P2-дефектов в Ozero и доводился до push/CI на ветке `dev`.

**Key Exchanges:**
- Исправлены DOMAIN regex patterns в `runContainer`: поддержка leading whitespace, одинарных/двойных кавычек для non-empty array и удаление indented DOMAIN keys.
- В `SshjTransport.exec` изменен порядок чтения stdout/stderr: drain futures стартуют сразу после `session.exec`, до `cmd.join`, чтобы remote process не блокировался на заполненном channel buffer.
- Добавлен `finally shutdownNow` для lifecycle executor, чтобы исключения до обычного shutdown не оставляли threads.
- Contract tests обновлены под новые regex и новый SSHJ transport contract.
- Локальный style-gate сначала нашел только ktlint-formatting; после точечных правок gate стал зеленым.
- Саб-агент code review был запущен, но завершился без ответа; работа продолжена без ожидания.

**Decisions Made:**
- Старый SSH contract test, требовавший `join()` до `readFully`, признан вредным и заменен тестом, закрепляющим drain-before-join поведение.
- Compile/full tests локально не запускались по проектному правилу; проверка ограничена локальным style-gate, остальное через CI.
- `.memory` была чистой, поэтому в коммит вошли только код и тесты.
- Untracked files не трогались.

**Lessons Learned:**
- Source-based contract tests чувствительны к точной форме строк, поэтому перед Gradle-gate полезно быстро проверять новые substrings.
- PowerShell quoting может ломать предварительные `rg` команды без связи с корректностью diff.
- `gh`/watcher могут падать по сетевому `EOF` или локальному timeout; это не равно CI failure, нужно перепроверять через `run list`/jobs.

**Action Items:**
- Забрать логи failing CI jobs после terminal status и чинить первый подтвержденный корень.
- Текущий push `dev` на SHA `9bd96123`: push workflow зеленый, PR-run `27684624827` еще мониторился; были видны failures в `Tests — engine-masterdns`, `Tests — singbox + extra modules` и одном app test shard.

### Session (14:54)

**Context:** Продолжалась работа над ускоренным закрытием покрытия и CI-стабилизации в Ozero на ветке `dev`.

**Key Exchanges:**
- После зеленого CI пользователь попросил ускориться и закрыть за текущий рывок 50%+ вместо прежних 15-20%.
- Ассистент решил перейти к более быстрому режиму: атомарные фиксы делать inline, сабагентов оставлять только для независимых крупных веток, не простаивать во время CI.

**Decisions Made:**
- Следующий фокус выбран на user-facing слой настроек и локальных данных: settings, servers, split tunnel, backup.
- Решено использовать существующие test harness’ы и фейки, чтобы быстро расширять покрытие без переписывания инфраструктуры.

**Lessons Learned:**
- Избыточная церемония с сабагентами и ожиданием CI без параллельной подготовки следующего пласта заметно замедляли темп.
- Для Kotlin assertion inference иногда нужна явная типизация ожидаемых списков, например `listOf<String?>(command)`.
- В shell-скриптах MasterDNS raw single quote внутри `sh -c '...'` ломает quoting; нужно использовать shell-safe escape pattern.

**Action Items:**
- Дочитать реализации и текущие тесты ViewModel для settings, servers, split tunnel и backup.
- Дописать сценарии покрытия в существующем стиле без новых зависимостей.
- Перед следующим push снова прогнать разрешенный style-gate и дождаться terminal CI status.

### Session (15:40)

**Context:** Работали над повышением покрытия и надежности тестов Ozero, особенно вокруг runtime/settings, backup/import lifecycle и CI validation.

**Key Exchanges:**
- CI несколько раз мониторился до terminal status; были повторяющиеся EOF от GitHub API, но runs доходили до green.
- Пользователь остановил практику push на каждый мелкий тест и попросил оценить прогресс и ускориться.
- Ассистент оценил прогресс покрытия/стабилизации примерно в 45-50% и предложил дальше собирать изменения крупными пакетами.

**Decisions Made:**
- Перестать пушить микрокоммиты; вместо этого группировать тестовые изменения в более крупные пакеты: один локальный style-gate, один commit, один push, один CI на пакет.
- Субагентов не использовать для атомарного Kotlin-тестинга; подключать только при 2+ независимых крупных направлениях анализа.

**Lessons Learned:**
- `SettingsRepositoryImpl` валидировал DNS/hosts только в setter’ах, но read path доверял raw DataStore CSV; поврежденные/ручные prefs могли отдавать invalid DNS/hosts в runtime.
- Для backup/import важен state-machine coverage: cancel/failure должны сбрасывать pending import и не позволять повторно применить stale backup.
- GitHub API может нестабильно отдавать EOF во время CI polling, при этом сам push/run может быть нормальным.

**Action Items:**
- Собрать следующий крупный локальный пакет тестов: backup/import lifecycle, runtime observers, MainViewModel/start-stop visible flows.
- После пакета прогнать локальный `ktlintCheck detekt --continue`, затем сделать один commit/push и дождаться CI.
- Цель следующего рывка: поднять прогресс примерно до 60-65% без промежуточных push на каждый файл.

### Session (15:44)

**Context:** Продолжается расширение тестового покрытия Ozero по основной бизнес-логике, с переходом от микропушей к более крупным пакетам изменений.

**Key Exchanges:**
- Пользователь потребовал перестать пушить каждый мелкий тест, оценить прогресс и ускориться.
- Ассистент оценил текущий прогресс примерно в 45-50% и признал главный недобор: e2e/UI-инструментальный слой почти не покрыт.
- После пуша пакетного коммита ассистент начал ждать CI, пользователь резко остановил ожидание и потребовал продолжать работу параллельно с CI.

**Decisions Made:**
- Больше не пушить микрокоммиты; собирать изменения крупными пакетами: один локальный style-gate, один commit/push/CI на пакет.
- CI не ждать без дела: продолжать следующий локальный пакет, а статус CI проверять коротким polling позже.
- Следующий фокус после backup/settings пакета: runtime observers, MainViewModel и start-stop visible flows, потому что это дает высокий прирост доверия к бизнес-логике.

**Lessons Learned:**
- В `BackupViewModelTest` нужны state-machine проверки pending import lifecycle: cancel/failure/success не должны оставлять stale pending backup и не должны приводить к повторному import.
- `onMoveAutoPriority(delta=0)` был обнаружен как потенциальный лишний settings write без пользовательского изменения; это стоит фиксировать no-op тестом.
- `.memory` dirty после сессии нужно включать в ближайший рабочий коммит вместе с кодом, а не оставлять отдельно.

**Action Items:**
- Продолжить локальную работу над следующим крупным тестовым пакетом: runtime observers, MainViewModel/start-stop flows.
- Проверить CI для последнего пакетного push позже, без блокировки основной работы.
- Двигаться к следующему рубежу покрытия примерно 60-65% одним или несколькими крупными пакетами, не микропушами.

### Session (16:01)

**Context:** Продолжалась работа над повышением покрытия бизнес-логики и пользовательских сценариев Ozero тестами, с фокусом на настройки, runtime/start sequence и UI/navigation contracts.

**Key Exchanges:**
- Пользователь попросил ускориться и делать более крупные batch-и, не дробя работу на один тест за раз.
- Был сделан batch `4a9245f9 FIX: Исправление отказа VPN и runtime replay`: исправлен `MainViewModel.onVpnPermissionDenied()`, добавлены regression-тесты для `WARP`/`URNETWORK`, runtime pending replay и `StartSequence` public `run()` сценарий.
- Был сделан batch `90876a9e FIX: Нормализация raw настроек приложения`: исправлена нормализация raw `URNETWORK_COUNTRY_CODE`, добавлены тесты raw settings и observable state в `OnboardingViewModel`.
- Следующий batch начат по UI/navigation coverage: добавляются JVM route-mapper тесты для всех `EngineId` и Android Compose тесты навигационных callbacks `SettingsScreen`.

**Decisions Made:**
- Работать более крупными batch-ами: сначала набирать несколько связанных тестов/фиксов, затем один style-gate, commit и push.
- Не ждать CI без дела: после push только коротко проверять статус Actions и продолжать работу.
- Для UI/navigation coverage использовать стабильные `testTag`, а не поиск по русскому тексту, чтобы тесты не были хрупкими к локалям.
- Не добавлять тесты “ради количества” в уже плотно покрытые области watchdog/start sequence/settings, если нет реального пробела.

**Lessons Learned:**
- `setUrnetworkCountryCode()` нормализовал значение при записи, но прямое чтение raw/старого DataStore значения возвращало его как есть; это могло ломать UI/engine config после миграций или restore.
- `SettingsRepositoryImpl` должен нормализовать raw `URNETWORK_COUNTRY_CODE` при чтении и игнорировать invalid raw значения.
- Android Compose тесты настроек лучше опирать на стабильные test tags, а не на локализованный текст.
- PowerShell `||` вызвал сбой поисковой команды; для локальных команд в этом окружении нужно учитывать синтаксис PowerShell.

**Action Items:**
- Завершить текущий UI/navigation batch: поправить ktlint формат в androidTest, повторно запустить `.\gradlew.bat ktlintCheck detekt --continue`, затем commit/push.
- Продолжать закрывать цель: e2e/UI-инструментальные сценарии и engine-specific flows остаются основным долгом.

### Session (16:15)

**Context:** Продолжалась работа над доведением покрытия бизнес-логики Ozero e2e/integration/unit тестами до 100%.

**Key Exchanges:**
- Добавлен regression-тест backup export: если `export()` и encryption успешны, но запись в `ContentResolver` падает, VM должна показать Error, а не Success.
- Запушены коммиты в `dev`: `c6d66eef`, `e8d45f96`, `8d8fef95`, затем `65a695d2`.
- Добавлены regression-тесты для ByeDPI runtime observer: рестарт при изменении только `byedpiUseUiMode`, даже если `byedpiWinningArgs` не менялись.
- Пользователь потребовал продолжать работу до 100%; следующий выбранный участок — UI-level покрытие auto-mode/priority.

**Decisions Made:**
- Не дублировать уже покрытую backup-сериализацию, а усиливать user-facing orchestration: ошибки записи/чтения, pending import, поведение ViewModel.
- Не добавлять ViewModel-тест на empty import confirm, потому что UI уже запрещает confirm с пустым набором категорий; такой тест был бы слабым.
- Для ByeDPI добавить именно regression на `byedpiUseUiMode`, так как код учитывал флаг, но отдельного теста на “args same, mode changed” не было.

**Lessons Learned:**
- В `BackupViewModelTest` уже есть happy/error/pending cases; полезнее закрывать trust gaps, а не наращивать дубли.
- В старых тестах есть проблемы с mojibake/кодировкой, поэтому новые тесты лучше называть ASCII и вставлять по стабильным якорям.
- `gh run list` после push может падать с GitHub API `EOF`; CI для последнего SHA не был проверен.

**Action Items:**
- Продолжить покрытие app-level e2e/Compose сценариев backup/import и auto-mode settings/priority.
- Добрать runtime/startup edge cases вокруг `StartSequence`.
- Перед каждым push продолжать гонять `.\gradlew.bat ktlintCheck detekt --continue`.

