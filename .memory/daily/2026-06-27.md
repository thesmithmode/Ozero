# Daily Log: 2026-06-27

## Sessions

### Session (singbox-upgrade-fix)

Context: Updated `dev`, fixed Room migration schema mismatch for sing-box profiles, and reviewed sing-box behavior against the local Karing reference.

Key Changes:
- Added `@ColumnInfo(defaultValue = "0")` to `ProxyProfile.lastProbeAt` so Room entity schema matches `MIGRATION_2_3`.
- Added regression coverage for the `lastProbeAt` SQL default annotation.
- Made manual/file sing-box imports create local groups with `autoUpdate = false`.
- Made subscription auto-update skip groups without a subscription URL.
- Made non-auto sing-box TUN attach fail when routed warmup probing fails instead of reporting success.
- Preserved `plugin-opts` from `ss://` links.
- Serialized comma-separated HTTP/H2 transport hosts as multiple JSON array entries.

Decisions:
- Did not enable `splithttp`, `kcp`, or `quic` based only on Karing parity because project memory links `splithttp` to device crashes; support needs separate runtime proof.
- Kept the fixes minimal and covered by focused regression tests instead of widening sing-box transport support.

Validation:
- `git diff --check` passed.
- `.\gradlew.bat ktlintCheck detekt --continue` passed outside the sandbox after clearing stale `buildSrc` generated cache.

Lessons Learned:
- Room manual migrations must keep SQL defaults synchronized with entity annotations, or upgraded databases fail schema validation.
- Sing-box "connected" state is not enough; selected non-auto profiles need routed readiness proof before being treated as usable.
- Local imported profiles and remote subscriptions need separate update semantics; an empty subscription URL must never enter the refresh path.

### Session (ci-warp-cleanup)

Context: Followed failed dev CI after pushing sing-box fixes and reviewed a reported WARP proxy-mode foreground service leak.

Key Changes:
- Fixed the `lastProbeAt` annotation test by putting Room `ColumnInfo` on the Java field with `@field:ColumnInfo`.
- Fixed HTTP/H2 host array serialization to avoid spaces in generated JSON arrays.
- Added failed WARP proxy-start cleanup in `RealWarpSdkBridge` for thrown `startProxy` and negative proxy handles.
- Added regression tests proving failed proxy start calls proxy reset, stop, and runtime close.

Validation:
- `.\gradlew.bat ktlintCheck detekt --continue` passed outside the sandbox.

Lessons Learned:
- Tests that reflect Java fields need Kotlin use-site targets when the production annotation must exist on the field.
- WARP proxy-mode start can raise a foreground service session before the handle exists; every failure after runtime entry must explicitly close the remote runtime.
