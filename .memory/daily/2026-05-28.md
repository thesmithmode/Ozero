# Daily Log: 2026-05-28

## Sessions

## Memory Maintenance

### Session (11:43)

**Context:** Проверка миграции memory wiki на Codex.

**Key Exchanges:**
- Пользователь попросил сохранить факт smoke test миграции wiki на Codex.
- Подтверждено, что Codex hooks используют общий transcript parser и Codex backend.

**Decisions Made:**
- Зафиксировать smoke test как подтверждение работоспособности миграции wiki на Codex.

### Session (12:09)

**Context:** Проверялся smoke-тест Codex-only wiki hook.

**Key Exchanges:**
- Подтвержден прежний алгоритм memory: transcript -> flush -> daily.

**Decisions Made:**
- Codex-only hook smoke считается пройденным.
### Session (14:25)

**Context:** Пользователь поручил довести доставку до конца: проверить незакоммиченные/незапушенные изменения, закоммитить, запушить, дождаться зеленого CI, затем смержить в main и проконтролировать успешную сборку релиза.

**Key Exchanges:**
- Пользователь задал полный workflow-контракт завершения: задача считается выполненной только после успешно собранного релиза.
- Ассистент подтвердил порядок действий: проверка ветки/`git status`/незапушенных коммитов/статуса CI и дальнейшее выполнение цепочки до релиза.

**Decisions Made:**
- Принято явное условие завершения: финальный критерий успеха — успешная сборка релиза (а не только зеленый CI).
- Принята последовательность выполнения: сначала аудит текущего git/CI состояния, затем push/merge/release-monitoring.

**Action Items:**
- Проверить рабочее дерево и незапушенные коммиты.
- Закоммитить и запушить изменения.
- Дождаться зеленого CI (или подтвердить, что текущий зеленый статус валиден для пуша).
- Выполнить merge в `main`.
- Отследить релизную сборку до успешного завершения.

### Session (14:57)

**Context:** Пользователь поставил цель довести текущие изменения до завершенного релизного цикла: проверить состояние git, выполнить нужные коммиты/пуши, дождаться зеленого CI, смержить `dev -> main` и подтвердить успешную release-сборку.

**Key Exchanges:**
- Ассистент подтвердил пошаговый план: проверка статусов git/CI, затем merge и мониторинг релиза.
- В процессе выявлен блокер: незакоммиченный файл `.memory/daily/2026-05-28.md` мешал переходу на `main`.
- Пользователь уточнил правило на будущее: memory-коммит сам по себе не влияет на CI, так как это текстовый файл вне тестового покрытия.

**Decisions Made:**
- Зафиксировать `.memory` отдельным коммитом в `dev` (`2fba4ca7`, `CHORE: Обновление памяти`), чтобы снять блокировку workflow и соблюсти правило по памяти.
- После зеленого CI для этого SHA выполнить `fast-forward merge dev -> main` и пуш в `main`.
- Отслеживать конкретный release-run до terminal status; релиз `#26572541285` завершился `success`.

**Lessons Learned:**
- Изменения только в `.memory/` не требуют избыточной перестраховки по CI-рискам.
- Даже служебный незакоммиченный файл может заблокировать checkout/merge-процедуру и должен фиксироваться сразу по правилам репозитория.

**Action Items:**
- В следующих сессиях учитывать, что memory-only коммиты не являются существенным риском для CI, но должны быть своевременно закоммичены вместе с рабочим процессом.

### Session (15:35)

**Context:** Пользователь планировал верифицировать и исправить ошибки релиза Ozero, провести dev C9 до зеленого CI, затем смержить в main для релизной сборки.

**Key Exchanges:**
- Пользователь приложил `ozero_trace.log` и перечислил критичные регрессии: ByeDPI не работает, WARP отвалился из-за ByeDPI, URnetwork не стартует, sing-box падает/не переключает серверы/не работает автовыбор/ломает VLESS-подписку с `Chain validation failed`, добавление proxy chain вызывает краш.
- Пользователь указал, что ByeDPI стабильно работал в релизе `0.2.11`, и предложил при необходимости сравнить с ним.
- Пользователь подозревает, что URnetwork мог быть сломан при переходе с ветки `0.2.*` на `1.0.*`, возможно из-за регистрации.
- Пользователь ожидает, что URnetwork при отсутствии пиров должен продолжать поиск около 5 минут, а не дропаться примерно за минуту.
- Пользователь отметил неясность по работе раздачи трафика через URnetwork relay при активном другом движке.

**Decisions Made:**
- Критерий завершения задачи: релиз собран, все найденные проблемы исправлены корректно, dev C9 зеленый, затем merge в main для релизной сборки.
- Нужно искать корневые причины по логам и истории работающего релиза, а не ограничиваться симптоматическими правками.

**Lessons Learned:**
- ByeDPI и WARP связаны регрессионно: поломка ByeDPI уже раньше приводила к поломке WARP, это нужно сохранить как повторяющийся паттерн расследования.
- Для URnetwork важен сценарий продолжительного поиска пиров: быстрый drop примерно за минуту считается подозрительным поведением даже если пиров нет.
- sing-box имеет несколько связанных симптомов, вероятно требующих общего расследования цепочек, серверного переключения, подписок VLESS и proxy chain.

**Action Items:**
- Проанализировать `ozero_trace.log`.
- Сравнить ByeDPI с релизом `0.2.11`, если по текущему коду причина не очевидна.
- Проверить регистрацию и startup flow URnetwork после перехода с `0.2.*` на `1.0.*`.
- Исправить sing-box hot switch, auto-select, VLESS chain validation и краш при добавлении proxy chain.
- Проверить relay-сценарий URnetwork при активном другом движке.
- Провести исправления через dev C9 до зеленого CI и затем выполнить merge в main для релизной сборки.

### Session (15:44)

**Context:** Проверка релизных регрессий Ozero по `ozero_trace.log`: ByeDPI/WARP/URnetwork/sing-box перед dev CI и релизным merge.

**Key Exchanges:**
- Пользователь сообщил множественные регрессии: ByeDPI не работает с версии после `0.2.11`, из-за него ломается WARP, URnetwork быстро дропается, sing-box падает на hot switch/autoselect/VLESS/добавлении proxy chain.
- Стартовая ветка была `main`, рабочее дерево уже содержало изменение `.memory/daily/2026-05-28.md`; работа перенесена на `fix/release-regressions` от `dev`.
- По логу выделены корни-кандидаты: ByeDPI зависает на повторном старте после неудачного `stop`; URnetwork обрывается по `awaitReady` через 45 секунд; sing-box генерирует неподдерживаемый VLESS flow `xtls-rprx-vision-udp443`; WARP показывает stale/reused tunnel через старые UAPI `.sock`.

**Decisions Made:**
- Для ByeDPI: дать `stop` больше времени и корректно освобождать native/proxy состояние перед повторным стартом.
- Для WARP: чистить все WARP UAPI `.sock`, чтобы не читать handshake со старого `tunN.sock`.
- Для URnetwork: увеличить ожидание готовности до 5 минут, чтобы движок продолжал искать peer’ов вместо раннего drop.
- Для sing-box: нормализовать неподдерживаемый VLESS flow, который libbox отвергает как `xtls-rprx-vision-udp443`.
- Локальные тесты не запускать по правилам репо; валидация должна идти через статические проверки и CI.

**Lessons Learned:**
- WARP уже повторяет старый класс бага: stale socket/tunnel state может ломать следующий старт после ByeDPI.
- URnetwork может выглядеть как отсутствие peer’ов, но ранний timeout `awaitReady` маскирует реальные проблемы регистрации/старта.
- sing-box VLESS flow надо валидировать/нормализовать до передачи в libbox, иначе рабочая подписка падает на chain validation.

**Action Items:**
- Перепроверить внесенные изменения по diff/search после sandbox-сбоев.
- Проверить историю `v0.2.11` для ByeDPI stop behavior.
- Довести изменения до commit, затем прогнать dev CI до terminal status.
- После зеленого dev CI выполнить merge в `main` только по релизному workflow проекта и дождаться сборки релиза.

### Session (16:21)

**Context:** Продолжение фикса релизных регрессий по `ozero_trace.log` перед dev CI и релизным merge.

**Key Exchanges:**
- Проверен trace-log: ByeDPI зависает после stop timeout 2000ms, URnetwork падает по `awaitReady` через 45000ms, sing-box валится на `unsupported flow: xtls-rprx-vision-udp443`, WARP стартует с подозрительным stale UAPI socket состоянием.
- Сравнение с `v0.2.11` показало, что ByeDPI уже имел двухфазный stop с `STOP_GRACE_MS`, но в текущем orchestration не хватало per-engine stop timeout.
- BootDiag показал ANR на tap в `MainActivity` после sing-box сценариев; UI chain add строил обычный `DropdownMenu` по всем профилям подписки.

**Decisions Made:**
- ByeDPI: `stopTimeoutMs()` увеличен до 5000ms, чтобы ChainOrchestrator не отпускал mutex раньше двух фаз JNI drain.
- WARP: перед `awgTurnOn/startProxy` чистятся все `uapiPath/sockets/*.sock`, не только socket текущего tunnelName.
- URnetwork: initial peer discovery timeout увеличен до 300000ms, progress logging растянут пропорционально.
- sing-box: VLESS flow `xtls-rprx-vision-*` нормализуется до `xtls-rprx-vision`, неизвестный flow не попадает в config.
- sing-box auto-select: ping/refresh больше не сбрасывает `SELECTED_AUTO=-1` на конкретный профиль.
- sing-box proxy chain UI: выбор добавляемого профиля перенесен из eager `DropdownMenu` в bounded `LazyColumn` dialog.

**Validation:**
- `git diff --check` зеленый.
- `./gradlew.bat ktlintCheck --continue` зеленый после исправления одного ktlint замечания.
- Локальные tests/build не запускались по правилу проекта; regression/sentinel tests добавлены для CI.

**Lessons Learned:**
- ByeDPI stop timeout должен учитывать внутренние native drain окна, иначе следующий старт может войти в busy guard и ломать соседние engine transitions.
- WARP stale `tunN.sock` опасен даже если собственный `<tunnel>.sock` удален: handshake fallback может читать старую UAPI сессию.
- sing-box auto-select в Ozero хранится как режим `selected_profile_id=-1`; любые фоновые latency probes обязаны не менять этот sentinel.
- Большая подписка делает eager popup для proxy chain источником ANR; такие списки должны быть lazy и ограничены по высоте.

**Action Items:**
- Закоммитить код вместе с `.memory/`.
- Запушить `fix/release-regressions` в `dev`, дождаться terminal CI status.
- После зеленого dev CI выполнить merge `dev -> main` и дождаться release workflow.

### Session (16:11)

**Context:** Работа над регрессионными исправлениями релиза Ozero по логам: ByeDPI, WARP, URnetwork, sing-box и CI.

**Key Exchanges:**
- Пользователь напомнил использовать `/wiki-find`, `.memory/` и прошлые релизы вроде `v0.2.11`, где ByeDPI работал.
- Пользователь указал, что перед прогоном CI нужно убедиться, выполнены ли все задачи исходного промпта.
- Ассистент признал, что перед первым CI не все пункты были доказательно закрыты: часть выводов по URnetwork и sing-box требовала дополнительного evidence.

**Decisions Made:**
- Создана ветка `fix/release-regressions` от `dev`, изменения закоммичены как `79a2fc95`, затем fast-forward merge в `dev` и push для CI.
- Исправления сфокусированы на корнях из trace-log: ByeDPI stop timeout, cleanup stale WARP UAPI sockets, увеличение URnetwork awaitReady до 5 минут, нормализация VLESS flow для sing-box.
- Для sing-box auto-select решено не сбрасывать `selected_profile_id = -1` после ping/latency refresh.
- Для UI добавления профиля в цепочку решено заменить синхронный `DropdownMenu` на bounded lazy dialog, чтобы не блокировать main thread на больших подписках.

**Lessons Learned:**
- В `v0.2.11` у ByeDPI уже был двухфазный stop, но текущий orchestrator с общим 2-секундным timeout может обрывать легальный stop перед JNI drain, из-за чего следующий старт попадает в занятый native guard.
- WARP может переиспользовать stale tunnel/UAPI socket, если чистится не весь набор `.sock`.
- URnetwork issue нельзя закрывать только как “нет пиров”: в trace был отдельный сигнал `registry id not resolved after fetch+register`.
- CI упал на Kotlin compilation из-за вывода типа `MutableStateFlow<MutablePreferences>` вместо ожидаемого `Preferences` в fake DataStore; нужен явный generic.

**Action Items:**
- Повторно пройти исходный пользовательский промпт как checklist с evidence по каждому пункту.
- Исправить CI compile failure и снова запустить/мониторить CI до terminal status.
- Не считать URnetwork registration/relay и sing-box hot switch/autoselect полностью закрытыми без подтверждения кодом, логом или CI.

### Session (16:47)

**Context:** Закрывали release-regression фиксы Ozero до публикации релиза v1.0.3.

**Key Exchanges:**
- Пользователь напомнил использовать `/wiki-find`, `.memory/` и прошлые релизы вроде `0.2.11`, где ByeDPI работал.
- Пользователь указал, что перед CI нужно проверить выполнение всех задач из исходного промпта.
- Был признан преждевременный первый CI: часть пунктов была покрыта кодом/тестами, но не доказана checklist/evidence перед запуском.

**Decisions Made:**
- Не трогать `main`/release до зелёного `dev` CI и проверки checklist по исходному промпту.
- Считать URnetwork relay подтверждённым по коду и существующим тестам, но честно зафиксировать отсутствие device-runtime пруфа по живым пирам.
- CI fail `engine-warp` на Maven HTTP 403 признан инфраструктурным, поэтому сделан rerun failed jobs без изменения кода.

**Lessons Learned:**
- Перед CI нужен explicit checklist исходного промпта с evidence по каждому пункту, иначе возможна ложная уверенность готовности.
- `gh run watch` может падать на jobs endpoint 404; run-level polling и check-runs подходят для terminal-status monitoring.
- Fake DataStore test может вывести слишком узкий тип `MutablePreferences`; нужен явный generic `Preferences`.

**Action Items:**
- Для похожих release-regression задач сначала поднимать `.memory/knowledge`, relevant `.codex/Контекст/*_ANALYSIS.md` и прошлые рабочие релизы, затем фиксить и валидировать checklist.

### Session (16:52)

**Context:** Пользователь попросил открыть встроенный браузер и посмотреть выдачу Avito по запросу «Инфографика».

**Key Exchanges:**
- Пользователь: открыть встроенный браузер по ссылке Avito и проверить результаты поиска «Инфографика».

**Action Items:**
- Открыть встроенный браузер и просмотреть выдачу Avito по запросу «Инфографика» по предоставленной ссылке.

### Session (16:54)

Context: Отладка регрессий в Ozero, прохождение CI/dev→main и выпуск релиза; затем запрос на углублённое код-ревью фиксов.

Key Exchanges:
- Assistant: `gh run watch` падает на jobs endpoint (404), поэтому мониторинг переведён на polling по run-level (`gh run view`) при сохранении terminal status контроля.
- User: напомнил использовать /wiki-find и `.memory` (в т.ч. релиз 0.2.11 для ByeDPI); Assistant подтвердил опору на `.memory/knowledge` и сравнение с v0.2.11 при необходимости.
- User: спросил, все ли пункты исходного промпта были закрыты перед CI; Assistant признал, что первый CI был запущен преждевременно, и пообещал пройти чеклист с evidence по каждому пункту.
- Assistant: точечный compile fail в тестах (MutableStateFlow вывел `MutablePreferences`, а fake DataStore ожидал `Preferences`) — исправлено явным generic; локально прогнан только быстрый `ktlintCheck --continue`.
- Assistant: в dev CI упал `Tests — engine-warp` из‑за внешнего Maven/Plugin Portal HTTP 403 (инфраструктурный фейл), код не менялся; сделан rerun failed job, после чего dev CI стал зелёным.
- Assistant: main был fast-forward’нут до dev и запушен для запуска release workflow; релизный workflow прошёл успешно и опубликован релиз `v1.0.3`.
- User: остановил и потребовал ревью архитектурной корректности фиксов (не костыли), особенно по ByeDPI/URnetwork/sing-box; Assistant согласился вернуться на dev и провести ревью.

Decisions Made:
- Перейти с `gh run watch` (jobs endpoint нестабилен/404) на polling по run-level статусу, чтобы не терять terminal event monitoring.
- Не чинить код под внешний HTTP 403 в CI; вместо этого rerun failed job.
- Не трогать main/release до `dev` CI `success`; после зелёного dev выполнить sync main (fast-forward) и запустить release workflow.
- После пользовательского стопа — вернуться на `dev` и провести углублённое код-ревью собственных изменений, готовность переосмыслить решения при выявлении рисков.

Lessons Learned:
- Kotlin type inference в тестах может “утечь” в `MutablePreferences` и ломать ожидания fake DataStore (`Preferences`); фикс — явная типизация generic.
- CI jobs endpoint может быть нестабилен (404); надёжнее опираться на run-level статус и check-runs.
- Не все падения CI являются регрессией кода: внешние 403 на скачивание артефактов требуют rerun, а не изменений в репозитории.
- Увеличение stop timeout для engine (ByeDPI) потенциально корректно как owning-layer решение (`EnginePlugin.stopTimeoutMs`), но требует ревью на предмет маскирования реальных hang/JNI блокировок.
- По URnetwork выявлен риск: релизный коммит фактически изменил только timeout `awaitReady`, не добавив нового “корневого” фикса регистрации/peer discovery; нужно проверить полноту ранее известных инвариантов (main-thread SDK calls, shared NetworkSpace, `connectBestAvailable`, peerCount).

Action Items:
- На ветке `dev` провести архитектурное ревью изменений по ByeDPI/WARP/URnetwork/sing-box: подтвердить root-cause фиксы vs таймаут/зазоры, оценить риски и при необходимости доработать.

### Session (17:20)

Context: Пользователь остановил релизную ветку после v1.0.3 и потребовал вернуться на `dev`, провести код-ревью собственных фиксов и не считать решения верными без проверки архитектуры.

Key Exchanges:
- User: спросил, проводилось ли ревью перед релизом, и отдельно указал риск, что ByeDPI мог быть закрыт временным зазором, URnetwork непонятно чем исправлен, а sing-box мог остаться нерабочим.
- Assistant: вернулся на `dev`, признал, что релизный URnetwork-коммит менял только timeout ожидания peer discovery, и провёл повторную проверку против `.memory/knowledge` и reference URnetwork app.
- Assistant: подтвердил, что ByeDPI stop timeout является owning-layer контрактом `EnginePlugin.stopTimeoutMs()`, потому что `ByeDpiEngine.stop()` легально имеет две фазы drain по `STOP_GRACE_MS`, а общий orchestrator timeout 2000ms обрывал этот путь до завершения.
- Assistant: нашёл архитектурный пробел URnetwork: readiness был завязан только на `grid.windowCurrentSize`, тогда как reference app считает соединение по `ConnectViewController.connectionStatus`.
- Assistant: нашёл дополнительный sing-box пробел: `buildAutoChainConfig()` не фильтровал unsupported transports, хотя standalone auto-select уже фильтрует их через `isSupportedBean()`.

Decisions Made:
- Для URnetwork добавить отдельный `connectionStatus()` в `UrnetworkSdkBridge`, подписку на `addConnectionStatusListener` в `RealUrnetworkSdkBridge`, очистку `Sub` в stop и принимать `CONNECTED` как `awaitReady` даже если grid peer count ещё 0.
- Для sing-box auto-chain применить тот же filter unsupported transports, что и в обычном auto-select config, чтобы chain/relay path не передавал `splithttp` в libbox.
- Оставить ByeDPI/WARP релизные решения как архитектурно оправданные: ByeDPI timeout согласован с реальной stop-длительностью engine, WARP `.sock` cleanup закрывает stale UAPI socket root cause.

Lessons Learned:
- URnetwork grid peer count не эквивалентен SDK connection status; использовать его как единственный gate старта неправильно и приводит к false timeout.
- У sing-box одинаковая фильтрация поддерживаемых транспортов должна быть общей для standalone auto-select и auto-chain; иначе relay/chain path снова генерирует невалидный config.
- Перед релизом по регрессионным engine-fix задачам нужен explicit self-review checklist с root-cause/evidence по каждому движку, особенно когда первый фикс выглядит как timeout.

Action Items:
- Прогнать dev CI после коммита review-fix изменений и мониторить до terminal status.
- Если на устройстве sing-box продолжит падать после `checkConfig passed`, нужен tombstone из `/data/user/0/ru.ozero.app/files/debug/`, потому что это уже native runtime crash, не Kotlin config builder.

