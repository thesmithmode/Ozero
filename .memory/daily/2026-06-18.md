# Daily Log: 2026-06-18

## Sessions

## Memory Maintenance

### Session (11:03)

**Context:** Пользователь исправлял падение MasterDNS при deploy/config_init по логам и скриншоту.

**Key Exchanges:**
- Пользователь указал, что MasterDNS снова падает, и потребовал сначала смотреть конкретную проблему в логах/скрине, а уже потом искать аналогичные.
- По логу подтвержден сбой `run_failed|phase=config_init|exit=2|error=awk...`: контейнер не создавался, поэтому `docker logs masterdns-ozero` закономерно давал `No such container`.

**Decisions Made:**
- Корень исправлен в генераторе deploy-скрипта `MasterDnsDockerScripts.kt`: проблемный awk regex `[^[]*\[` заменен на переносимый разбор через `index(line, "[")` и `substr(...)`.
- Добавлен regression-test в `MasterDnsDockerScriptsContractTest.kt`, запрещающий возвращение опасного regex в generated deploy script.
- Соседние MasterDNS awk-фрагменты проверены: аналогичный опасный шаблон найден только в двух местах миграции `DOMAIN`, оба исправлены.

**Lessons Learned:**
- В MasterDNS `config_init` выполняется во временном контейнере до создания постоянного контейнера; если init-скрипт падает, последующий `docker logs masterdns-ozero` неинформативен, потому что контейнера еще нет.
- Regex `[^[]*\[` в awk непереносим и может ломаться как синтаксическая ошибка на некоторых реализациях awk; для поиска literal `[` надежнее использовать `index/substr`.

**Action Items:**
- Изменения проверены узким тестом `:engine-masterdns:testDebugUnitTest --tests ru.ozero.enginemasterdns.deploy.MasterDnsDockerScriptsContractTest --continue`: 54 теста прошли.
- Style gate `ktlintCheck detekt --stacktrace --continue` прошел.
- В worktree оставались чужие untracked файлы: `.codex/config.toml`, `.tmp/`, `_ozero_tmp/`, `singbox-core/libs/libbox.jar`; они не трогались.

### Session (11:10)

**Context:** Пользователь запросил быстрый аудит оставшихся high-value gaps в app UI/instrumented/e2e тестах Ozero без редактирования файлов.

**Key Exchanges:**
- Нужно проверить `app/src/main/java/ru/ozero/app/ui` и `app/src/androidTest/java`.
- Фокус аудита: onboarding, main connect/disconnect UI, settings/per-engine screens, backup, strategy, split tunnel, logs/stats.
- Требуемый результат: короткий список 5-8 gaps с `path:line` evidence и предложением теста, без хрупких приватных тестов.
- Запрещено использовать `gh`.

**Decisions Made:**
- Работать как review-only: читать код и тесты, не вносить изменения.
- Сначала собрать карту существующих UI-экранов и instrumented/e2e тестов, затем сверить зоны покрытия и выделить только high-value интеграционные пробелы.

**Lessons Learned:**
- В `app` есть много UI-экранов, но инструментированное покрытие выглядит фрагментарным: часто покрыты отдельные composables, а не пользовательские сценарии.
- Для полезных gaps важно привязываться к навигационным сценариям и публичным пользовательским flows, а не к хрупким деталям полей/внутренней реализации.

### Session (11:11)

**Context:** Пользователь просил без правок быстро найти оставшиеся высокоценные gaps в тестах runtime/VPN/business logic проекта Ozero.

**Key Exchanges:**
- Запрос ограничивал область: `app/src/test/java/ru/ozero/app/vpn`, `common-vpn` tests, `engine-*` tests, `StartSequence`, `EngineSettingsRestartObserver`, `Shutdown`, `Watchdog`, auto-mode.
- Требования: не редактировать файлы, не ждать CI, не использовать `gh`, вернуть 5–8 gaps с evidence `path:line` и предложением теста.

**Decisions Made:**
- Сначала строить карту существующих тестов и контрактов по указанным зонам, затем сравнивать кодовые ветки с тестовыми сценариями.
- Фокусировать итог не на общем покрытии, а на оставшихся незакрытых edge-cases и негативных сценариях в координаторах restart/watchdog/start/shutdown/auto-mode.

**Lessons Learned:**
- По `EngineSettingsRestartObserver` и `StartSequenceCoordinator` покрытие уже выглядит глубоким, поэтому gaps нужно искать через точное сопоставление веток кода и тестовых сценариев, а не по отсутствию тестовых классов.
- Для runtime/VPN координаторов полезнее искать неочевидные переходы состояния, race/ordering cases и негативные ветки, чем добавлять дублирующие happy-path тесты.

### Session (11:14)

**Context:** Пользователь просил без правок быстро проаудировать оставшиеся высокоценные gaps в тестах runtime/VPN/business logic проекта Ozero.

**Key Exchanges:**
- Пользователь ограничил область аудита: `app/src/test/java/ru/ozero/app/vpn`, `common-vpn tests`, `engine-* tests`, `StartSequence`, `EngineSettingsRestartObserver`, `Shutdown`, `Watchdog`, `auto-mode`.
- Пользователь явно запретил редактировать файлы, ждать CI и использовать `gh`; требовался короткий список 5–8 gaps с evidence `path:line` и предложением теста.
- Ассистент начал строить карту существующих тестов и контрактов по runtime/VPN, `common-vpn`, `engine-*`, координаторам start/restart/shutdown/watchdog/auto-mode.
- Ассистент отметил, что покрытие уже широкое, поэтому аудит должен быть gap-фазой: сверка кодовых веток с фактическими тестовыми сценариями, а не поиск по именам.

**Decisions Made:**
- Аудит вести как inventory без изменений файлов.
- Приоритизировать реальные незакрытые ветки и негативные сценарии в `StartSequenceCoordinator`, `EngineWatchdogCoordinator`, `ShutdownCoordinator`, `EngineSettingsRestartObserver` и auto-mode.
- Не считать gap’ом сценарий, если он уже покрыт контрактными, behavior или extra-тестами.

**Lessons Learned:**
- В проекте уже есть глубокое покрытие по `StartSequenceCoordinator` и `EngineSettingsRestartObserver`; дальнейшие gaps нужно искать на уровне edge-case веток и переходов состояния.
- Для такого аудита полезнее evidence map `path:line -> ветка/символ -> почему важно`, чем общий список тестовых классов.

**Action Items:**
- Завершить аудит и вернуть 5–8 высокоценных gaps с точными `path:line` и предложением конкретного regression/contract теста для каждого.

### Session (11:15)

**Context:** Работа над надежностью Sing-box: разбор trace с падением `checkConfig` и фиксы readiness/auto-select.

**Key Exchanges:**
- Пользователь сообщил, что Sing-box “ужасен, не работает”, и приложил `ozero_trace (23).log`.
- По логу найден первичный сбой: Sing-box стартовал, `libbox` загрузился, AIDL дошел до `checkConfig`, затем конфиг отклонен из-за `initialize outbound[4]: invalid public_key`.
- Пользователь дополнительно попросил закрыть P2 bug: redirect 301/302 в routed probes ошибочно считался успешной latency/readiness проверкой при `instanceFollowRedirects=false`.

**Decisions Made:**
- Фикс Reality `public_key` сделан в owning layer `singbox-config`, в `ConfigBuilder.isSupportedBean()`, чтобы битые профили не попадали ни в single, ни в auto-select, ни в chain runtime configs.
- Parser-тесты не менялись: парсер должен сохранять входные поля как есть, а runtime-валидация находится ниже, в config builder.
- Success-коды routed probe сужены с `200..399` до `200..299`, чтобы 3xx redirect не считался готовностью, но новый `200` fallback оставался разрешенным.
- Локальные Gradle test/build не запускались из-за правил проекта; запущен только разрешенный style-gate `ktlintCheck` + `detekt`.

**Lessons Learned:**
- Один невалидный Reality outbound внутри auto-select может завалить весь sing-box config validation, поэтому unsupported/broken profiles нужно фильтровать до `checkConfig`.
- Старые unit fixtures использовали короткие fake Reality keys вроде `pub`, хотя `libbox` требует валидный 32-byte base64url public key.
- При `instanceFollowRedirects=false` диапазон `200..399` небезопасен: 301/302 дает ложный успех readiness/latency probe без фактического успешного probe response.

**Action Items:**
- Дождаться CI/test validation для добавленных regression-тестов, так как локальные unit tests/build запрещены правилами проекта.
- Перед push учитывать, что в рабочем дереве были чужие dirty files (`.memory`, UI-файлы, `libbox.jar`, временные каталоги), не относящиеся к этим фиксам.

### Session (11:15)

**Context:** Пользователь искал оставшиеся высокоценные пробелы в app UI/instrumented/e2e тестах Ozero без редактирования файлов.

**Key Exchanges:**
- Нужно проверить `app/src/main/java/ru/ozero/app/ui` и `app/src/androidTest/java`, фокус: onboarding, main connect/disconnect UI, settings/per-engine screens, backup, strategy, split tunnel, logs/stats.
- Требовался короткий список 5-8 gaps с evidence `path:line` и предложением теста, без `gh` и без хрупких приватных тестов.

**Decisions Made:**
- Анализ строить через карту существующих UI-экранов и instrumented/e2e тестов, затем сверять сценарии по пользовательским зонам.
- Отбирать gaps как high-value интеграционные сценарии, а не просто отсутствие тестов на отдельные composables или поля.

**Lessons Learned:**
- Покрытие по фокусным зонам есть, но фрагментарное: часто тестируются отдельные composables, а не пользовательские переходы и связки экранов.
- Нужно отдельно проверить навигационные точки входа и per-engine instrumentation покрытие, так как оно может существовать только для части движков.

**Action Items:**
- Добрать точные `path:line` evidence по критичным UI-веткам и существующим тестам.
- Вернуть финальный список 5-8 тестовых gaps с конкретными предложениями instrumented/e2e тестов.

### Session (11:16)

**Context:** Пользователь просил без правок быстро найти оставшиеся высокоценные gaps в тестах runtime/VPN/business logic Ozero.

**Key Exchanges:**
- Нужно проверить `app/src/test/java/ru/ozero/app/vpn`, `common-vpn` tests, `engine-*` tests, `StartSequence/EngineSettingsRestartObserver/Shutdown/Watchdog/auto-mode`.
- Формат результата: короткий список 5–8 gaps с evidence `path:line` и предложением, какой тест добавить.
- Ограничения: не редактировать файлы, не ждать CI, не использовать `gh`.

**Decisions Made:**
- Аудит следует вести как gap-фазу: сравнивать реальные ветки ключевых runtime-классов с существующими тестовыми сценариями, чтобы не предлагать уже покрытые случаи.
- Приоритизировать высокоценные edge-cases в restart/watchdog/start/shutdown/auto-mode, а не общий coverage inventory.

**Lessons Learned:**
- Покрытие `StartSequenceCoordinator` и `EngineSettingsRestartObserver` уже широкое, поэтому новые gaps нужно искать в оставшихся негативных сценариях, гонках, ordering/in-flight состояниях и fallback-paths.
- Для этой зоны полезнее evidence-map по `path:line` и конкретный тестовый контракт, чем общая оценка покрытия.

### Session (11:23)

**Context:** Работа над Ozero: закрытие тестовых пробелов UI/runtime, проверка чужого незапушенного singbox batch, локальный style gate и push в `dev`.

**Key Exchanges:**
- Пользователь попросил доделать до логичного этапа работу текущего и другого агента, прогнать `linter + detect`, запушить свое и чужое при пригодности, затем подвести итоги и оценить прогресс.
- Runtime-субагент предложил 7 потенциальных runtime/VPN test gaps; часть была признана устаревшей после проверки, из подтвержденного взят edge case `EngineSettingsRestartObserver` вокруг `Probing(null)`.
- Чужой singbox batch был проверен и включен: HTTP `302` больше не считается успешным routed probe, Reality VLESS с невалидными ключами отсекается до генерации конфига, тесты обновлены.

**Decisions Made:**
- Не ждать зависших/закрытых субагентов, продолжать inline, чтобы не простаивать.
- Стадировать только валидные изменения текущего batch, `.memory` и проверенный чужой singbox batch; локальные untracked артефакты не трогать.
- UI-тесты строить на stable test tags/callbacks, а не на локализованном тексте или системном share/clipboard.
- `PowerDisc` testTag перенесен на кликабельный слой, чтобы тест проверял реальный click action.
- BYEDPI `byedpiUseUiMode` restart gap не дублировать, так как он уже покрыт текущими тестами.

**Lessons Learned:**
- `Probing(null)` не должен сбрасывать already accepted startup snapshot в `EngineSettingsRestartObserver`, иначе возможен ложный поздний restart.
- Для Logs screen устойчивее проверять фильтры, строки, clear/back callbacks; экспортное меню лучше не цеплять к системным механизмам в этом тесте.
- Перед объединением чужих изменений полезно сначала проверить root behavior и тестовые данные: singbox Reality tests требовали валидный base64url public key.

**Action Items:**
- Оставшиеся runtime gaps из списка субагента: `StartSequenceCoordinator`, `EngineWatchdogCoordinator`, `ShutdownCoordinator`.
- Нужен более полный e2e/instrumented прогон на устройстве/CI с проверкой N>0 discovery.
- После завершенного этапа push выполнен в `origin/dev` на commit `88b4e775 TEST: Расширение покрытия UI и singbox сценариев`; локальные untracked артефакты оставлены незапушенными.

### Session (11:24)

**Context:** Пользователь добивает покрытие бизнес-логики Ozero до 100% перед `ktlintCheck detekt --continue` и push в `dev`.

**Key Exchanges:**
- Пользователь попросил принять и запушить незапушенные фиксы другого агента вместе с текущей работой.
- Был проверен и включен чужой singbox batch: HTTP `302` больше не считается успешным probe; невалидные Reality VLESS public key/short id отсекаются до генерации конфига; тесты обновлены.
- Был создан и запушен commit `88b4e775 TEST: Расширение покрытия UI и singbox сценариев` в `origin/dev`.
- Пользователь затем попросил добить покрытие бизнес-логики до 100%, прогнать линтер + detekt и push.

**Decisions Made:**
- Не ждать зависших субагентов; продолжать inline и лично перепроверять найденные gaps.
- Включить чужой singbox batch в общий commit, потому что diff был валиден и покрыт тестами.
- Не трогать локальные untracked артефакты: `.codex/config.toml`, `.tmp/`, `_ozero_tmp/`, `singbox-core/libs/libbox.jar`.
- Для следующего этапа брать только реальные недублирующие runtime/business gaps; часть предложенных gaps уже оказалась покрыта существующими тестами.

**Lessons Learned:**
- BYEDPI mode restart, manual TUN establish null и `health degraded while stopping` уже покрыты существующими тестами, их не нужно дублировать.
- Реальный оставшийся полезный gap найден в `ShutdownCoordinator`: сценарий с актуальным `latestStartIdProvider` при повторных shutdown/startId изменениях.
- `Probing(null)` в `EngineSettingsRestartObserver` должен сохранять уже принятый startup snapshot и не провоцировать лишний restart.

**Action Items:**
- Добавить недостающий бизнес-logic regression test для `ShutdownCoordinator` вокруг актуального `latestStartIdProvider`.
- Проверить оставшиеся runtime gaps по `StartSequence`, `Watchdog`, `Shutdown` на реальные недубликаты.
- Прогнать `.\gradlew.bat ktlintCheck detekt --continue`.
- Commit без AI-подписей и push в `dev`.

### Session (12:33)

**Context:** Расследование MasterDNS после успешного deploy: серверная часть дошла до завершения, но Android-клиент не подключался.

**Key Exchanges:**
- По свежему `ozero_trace (24).log` выяснено, что предыдущий `config_init` уже исправлен: deploy завершался (`RUN_OK`, `FW_UFW_OK`, ключ прочитан).
- Новый корень сбоя оказался на Android-клиенте MasterDNS: subprocess не стартовал из-за `Cannot run program ... .so: error=2, No such file or directory`.
- Обнаружено, что `libmdnsvpn.so` отсутствовал в рабочем дереве и не был описан в `build-tools/binaries.lock.yaml`; существующий sentinel проверял только `jniLibs.useLegacyPackaging=true`, но не факт наличия native binary в APK pipeline.
- Через GitHub Actions был запущен существующий workflow сборки MasterDNS binary для upstream ref `27c7e11`, соответствующего серверному Docker pin `v2026.05.10.180256-27c7e11`.

**Decisions Made:**
- Фиксировать проблему на уровне binary manifest/download pipeline, а не клиентского runtime: причина была в отсутствующем packaged artifact, а не в пути или сетевой доступности.
- Подключить `ozero.binaries` в `engine-masterdns`, чтобы `downloadBinaries/preBuild` реально подтягивал `libmdnsvpn.so`.
- Добавить lock-запись для `libmdnsvpn-arm64-v8a.so` с target filename `jniLibs/arm64-v8a/libmdnsvpn.so`.
- Добавить regression/sentinel tests: модуль должен подключать `ozero.binaries`, запрашивать MasterDNS artifact, а lock должен описывать target filename `libmdnsvpn.so`.

**Lessons Learned:**
- `jniLibs.useLegacyPackaging=true` недостаточен как защита: нужен отдельный sentinel, что native subprocess-бинарь реально присутствует в binary lock/download pipeline.
- Для MasterDNS серверный Docker и клиентский `libmdnsvpn.so` должны быть собраны из одного upstream ref/tag, иначе возможна несовместимость протокола.
- Отсутствие `.so` проявляется на устройстве как runtime `No such file or directory`, даже если deploy и серверная настройка полностью успешны.

**Action Items:**
- Довести фиксацию до конца: проверить hash/наличие скачанного `engine-masterdns/src/main/jniLibs/arm64-v8a/libmdnsvpn.so`, прогнать style gate перед push, staged diff, commit и push в `dev`.

