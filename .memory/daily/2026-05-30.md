# Daily Log: 2026-05-30

## Sessions

## Memory Maintenance

### Session (00:02)

**Context:** Работа над ревью diff `origin/main..origin/dev` после коммита `8bdebb50 FIX: Унификация выхода IP движков` и расследование красного CI на `dev`.

**Key Exchanges:**
- Пользователь потребовал действовать “как хирург”: без гипотез, только по доказанным фактам.
- Был подтверждён push: `dev` равен `origin/dev` на `8bdebb50`; кодовые изменения запушены.
- Review выявил красный CI run `26661195213`: текущий blocker — `ktlint + detekt`, downstream jobs skipped.
- Пользователь усомнился в предложенных fixes для `SingboxEngine.activeSocksPort` и `FptnEngine` startup auth ladder; потребовал перепроверить глубже.

**Decisions Made:**
- Для review использовать remote-истину `origin/main..origin/dev`, потому что локальный `main` отставал от `origin/main`.
- Не чинить FPTN “по догадке”: сначала сверить текущий код, `.memory` и reference implementations.
- По reference FPTN: smart/auto выбирает один сервер через параллельную проверку доступности, затем логинится только в него; serial login по всем серверам в одном startup не является обязательным контрактом.

**Lessons Learned:**
- У старой архитектуры был доказанный опасный дефект: при ошибке `ViaSocks` мог уйти в direct fetch и показать IP устройства.
- Новый `ExitNodeStrategy` нужен как защита контракта: proxy-based движки показывают IP только через локальный SOCKS, URnetwork остаётся location-only, WARP пока показывает provider label.
- Возможные риски после миграции: stale SOCKS port в `SingboxEngine`, отсутствующий `IpInfo` import в `MainViewModel`, mojibake в Kotlin-строках, ktlint failures.

**Action Items:**
- Довести глубокую проверку FPTN против исторической базы `v0.2.0` перед выбором точного fix.
- Исправить красный CI: ktlint/detekt, возможный compile blocker `IpInfo`, mojibake.
- Перепроверить failure paths `SingboxEngine` и только после evidence вносить точечный fix.
- После исправлений закоммитить в `dev`, запушить и мониторить CI до финального статуса.

### Session (00:05)

**Context:** Миграция Ozero на единый `ExitNodeStrategy` и последующий review diff `origin/main..origin/dev` с красным CI на `dev`.

**Key Exchanges:**
- Рабочий коммит `8bdebb50 FIX: Унификация выхода IP движков` был создан и запушен в `origin/dev`.
- Проверено: `dev` равен `origin/dev`; локальный `main` отстаёт от `origin/main` на 3 коммита.
- После hook появились новые dirty `.memory/` изменения, но отдельным memory-коммитом их не пушили.
- CI `dev` красный на run `26661195213`; первичная причина — `ktlint + detekt`, downstream jobs skipped из-за `needs: kotlin-style`.
- В review найдены риски: missing import `IpInfo` в `MainViewModel`, stale `activeSocksPort` в `SingboxEngine`, serial FPTN auth fallback `15s * N`, mojibake в изменённых строках.

**Decisions Made:**
- Для review использовать `origin/main..origin/dev`, а не локальный `main`, потому что локальный `main` устарел.
- `SingboxEngine`: `activeSocksPort` должен означать только реально активный SOCKS probe, а не просто выделенный порт; публиковать порт только после успешного старта runtime/proxy.
- `FptnEngine`: убрать serial auth ladder из критического startup path; startup должен выбирать один сервер и делать один bounded auth attempt.
- Для FPTN безопаснее восстановить семантику `v0.2.0`: explicit selected server, иначе первый сервер из токена. Upstream-like fastest selection считать отдельной фичей, не текущим фиксoм.
- Full fallback для FPTN не должен выполняться внутри обычного startup под mutex оркестратора; если нужен, то отдельный cancellable recovery/diagnostic path.

**Lessons Learned:**
- FPTN upstream не логинится во все серверы подряд: сначала выбирает один сервер через availability logic, затем делает login только в него.
- Serial fallback по всем FPTN servers не подтверждён ни upstream, ни стабильным `v0.2.0`; он может блокировать `ChainOrchestrator` на `15s * N`.
- Mojibake может маскировать ktlint/compile проблемы после патчей через PowerShell или неверную кодировку; такие строки надо отдельно искать и чистить.

**Action Items:**
- Починить ktlint/detekt failures из CI run `26661195213`.
- Вернуть `ru.ozero.commonnet.IpInfo` import в `MainViewModel`.
- Исправить `SingboxEngine` invariant для `activeSocksPort`.
- Убрать FPTN serial auth ladder из startup path.
- Почистить mojibake в затронутых Kotlin source/test файлах.
- Коммитить `.memory/` вместе с ближайшим рабочим коммитом, не отдельным push.
