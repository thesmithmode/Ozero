# Daily Log: 2026-05-18

## Sessions

## Memory Maintenance

### Session (10:41)

**Context:** Починка упавшего релиза v0.1.1 — sentinel в release.yml требовал libmtg.so для 3 ABI, APK содержит только arm64-v8a.

**Key Exchanges:**
- Release CI упал на sentinel-проверке `libmtg.so` для `armeabi-v7a` — ABI отсутствует в APK после коммита 5d35fc4b (abiFilters = arm64-v8a only)
- Все остальные sentinels (libhev, libbyedpi, libam) уже были arm64-v8a-only — только libmtg выбивался

**Decisions Made:**
- libmtg sentinel приведён к arm64-v8a-only (аналогично остальным native libs)
- CLAUDE.md инвариант про abiFilters обновлён — было "arm64-v8a, armeabi-v7a, x86_64" (устаревшее), стало "только arm64-v8a"

**Lessons Learned:**
- При добавлении нового native binary (libmtg) sentinel был скопирован с multi-ABI шаблона, не адаптирован под текущий abiFilters. Новые sentinels проверять на соответствие abiFilters.
- CLAUDE.md инварианты могут рассинхронизироваться с кодом — при изменении build config проверять документацию.

**Action Items:**
- Коммиты: `7094d642` (memory CHORE), `b42a27fd` (release.yml + CLAUDE.md fix)
- Тег v0.1.1 переставлен на fix-коммит, релиз опубликован с APK + sha256 + GPG

### Memory Flush (11:08)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (продолжение)

**Context:** Визуальный баг — иконка и текст таба "Раздельное" в BottomDock съезжали (иконка вверх, текст вниз).

**Root Cause:** `Column` с `weight(1f)` в `Row` — weight контролирует горизонтальное пространство, Column оборачивала контент вертикально, но `Arrangement.Center` некорректно работал в сочетании с `verticalAlignment = CenterVertically` в Row для таба с длинным label.

**Fix:**
- Обернул `Column` в `Box(contentAlignment = Alignment.Center)` — Box берёт полную высоту от Row (через weight), Column wrap-content и центрируется внутри
- Добавлен `TextOverflow.Ellipsis` для защиты от overflow во всех локалях
- `tab_split_tunnel` в strings.xml сокращён с "Раздельное туннелирование" до "Раздельное"
- Удалены неиспользуемые импорты Spacer/height (detekt fail на первом CI)

**CI Notes:**
- Первый пуш (825477bd) упал на detekt — unused imports. Исправлено в 9d6a30c2.
- Второй CI (9d6a30c2) упал на flaky тест `ByeDpiEngineTest.startStopsOldProxyBeforeLaunchingNew` — CountDownLatch + runTest virtual time vs real IO. Rerun → success.

**Commits:** `825477bd`, `9d6a30c2`

### Memory Flush (11:25)

FLUSH_OK - Nothing worth saving from this session

### Session (11:38)

**Context:** Баг-репорт на 4 проблемы в Ozero: strategy favorites vs apply, UI подбора стратегий, ByeDPI crash, 0% probe score.

**Key Exchanges:**
- Юзер дал 4 бага со скриншотами: (1) "Применить" стратегию добавляет в избранные — не должно, (2) список сайтов мешает, нет прогресс-бара подбора, (3) "Поиск маршрута" для ByeDPI — бессмысленно + crash, (4) подбор стратегий возвращает 0%
- Юзер прервал параллельную работу: "Будь дотошен, фикси корень, задачи поочерёдно"

**Decisions Made:**
- Root cause #1 найден: `onApply` пишет в `savedStrategyStore` (favorites) — нужно разделить favorites и apply/history
- Root cause #3/#4: sticky `g_proxy_running` guard в native-lib.c — `jniForceClose` не доводит `main()` до return → следующий start fails forever (тот же баг byedpi native guard ownership из memory)
- Статус "Поиск маршрута" показывается для всех engines кроме WARP через Probing state — для ByeDPI бессмысленно

**Lessons Learned:**
- Юзер требует последовательное выполнение задач, не параллельное (reinforces `feedback_no_parallel_tasks`)
- Evolution auto-save best chromosomes в saved store засоряет favorites — нужно разделять persistence

**Action Items:**
- [ ] #1: Разделить savedStrategyStore (favorites) и apply action — apply не должен записывать в favorites
- [ ] #2: Скрыть site list из UI подбора, добавить progress bar
- [ ] #3: Убрать "Поиск маршрута" для ByeDPI (instant connect) или сделить engine-aware status text
- [ ] #4: Починить sticky `g_proxy_running` в native-lib.c (jniForceClose must release guard)
- [ ] #5: Разобраться почему probe score 0% — возможно следствие #4 (ByeDPI crash)

### Session (11:54)

**Context:** Fixing 4 user-reported bugs in Ozero VPN app: strategy favorites pollution, hidden site list UI, nonsensical "Поиск маршрута" label for ByeDPI, and ByeDPI crash/0% strategy testing.

**Key Exchanges:**
- Task 1 (Apply → favorites): Root cause = 3 auto-add points in `savedStrategyStore` during `onApply`/`runEvolution`. Fixed by removing `savedStrategyStore.add` calls. Sentinel test added. Committed 8913fa88.
- Task 3 (Probing label): Root = universal UI mapping `TunnelState.Probing` → "Поиск маршрута" nonsensical for ByeDPI (~50ms preflight). Three variants discussed (A=UI branch, B=skip state, C=new string). User approved Variant A (UI-only). Committed 26c31a9f.
- Task 4 (ByeDPI crash + 0%): Deep log analysis revealed `ByeDpiEngine` is `@Singleton` shared between production VPN and StrategyTestViewModel. Native `main()` returns `-1` after 28s, proxyScope single-thread queues all starts. Four fix levels proposed (F1=diagnostic logging, F2=emergency reset on fail, F3=separate instance, F4=subprocess). **Not implemented — awaiting user confirmation.**
- Task 2 (hide site list + progress bar): **Not started.**

**Decisions Made:**
- Variant A for Probing label — UI-only branch, zero state machine changes, minimal regression risk
- Strategy testing singleton problem identified but fix approach not confirmed

**Lessons Learned:**
- `ByeDpiEngine` singleton shared between production VPN and strategy testing — proxyJobRef/server_fd state leaks between use cases
- Native byedpi `main()` not reentrant: static globals (server_fd, params, signal handlers) retain stale state between runs
- `proxyScope = Dispatchers.IO.limitedParallelism(1)` means hung native call blocks ALL subsequent proxy operations
- Emergency reset only triggers on GUARD_BUSY but not on generic fail codes — recovery gap

**Action Items:**
- Task 4: Implement F1 (diagnostic logging in native-lib.c) → F2 (emergency reset on non-zero non-GUARD_BUSY exit)
- Task 2: Hide site list config UI, add strategy testing progress bar
- Consider long-term: separate ByeDpiEngine instance or subprocess for strategy testing isolation

### Session (12:06)

**Context:** ByeDpi engine reliability fix — stale server_fd causing cascade failures + Strategy UI cleanup

**Key Exchanges:**
- Юзер отверг поверхностный advisor-анализ, потребовал глубже вникнуть самостоятельно
- Найден root cause: `jniStopProxy` делает только `shutdown()` без `close()`/reset; failure path в `ByeDpiEngine.start()` пропускал `forceClose()` когда `proxyJob.isActive==false` → stale `server_fd` → cascade -1 returns
- Доказательство из прод-лога: 10 fails подряд, единственный `jniForceClose` (строка 15254) привёл к successful connect через 4 мин
- 0% fitness в EvolutionEngine = тот же root cause (600 start/stop циклов накапливают stale fd)

**Decisions Made:**
- Fix Kotlin-only без форка submodule: убрать conditional `if (proxyJob.isActive)` — всегда `join + forceClose` в start() failure path и stop()
- `forceClose` noop при `server_fd < 0` — safe для дубль-вызовов
- Инвертирован существующий тест `startFailureNoStopProxyWhenProxyReturnedErrorImmediately` (ранее доказывал баг как "expected behavior")
- Strategy UI: убран `DomainListsHeader` с главного экрана, перенесён в TopAppBar action icon, добавлен progress bar

**Lessons Learned:**
- `ByeDpiEngine.stop()` и `start()` failure path **всегда** должны вызывать `proxy.forceClose()` после `job.join()`, не conditional на `isActive`. Upstream byedpi main() хранит server_fd глобально — без close stale fd блокирует все следующие starts
- Тест что верифицирует "forceClose NOT called" может быть sentinel бага, а не корректного поведения — проверять семантику assertion
- Юзер требует глубокого самостоятельного исследования, не advisor-гадание на неполных данных

**Action Items:**
- CLAUDE.md обновлён новым инвариантом про forceClose
- Push ветки + CI verification pending (прерван юзером)
- Task 2 (Strategy UI cleanup) завершён но push не подтверждён

### Memory Flush (12:18)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (12:42)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (13:08)

**Context:** Починка красного CI на dev + исследование URnetwork feature gaps vs reference implementation

**Key Exchanges:**
- CI fix: `Icons.Filled.List` замена на core-вариант + `UnconfinedTestDispatcher` инжекция в byedpi sentinel-тесты
- Второй CI fail: рефактор переименовал `proxyDispatcher` → `effectiveDispatcher`, sentinel ловит дословное имя поля — нужно вернуть оригинальное имя
- CI watcher race condition: top-level conclusion пустой при job-level завершении — нужна проверка non-empty conclusion
- Юзер дал большой блок задач по URnetwork feature parity с оригинальным приложением

**Decisions Made:**
- URnetwork locations offline: bridge.start() уже расщеплён (DeviceLocal без tunFd). Корень — `UrnetworkRelayCoordinator` запускает bridge.start ТОЛЬКО при connect другого engine. Fix: lazy bootstrap в `UrnetworkLocationsViewModel`
- «Усиленная анонимизация» = `setProvideControlMode(AUTO/MANUAL/ALWAYS)` — уже вызывается в `EngineUrnetwork.start:109`, нужен только UI toggle
- «Фиксированный IP» = `fixedIpSize` + `applyPerformanceProfile` — уже вызывается в start:104, нужен только UI label
- Region/City — реальный gap: EngineConfig хранит только `urnetworkCountryCode`, reference имеет иерархию country→region→city
- UI для «Усиленная анонимизация» + «Фиксированный IP» должны быть на ГЛАВНОМ экспертном экране URnetwork, не в глубоком settings
- Bootstrap-coordinator для engine-urnetwork должен жить В engine-urnetwork (как TelegramProxyCoordinator), app/ только инжектит через DI

**Lessons Learned:**
- CI watcher job-level pattern имеет race: conclusion может быть пустой при status=completed на job level — нужна non-empty conclusion guard (обновлено в memory `feedback_ci_watcher`)
- Sentinel-тесты ловящие дословные имена полей ломаются при рефакторинге — переименование поля = регрессия sentinel
- URnetwork SDK: `openLocationsViewController()` доступен offline после DeviceLocal init (без IoLoop/tunFd) — reference паттерн

**Action Items:**
- Task #14: добавить region+city в EngineConfig/ConfigStore (в процессе, прервано)
- Task #15: Bootstrap coordinator для URnetwork (locations offline)
- Task #16: UrnetworkLocationsViewModel
- Tasks #6/#7: UI toggles «Усиленная анонимизация» + «Фиксированный IP» на главном экране
- Task #11: WARP onRevoke bug — записан, не начат
- Killswitch scope, раздача трафика, мёртвый код — из исходного запроса юзера, ещё не исследовано
- Hotfix 6c33c98f: 6 selectedCountry/Region/City функций → консолидированы в `UrnetworkLocationSelection` data class (detekt TooManyFunctions 23>20 + ktlint braces); reference audit добавил tasks #20-#28

### Session (13:26)

**Context:** Работа над URnetwork-фичами в Ozero: CI fix после ktlint/detekt violations, планирование traffic display (#20), изучение reference app для недостающих фич.

**Key Exchanges:**
- User обнаружил что URnetwork features не полностью перенесены: stability/privacy badges, region/city selection, traffic balance display
- User потребовал внимательнее переносить фичи из reference app, не скипать
- Reference agent нашёл: "очки" = isStable/isStrongPrivacy badges, "облачка" = unstable icon, region/city selector, Best matches/Devices categories
- User одобрил pull-to-refresh как bonus-фичу
- User сказал не торопиться, делать в логичном порядке

**Decisions Made:**
- Detekt TooManyFunctions fix через декомпозицию (6 country/region/city пар → data class + 1 Flow + 1 setter), не через threshold bump
- DataStoreUrnetworkConfigStore: вынос `writeOrRemove` как top-level extension для ≤20 functions
- Traffic balance (#20): архитектура через `UrnetworkBalanceRepository` (отдельный repository, не в SharedTrafficViewModel) — reuse для main expert/status/shared traffic экранов
- Memory: один блок в конце сессии, не per-fix

**Lessons Learned:**
- User feedback: не скипать фичи при портировании — всегда полный audit reference app
- Процесс упрощён: memory 1 блок/сессия, TaskCreate короткие, отчёты 1-2 строки

**Action Items:**
- Tasks #20-#25 созданы (traffic display, badges, region/city, bonus фичи)
- #20 traffic display — started planning, interrupted mid-architecture (UrnetworkBalanceRepository)
- CI fix pushed (commit 2aa336c8 + ktlint/detekt fix) — ожидает результат CI v3
- Остальные tasks (#21 badges, #5/#16 region/city) в очереди

### Session (13:41)

**Context:** URnetwork balance feature (#20) — Repository/Card/ViewModel/DI/Screen/тесты; закрытие #18 (enhanced anonymization)

**Key Exchanges:**
- Code review нашёл onCleared без теста → решение перейти на `stateIn + WhileSubscribed` (auto-cleanup) вместо хрупкого reflection через `ViewModel.clear()`

**Decisions Made:**
- `OzeroPalette.Accent` не существует → замена на `Aqua`
- onCleared тест удалён — reflection через ViewModel.clear() хрупкий, правильный fix = stateIn refactor после CI green
- #18 закрыт — provideControlMode уже = Enhanced anonymization, фича существует

**Lessons Learned:**
- Bash съедает PowerShell heredoc для русских commit messages — нужен PowerShell напрямую
- ViewModel.clear() reflection в тестах хрупкий подход; stateIn(WhileSubscribed) даёт auto-cleanup без manual onCleared

**Action Items:**
- Refactor VM на `stateIn + WhileSubscribed` после CI green (устранит need для onCleared + тест станет надёжным)

### Memory Flush (14:02)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (14:02)

FLUSH_OK - Nothing worth saving from this session

### Session (14:21)

**Context:** Ozero Android — параллельная работа: killswitch audit (P31 binder death) + CI watcher + fix pipeline

**Key Exchanges:**
- Юзер ругался что TodoWrite не отображается — оказалось нужен встроенный TodoWrite, не TaskList
- Юзер ругался что слишком много разведки без фиксов — "нашёл критические баги, ну фикси"
- Юзер требовал параллельные субагенты вместо одного

**Decisions Made:**
- P31 fix: `RemoteAwgRuntime` получил `onProcessDied` callback через `linkToDeath` на binder; `WarpModule` вешает callback на `tunnelController.onEngineDied(EngineId.WARP, "remote-binder-died")`
- Killswitch audit выявил 6 critical gaps + 5 missing sentinels (записано в AUDIT.md)

**Lessons Learned:**
- Audit → fix pipeline: не разведывать бесконечно, после нахождения проблем сразу переходить к фиксам
- TodoWrite vs TaskList: юзер видит только TodoWrite, TaskList/TaskGet — невидимая система
- Параллельные субагенты для read-heavy задач ускоряют процесс

**Action Items:**
- #31 P32 (URnetwork IoLoop) — начат но прерван
- CI run 9a4c30cb — P31 push, ожидает результат
- Remaining audit findings из AUDIT.md — unfixed P-items

### Session (14:21)

**Context:** Ozero — аудит killswitch + фикс критических багов (P31 binder death, P32 URnetwork IoLoop), параллельно CI.

**Key Exchanges:**
- Юзер дважды прерывал из-за "топчешься на месте" — слишком много разведки, мало фиксов
- Юзер потребовал починить TodoWrite (встроенный трекер не отображался, ID формат путался с TaskGet)
- Юзер: "делай не одним субагентом а несколькими" — декомпозиция на 4 параллельных Explore
- Юзер: "никаких допущений, только по факту, как хирург" — требование zero-regression discipline

**Decisions Made:**
- P31 (binder death detection): RemoteAwgRuntime получил `onProcessDied` callback через linkToDeath, WarpModule вызывает `tunnelController.onEngineDied()` — запушен 9a4c30cb
- Killswitch audit выявил 6 critical gaps + 5 missing sentinels (записано в AUDIT.md)
- CI watcher на c4cda807 остановлен, новый на 9a4c30cb

**Lessons Learned:**
- Разведка без фиксов = "топтание на месте" для юзера. Audit → immediate fix, не audit → plan → fix
- TodoWrite vs TaskGet — разные системы, путаница с ID форматом ломает отображение
- Юзер крайне нетерпим к exploration без delivery — каждый cycle должен заканчиваться коммитом

**Action Items:**
- #31 P32 (URnetwork IoLoop) — started, in progress
- CI 9a4c30cb — ждёт результата
- Оставшиеся P-findings из AUDIT.md — очередь фиксов

### Session (14:45)

**Context:** Killswitch audit (P30-P37) для Ozero VPN — серия фиксов по reliability движков при crash/revoke сценариях.

**Key Exchanges:**
- Юзер напомнил: "как хирург, никаких допущений, только по факту" — zero-tolerance к регрессиям
- Прерывание на полпути P32 edit → продолжение без потери контекста

**Decisions Made:**
- P30 Telegram auto-restart: retry policy в Coordinator (owns policy), не в TelegramProxyService (SRP). Coordinator combine(tunnel+config+proxy.state) — полная картина для принятия решений
- P32 URnetwork IoLoopDoneCallback: compareAndSet(true,false) чтобы отличить graceful stop от SDK crash; onIoLoopDied только при wasRunning=true (иначе ложный killswitch)
- P33 sentinel: killswitch=false → stopVpnRequest без lockdown

**Lessons Learned:**
- Telegram MTProxy — side-car subprocess, не VPN engine; fix killswitch wiring неприменим, нужен auto-restart subprocess паттерн
- TelegramProxyService single-responsibility (запуск subprocess), Coordinator owns restart policy — альтернатива (retry внутри Service) нарушала бы SRP

**Action Items:**
- #11 WARP onRevoke 74ms — in_progress, ещё не начат реально (дважды прерван)
- CI watcher на последний push killswitch audit — проверить результат
- Оставшиеся pending туду (после cleanup)

### Session (15:02)

**Context:** Работа над Ozero — URnetwork engine: миграция location API и UI.

**Key Exchanges:**
- Юзер жёстко перенаправил с dead code audit (#10) на URnetwork задачи (#5) — приоритет фич над cleanup
- CI fix: unused import в коммите 27e6cd9e (тривиальный)
- #11 WARP onRevoke 74ms — отложена, нет воспроизводимых логов для диагностики

**Decisions Made:**
- #5 region+city selector = foundation для #16/#17, делать первым
- Bridge API: `setPreferredCountry(String?)` → `setPreferredLocation(ConnectLocation?)` — universal country/region/city через одну сущность
- `findBestMatch` helper в RealBridge для fuzzy location matching (region→city fallback)
- Все call sites мигрированы атомарно (Bridge, EngineUrnetwork.start, UrnetworkEngineSettingsViewModel, persistence test)

**Lessons Learned:**
- Dead code audit = конец спринта, не середина. Фичи первыми
- URnetwork SDK ConnectLocation = universal (country/region/city одна сущность), не иерархия отдельных API
- .memory файлы попали в feat-коммит — нарушение правила отдельного CHORE-коммита, пришлось разделять

**Action Items:**
- #16 UrnetworkLocationsScreen + ViewModel — прерван на старте, продолжить
- #11 WARP onRevoke 74ms — ждёт боевой лог с timestamps
- CI watcher был armed на момент прерывания — проверить результат

### Memory Flush (15:34)

FLUSH_OK - Nothing worth saving from this session

### Session (15:52)

**Context:** Работа над Ozero Android — reconnect indicator (#24), provide pause (#25), CI fixes

**Key Exchanges:**
- Юзер: "декомпозицией только усложняешь, рефлексируй" → избыточный TaskCreate на задачу уже в списке = потеря времени
- Юзер: "объясни что сделал и зачем, верно ли это" → detekt fix через extract helpers, не threshold bump

**Decisions Made:**
- #24 reconnect indicator: добавлен isReconnecting в StatusLabel/StatusRow, strings, MainViewModel, Simple/ExpertMainState. Закоммичен f580b26b
- Detekt CyclomaticComplexity fix: StatusLabel декомпозирован на pickStatusLabelRes/probingLabelRes/pickStatusEngine — поведение 1:1, сложность снижена
- Подход: decompose over threshold bump (memory rule подтверждён)

**Lessons Learned:**
- Декомпозиция задачи в TaskCreate когда задача уже в трекере = overhead без value. Проверять state и делать сразу
- Закрытие задачи после grep без верификации coverage = "silent skip" (нарушение feedback_no_silent_skip)
- Рефлексия через sequential thinking tool полезнее чем текстовый самоанализ

**Action Items:**
- CI fail f580b26b: detekt fix staged но не закоммичен — нужен push
- #25 provide pause: проверить наличие sentinel-тестов на setProvidePaused/observe pause-state, дописать если нет

### Session (16:09)

**Context:** URnetwork engine features — Fixed IP + Enhanced Anonymization toggles on main screen, plus CI debugging

**Key Exchanges:**
- User corrected task prioritization: don't pick big audit (#10), pick atomic tasks (#21/#26/#28) first
- User demanded sequential-only tasks, no parallel — memory rule updated
- User demanded branch cleanup: no side branches, only dev+main
- User demanded full context dump before continuing — got comprehensive WIP state

**Decisions Made:**
- Side branches permanently banned — all work directly in dev (overrides old feature-branch workflow)
- #6 + #7 (Fixed IP + Enhanced Anonymization) scoped to **main screen toggles**, not settings
- Root fix in `applyPerformanceProfile`: AUTO+allowDirect=false was silently skipped before — now applies profile
- `allowDirect` default=true (mirrors bringyour reference), "Strong Anonymization" = `!allowDirect`
- Sequential thinking only on explicit triggers ("рефлексируй"), not every step

**Lessons Learned:**
- `feedback_no_parallel_tasks` — tasks strictly sequential, never parallel
- `feedback_sequentialthinking_judicious` — sequential thinking expensive, only on trigger words
- Side branches add overhead for single-dev workflow; direct-to-dev simpler
- `applyPerformanceProfile` had hidden bug: skipped ALL AUTO profiles regardless of allowDirect state

**Action Items:**
- Fix CI failure on 9ab302b0 (#49 sentinel test) before pushing new WIP
- Complete #6+#7 WIP: update second FakeProfileBridge in EngineUrnetworkContractTest:534, add MainViewModel exposure, MainScreen toggles, strings, tests
- Update `feedback_feature_branches.md` to reflect no-side-branches rule
- Orphan worktree at `C:/Users/thesm/.codex/worktrees/8ead/Ozero` — cleanup when convenient

### Session (18:55)

**Context:** Завершение URnetwork toggles (Fixed IP + Enhanced Anonymization) — fix CI + закрыть WIP одним coherent коммитом

**Key Exchanges:**
- Юзер прервал избыточный контекст — попросил автономную работу с туду
- Юзер уточнил состав фич: тумблеры на главном экране + dev+main only

**Root Cause CI #49 (9ab302b0):**
- `probingLabelRes(engineId: EngineId, ...)` — после декомпозиции StatusLabel (a9bfad06) приняла non-null EngineId
- `TunnelState.Probing.engineId: EngineId? = null` — nullable
- Compile error `Argument type mismatch` в `MainScreen.kt:786` пробил оба джоба (`assembleDebug` + `Tests — app`)
- Fix: `probingLabelRes(engineId: EngineId?, ...)` + null → fallback `main_status_probing`
- Sentinel `StatusLabelProbingNullSentinelTest` закрепляет: pickStatusLabelRes + probingLabelRes корректно обрабатывают Probing(engineId=null)

**Decisions Made:**
- Тумблеры рендерятся в обоих режимах (Simple + Expert) под Status, gating через `isUrnetworkVisibleInMain(state, manualEngine)` — true когда manualEngine=URNETWORK либо state.engineId=URNETWORK (Probing/Connecting/Connected/Failed)
- `enhancedAnonymization` = `!allowDirect` — mirror bringyour ConnectActions.kt:251
- DataStore key `urnetwork_allow_direct` (default true)
- Тесты MainViewModelTest: 11 новых behavioral sentinels (store mirror, bridge gate by isUrnetworkActiveNow, BYEDPI active не триггерит URnetwork bridge)
- UI sentinel MainScreenUrnetworkTogglesSentinelTest: 11 проверок (gating fn + source-text inspection через regex)

**Commits:**
- `b3fe4f92` FIX: probingLabelRes принимает EngineId? (#49 root)
- `229cd275` FEAT: URnetwork — Фиксированный IP + Усиленная анонимизация на главном экране
- `cb2f7408` CHORE: обновить memory-логи

**Lessons Learned:**
- Декомпозиция функции с smart-cast'нутой переменной (state.engineId внутри `is TunnelState.Probing`) теряет smart-cast при extract — нужен explicit nullable type на параметре
- Sentinel pure-fn test (`internal fun` + unit test) проще и быстрее чем UI test через Compose

**Action Items:**
- CI на cb2f7408 watch — должен зеленеть
- Memory обновлено: `feedback_feature_branches.md` переписан под dev-only
- Следующая задача из туду: #25 (provide pause state) разблокирован после b3fe4f92 fix; #21/#26/#28 — атомарные URnetwork улучшения

### Session (16:30)

**Context:** URnetwork engine — добавление тумблеров "Фиксированный IP" и "Усиленная анонимизация" на главный экран + починка CI.

**Key Exchanges:**
- Юзер передал детальный контекст WIP с 7 подзадачами + список todo + CI failure на 9ab302b0
- Приоритет: сначала CI fix, потом WIP тумблеров

**Decisions Made:**
- CI fix первым — `MainScreen.kt:786` regression от a9bfad06 (декомпозиция StatusLabel): `Probing.engineId` nullable (`EngineId?`), а `probingLabelRes` ожидал non-null. Fix: принять `EngineId?` + fallback на null. Обе функции `internal`.
- Sentinel unit-test добавлен для probing label null-safety.
- FakeProfileBridge:534 в EngineUrnetworkContractTest — обновлён override на 3 params (allowDirect добавлен).
- MainViewModel расширен: expose `fixedIpSize`/`enhancedAnonymization` StateFlow + setter'ы.

**Lessons Learned:**
- `TunnelState.Probing.engineId` = nullable по дизайну (null = неизвестный engine при пробинге). Декомпозиция StatusLabel не учла это — чистая type-mismatch регрессия.
- При декомпозиции extract-function всегда проверять nullability параметров в sealed class variants.

**Action Items:**
- CI watcher на push с fix — дождаться green
- #3 sentinel AUTO+allowDirect=false — не завершён
- #4 MainScreen UI — 2 toggle row (visibility при engine=URnetwork)
- #5 Strings (main_toggle_fixed_ip, main_toggle_enhanced_anonymization) + EN
- #6 MainViewModelTest — тесты toggle
- #7 UI sentinel — toggle в MainScreen
- Orphan worktree `C:/Users/thesm/.codex/worktrees/8ead/Ozero` — не критично, удалить при случае
- Memory feedback: обновить `feedback_feature_branches` — side-ветки запрещены, только dev+main

### Session (16:59)

**Context:** Code review session — 5 parallel reviewer agents audited recent commits across URnetwork SDK, ByeDPI, killswitch sentinels, UI polish, and URnetwork UI/Balance. CI red on ktlint/detekt after REFACTOR push.

**Key Exchanges:**
- 5 reviewer agents returned ~25 unique findings across 5 commit clusters
- Each finding classified: confirmed/rejected/needs-decision with rationale
- CI failed twice — first on original push, second on REFACTOR fix (new ktlint PascalCase + line length violations)

**Decisions Made:**
- REJECTED `accountPoints while(true)` as false positive — same pattern as accepted `peerCount`/`sharedTrafficBytes` with `stateIn(WhileSubscribed)`
- REJECTED TOCTOU in `setUrnetworkFixedIp` HIGH→LOW — different store fields, race only at parallel user-toggle
- Product decision NEEDED: `TunnelState.Failed` + `isReconnecting` — show "Сбой" or "Переподключение"?
- Factory-function naming: rename to camelCase (`fakeStore`/`spyStore`) or inline `InMemoryUrnetworkConfigStore(...)` constructor

**Lessons Learned:**
- Replacing `class FakeXxx` with `fun FakeXxx()` factory triggers ktlint camelCase rule — use `newFakeXxx()` or `fakeXxx()` naming
- `val MinimalConfigStore` PascalCase triggers detekt VariableNaming — lowercase or extract to companion
- `RemoteAwgRuntime.onServiceDisconnected` ≠ `onBindingDied`: system OOM unbind goes through `onServiceDisconnected`, NOT `onBindingDied` — killswitch gap confirmed
- ByeDPI pre-flight `forceClose` inside `if (oldJob.isActive)` misses case where `main()` returned -1 during `withTimeoutOrNull` — same stale `server_fd` bug that was fixed in stop/failure paths but missed in start pre-flight
- Sentinel tests with `verify(atLeast=1, atMost=3)` give false confidence — always use `exactly=N` when testing retry bounds

**Action Items:**
- CI FIX (blocker): multiline breaks for 4 long lines + rename 5 PascalCase factory-funs to camelCase
- P1 FIX: `RemoteAwgRuntime.onServiceDisconnected` → add `onProcessDied()` call
- P1 FIX: `ByeDpiEngine.start()` pre-flight → unconditional `forceClose` after join (not gated on `isActive`)
- P1 FIX: `TelegramProxyCoordinatorTest` — replace `atLeast/atMost` with `exactly=N`, add `verify(exactly=0)` for Error+Idle
- HIGH: `probingLabelRes` rewrite — `isReconnecting` as first branch covering all engines
- HIGH: add unit tests for `pickStatusLabelRes`/`probingLabelRes` pure functions
- HIGH: `insufficientBalance` `Eagerly` → `WhileSubscribed`
- HIGH: `findBestMatch` — filter by `countryCode` when matching city
- HIGH: `BalanceRepository.refresh()` — add `withTimeout`
- HIGH: `BottomDock` — remove double TalkBack, add `Role.Tab`+`selected`
- MEDIUM: `CLAUDE.md` trim to ≤100 lines (move reference impls block to `Architect.md`)

### Session (19:55) — autonomous fix-cycle execution

**Context:** Юзер дал полную автономию: "сам принимай решения, принципы знаешь". 22 таска, выполнял последовательно. CLAUDE.md решено НЕ трогать (юзер: "опасно без присмотра").

**Commits pushed на dev (последовательно):**
- `78d8a002` FIX: ktlint/detekt — camelCase factory-функции + split длинных строк
- `8981a799` FIX: P1 — killswitch на OOM, byedpi stale fd, telegram false-green sentinels
- `ff7f5044` FIX: HIGH — MainScreen reconnecting label, BalanceRepo timeout, location guard, a11y
- `1764d4b3` FIX: MEDIUM — runCatching в poll-flows + distinctUntilChanged

**Lessons Learned:**
- `RemoteAwgRuntime.onServiceDisconnected` симметричен `onBindingDied`: unbind + null refs + onProcessDied(). System unbind ≠ binder death.
- `ByeDpiEngine.start()` pre-flight forceClose теперь безусловный после join — нельзя gating на isActive (главный класс бага со stale server_fd).
- `findBestMatch` без countryCode → wrong-country connect. City-by-name globally ambiguous (одинаковые имена). Early return null.
- `UrnetworkBalanceRepository.refresh` без withTimeout → mutex навечно при сетевом hang. Default 8s.
- Poll-flows `peerCount`/`sharedTrafficBytes`/`accountPoints` теперь с runCatching+last-value fallback. Без него throw отменяет flow → re-subscribe лагает.
- `BottomDock` Icon contentDescription=null (Text озвучивает), Modifier.semantics{role=Tab, selected}. Дубль TalkBack устранён.
- `pickStatusLabelRes(Failed)` теперь uses `isReconnecting` → "Переподключение" вместо "Сбой" пока reconnect не exhausted.
- `probingLabelRes` теперь exhaustive when (без else) + isReconnecting первой ветвью.
- Reviewer claim про `throw RuntimeException` в `UrnetworkSharedTrafficViewModelTest` оказался false-positive — там уже `error("test-fail")`.
- `UrnetworkContractStatusObserver.stop()` не критично — Application = process-lifetime, observer correctly resets disconnectInFlight при switch engine.

**Action Items:**
- Финальный CI watch на 1764d4b3
- .memory отдельным CHORE коммитом после CI green
### Memory Flush (17:22)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (17:38)

FLUSH_OK - Nothing worth saving from this session

### Session (17:51)

**Context:** Составление changelog v0.1.1→v0.1.2 (потом расширили до v0.0.12→v0.1.2) пока ждали CI

**Key Exchanges:**
- Вопрос: URnetwork раздача трафика работает всегда или только при активном URnetwork движке?
- Ответ: `UrnetworkRelayCoordinator` стартует независимо от движка через `OzeroApp.onCreate()`, НО требует `byClientJwt` в DataStore. JWT сохраняется только при первом подключении через URnetwork. Новый юзер без аккаунта URnetwork → relay не запускается вообще.

**Decisions Made:**
- Changelog v0.1.2 (patch) написан с пользовательской перспективой — технические детали выброшены
- Changelog v0.0.12→v0.1.2 (полный) сформирован из 293 коммитов с фильтром: только user-visible изменения

**Lessons Learned:**
- Нарушение `feedback_run_ktlint_before_commit` — в коммите UrnetworkConfigStore.kt:131 была однострочная функция без newlines, ktlint сломал CI. Обязательно гонять `./gradlew ktlintCheck` перед каждым коммитом с `.kt` файлами.
- URnetwork relay — не «всегда включена» фича, а «после первой авторизации». Важно учитывать при описании функциональности для новых пользователей.

**Action Items:**
- Дождаться зелёного CI → поставить тег `v0.1.2`
- Рассмотреть UX для relay: показать онбординг/объяснение новым пользователям что нужен аккаунт URnetwork

### Session (новая)

**Context:** CI красный на 832dc2c4 — compile errors после рефакторинга UrnetworkConfigStore.

**Root Cause:**
- `UrnetworkConfigStore` методы стали top-level extension functions (из interface members). Три файла не имели нужных import-ов: `UrnetworkRelayCoordinator`, `MainViewModel`, `UrnetworkEngineSettingsViewModel`.
- `EngineId` enum расширен новыми значениями (XRAY, AMNEZIA, HYSTERIA2, NAIVE, TOR, FPTN — все `isStub=true`), но `when (engineId)` в `MainScreen.probingLabelRes` не был exhaustive.

**Fixes (commit 2dc2f4d1):**
- Добавлены импорты extension functions в 3 файла
- `probingLabelRes` — новые stub EngineId → `main_status_connecting`, `null` отдельной веткой

**Lessons Learned:**
- CI watcher с `--limit 1` без проверки конкретного run ID срабатывает на предыдущий completed run — нужна проверка databaseId
- При переводе interface members → top-level extension functions нужно сразу grep по всем файлам-потребителям

### Session (18:25)

**Context:** Исследование механизма выплат URnetwork relay для монетизации Ozero — будут ли USDC приходить на кошелёк разработчика при текущей архитектуре.

**Key Exchanges:**
- Вопрос: реально ли выплаты идут на PRESET_WALLET при текущей схеме с guest JWT?
- Ответ: НЕТ. Server-side блокировка в `router/handler_utils.go` — все wallet endpoints (CreateAccountWallet, GetAccountWallets, RemoveWallet) возвращают 401 для `guestMode=true`. Relay работает в холостую, USDC не приходит.

**Decisions Made:**
- PRESET_WALLET хардкод `27wATh...vHMM` — намеренное поведение, не баг. Все выплаты идут разработчику, юзер НЕ видит/указывает свой кошелёк. Записано в memory.
- `UrnetworkJwtBootstrap` (guest JWT при старте) — идея корректная для запуска relay, но бесполезна для монетизации из-за server-side guest блокировки.

**Lessons Learned:**
- URnetwork guest mode (`guestMode=true`) полностью отрезан от earning: UI показывает "To start earning, create account", сервер блокирует wallet API 401. Relay трафик от guest аккаунтов не оплачивается.
- Источники: `AccountViewModel.kt:73` (LoginMode.Guest → GuestModeOverlay), `router/handler_utils.go` (WrapRequireAuth блокирует guest), `api/handlers/account_wallet_handlers.go`.

**Action Items:**
- Выбрать вариант решения для монетизации relay: (1) Master Ozero account с полноценным JWT — рекомендуемый; (2) Solana wallet auth; (3) Per-user полноценный аккаунт. Решение юзера pending.
- После выбора — реализовать bootstrap с non-guest JWT вместо текущего `acquireGuestJwt()`.

