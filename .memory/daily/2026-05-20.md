# Daily Log: 2026-05-20

## Sessions

## Memory Maintenance

### Session (00:00)

**Context:** Debug session — ByeDPI регрессия + WARP не работает + URnetwork ×2 quota

**Key Exchanges:**
- Юзер: лог ничего не дал ни по одному движку → добавлено discriminating logging (hev stats poller rx/tx, WARP dirListing, URnetwork clientId/provideEnabled)
- Юзер: ByeDPI работал раньше → bisect показал регрессию в коммите `89a6ecf3` (v0.1.6)
- Юзер: URnetwork 34 GB в оригинале vs 68 GB в Ozero → root: два клиента на одном кошельке, серверная квота суммируется

**Decisions Made:**
- ByeDPI MTU регрессия найдена: `TunBuilderHelper.kt` ставит `setMtu(8500)` которого нет ни в v0.1.0 ни в ByeByeDPI reference → фикс: убрать
- WARP UAPI path: lib кладёт сокет в `sockets/<tun-name>.sock`, не в `dataDir/ozero-warp.sock` → фикс: discovery через listFiles в `sockets/`
- URnetwork ×2 quota: серверная проблема (два клиента на кошельке), клиентски не лечится
- URnetwork missing cities: откладывается на v0.1.9 (medium risk, нужно extensive testing)
- Scope v0.1.8: только MTU revert + WARP sock path; всё остальное вынесено

**Lessons Learned:**
- `setMtu(8500)` добавлялось "для hev YAML consistency" — это ошибочная логика, hev YAML MTU и TUN MTU независимы. Android default (~1500) безопаснее
- При рефакторинге (v0.1.6 `OzeroVpnService extract → TunBuilderHelper`) появились изменения поведения которые не были в reference. Рефактор ≠ 1:1 перенос если добавляются новые параметры
- Диагностические логи нужно добавлять до начала debug-сессии, не после первого "логи ничего не дали"
- Advisor польза: поймал что ByeDPI (main complaint юзера) выпал из v0.1.8 scope

**Action Items:**
- Реализовать v0.1.8: revert setMtu(8500) + WARP sockets/ discovery + sentinel тесты
- При следующем crash URnetwork original app — запросить tombstone с устройства

### Session (KB query — ByeDPI IDLE root cause)

**Context:** Knowledge base query — причины IDLE/нет трафика в hev pipeline ByeDPI

**Key Exchanges:**
- Запрос: `ByeDPI трафик не идёт hev pipeline IDLE root cause`
- Синтез из 7+ KB статей: cascade root causes ранжированы по частоте

**Root Cause Summary:**
Первичные (самые частые):
1. `jniStartProxy=-1` от stale `server_fd` (unconditional `forceClose` отсутствует в stop/failure paths) → hev нет SOCKS upstream → IDLE
2. `setBlocking(true)` на TUN fd → замораживает epoll loop libhev → счётчик трафика stuck at 0
3. `excludeSelf=false` → трафик Ozero входит в TUN → hev роутит через ciadpi → обратно в TUN → бесконечный loop

Вторичные:
- Неправильный stop order (libhev раньше byedpi) → брошенные потоки держат mutex → deadlock при reconnect
- `g_proxy_running` guard wedged → следующий `jniStartProxy` возвращает -2 (GUARD_BUSY)
- `-Ku` в ByeDPI args → UDP-only whitelist, TCP/TLS desync не применяется

**Action Items (без изменений):**
- v0.1.8: revert setMtu + WARP sockets/ discovery — dirty файлы в репо, работа прервана компакцией

### Session (00:21)

**Context:** Знаниевый запрос — root cause ByeDPI IDLE (трафик не проходит через hev pipeline)

**Key Exchanges:**
- Вопрос: "ByeDPI трафик не идёт hev pipeline IDLE root cause"
- Ответ: синтез 7+ knowledge base статей в ранжированный список причин

**Decisions Made:**
- Root causes ранжированы по частоте: stale server_fd → setBlocking(true) → excludeSelf=false → stop order → guard wedge → -Ku flag

**Lessons Learned:**
- **Главная причина IDLE**: `jniStartProxy=-1` из-за stale `server_fd` (нет unconditional `forceClose` в stop/failure path) → у hev нет SOCKS upstream → трафик не идёт
- **Вторая частая**: `setBlocking(true)` на TUN fd → замораживает epoll loop libhev → счётчик трафика 0
- **Третья**: `excludeSelf=false` → routing loop Ozero→TUN→ciadpi→TUN→∞, реальный трафик не проходит
- **Редкие**: неправильный stop order (libhev до byedpi → mutex deadlock), `g_proxy_running` wedge, `-Ku` flag (UDP-only bypass)
- Каскадная природа: несколько причин могут работать одновременно; диагностировать по логам (`jniStartProxy завершился с кодом -1` → первая причина)

**Action Items:**
- Нет pending tasks — сессия была read-only запрос

### Memory Flush (00:23)

FLUSH_OK - Nothing worth saving from this session

### Session (post-compact) — ByeDPI double-argv[0] regression

**Context:** v0.1.7 ByeDPI всё ещё не работает после MTU-revert. Юзер: "проблема была ДО этого, ищи дальше через .claude/Контекст/" + "ищи в .memory/ мы её не зря ведём".

**Key Finding (TWO regressions):**
1. **Double argv[0]** (root cause IDLE): `native-lib.c:85` ставит `argv[0]=strdup("byedpi")`. `ByeDpiEngine.kt:252` + `ByeDpiUiArgsBuilder.kt:10` дополнительно prepend'ят `"ciadpi"`. Результат: `["byedpi","ciadpi","--ip",...]`. getopt_long читает argv[0]="byedpi" как program name, argv[1]="ciadpi" как первый positional — `--ip` сдвинут / теряется → SOCKS bind на дефолтный адрес → hev пишет в пустоту → TX 5 SYN retry → IDLE. Зафиксировано в memory `byedpi-args-parsing.md` (2026-05-13). Регрессия введена `7cc2348f FIX: ByeDPI ciadpi argv[0]` — fix без проверки native-lib.c.
2. **setMtu(8500)** (regression v0.1.6, отдельная): `TunBuilderHelper.kt:65` ставит `setMtu(HevTunnelConfig.DEFAULT_TUN_MTU)`. ByeByeDPI 1.7.4 этого НЕ делает (TUN link MTU vs lwIP internal MTU — независимы). Регрессия введена `89a6ecf3 FIX: ByeDPI TUN MTU=8500`.

**Fixes Applied:**
- `TunBuilderHelper.kt` — убрал `setMtu(8500)` из buildTunBuilder
- `ByeDpiEngine.kt:252` — убрал `"ciadpi"` из prefix
- `ByeDpiUiArgsBuilder.kt:10` — убрал `"ciadpi"` из prefix
- `ByeDpiEngineTest.kt:258-268` — sentinel переписан: **запрещает** ciadpi/byedpi в Kotlin-argv (защита от регрессии)
- `ByeDpiUiArgsBuilderTest.kt:15-23` — expected обновлён без ciadpi
- `WarpHandshakeUapi.kt` — `findUapiSocket()` ищет в `sockets/` subdir + любой `.sock` через listFiles (canonical path amneziawg-go)
- `WarpSocketDiagnostics.kt` — добавлен листинг `sockets/` subdir

**Lessons Learned:**
- **MEMORY IS NOT OPTIONAL.** `.memory/knowledge/byedpi-args-parsing.md` чётко документировал double-argv[0] trap (2026-05-13). Я ввёл регрессию `7cc2348f` ровно через 2 дня, не свернувшись в memory. Юзер: "ищи в .memory/ мы её не зря ведём".
- **Правило (новое в CLAUDE.md проекта):** перед спавном субагента / advisor / гипотезой → сначала `wiki-find` + чтение `.claude/Контекст/*_ANALYSIS.md`. Memory > субагент.
- handle=0 в WARP — НЕ баг. Это валидный первый slot amneziawg-go. Зафиксировано в memory `project_warp_handle_invariant` + комментарий `bcabdbcd`. Я доёбывался ошибочно несколько раз — больше не трогать.

**Action Items:**
- Коммит: разделить на 2 (memory отдельно, код вместе)
- CI на dev → если green → tag v0.1.8
- Pending для v0.1.9: URnetwork cities, URnetwork ×2 quota investigation, Original URnetwork crash diagnostics


### Session (00:48)

**Context:** Поиск и фикс регрессий ByeDPI/WARP/URnetwork + root-cause investigation

**Key Exchanges:**
- handle=0 в WARP — НЕ баг. `project_warp_handle_invariant` + комментарий в `bcabdbcd`. Убрано из туду навсегда. Guard всегда `< 0`, не `<= 0`.
- URnetwork ×2 quota (#10) — дизайн-ограничение: Ozero + URnetwork-app в разных sandbox, два instanceId на одном кошельке, backend суммирует. Только server-side fix. Закрыто как known constraint.
- URnetwork crash (#11) — `libgojni.so` нельзя unload на Android; possible конфликт VpnService ownership при переключении. Диагностический logging добавлен. Требует tombstone от юзера.

**Decisions Made:**
- handle=0 закрыт навсегда — не трогать, не обсуждать повторно
- CLAUDE.md правило добавлено: `wiki-find` + `.claude/Контекст/` ПЕРЕД любым субагентом/гипотезой. Обе регрессии (ciadpi prefix, setMtu) уже были в `.memory/knowledge/` и проигнорированы

**Lessons Learned:**
- ByeDPI IDLE root: `native-lib.c:85` ставит `argv[0]="byedpi"`, Kotlin дополнительно prepend'ил `"ciadpi"` → getopt терял `--ip`. Sentinel: нельзя добавлять argv[0] из Kotlin если native уже ставит
- WARP timeout root: amneziawg-go fork пишет UAPI socket в `sockets/<kernel-tun>.sock`, не в корне dataDir. `WarpHandshakeUapi` теперь cascade через `sockets/` + discovery
- setMtu(8500) — регрессия v0.1.6 (89a6ecf3). Android default ~1500, большие пакеты дропаются провайдерами. Revert'ить немедленно при обнаружении
- URnetwork cities root: SDK инициализировался с 1 параметром из 12 → SDK скрывал города/регионы. Паритет с `DeviceManager.kt:132-144` upstream

**Action Items:**
- CI fail на `a1579acc` — расследовать при следующей сессии если не починен
- #11 URnetwork crash — ждать tombstone от юзера при следующем репро для точного root cause

### Session (post-compact) — VPN slot release + ×2 quota архитектурный аудит

**Context:** Юзер: "VPN slot контракт Android" — можно обойти? + "×2 квота — ты уверен что server-side? мы должны генерить per-user аккаунт".

**Key Finding:**
- ×2 квота: per-user identity у Ozero уже корректная (Ed25519 keypair → walletAuth → отдельный URnetwork network с отдельным `byJwt`). Payout wallet = твой Solana адрес, привязывается через `walletVc.payoutWallet` отдельно (не identity). Архитектурно ×2 НЕ из-за shared account.
- Возможная причина: legacy migration (старый guest byJwt + walletAuth networkCreate подряд) → backend имеет 2 network на той же identity. Или backend накопил по networkId за всё время. Без логов точно не сказать.
- VPN slot: НЕ данность. `onRevoke()` ловит когда другой VPN перехватывает slot. Можно добавить отложенный `Process.killProcess` → процесс умирает, libgojni выгружается, slot моментально освобождается. Только в `onRevoke` (не stopVpn/onDestroy): revoke = юзер явно выбрал другой VPN, наша фоновая работа не нужна.

**Fixes Applied:**
- `OzeroVpnService.onRevoke()` — `Handler.postDelayed { Process.killProcess(myPid) }` через REVOKE_KILL_DELAY_MS=2500ms (даёт время shutdown+logs)
- `OzeroVpnServiceLifecycleTest` — sentinel: onRevoke содержит processKiller.kill + postDelayed + REVOKE_KILL_DELAY_MS ≥ 1500ms
- `RealUrnetworkSdkBridge.fetchSubscriptionBalance` — diagnostic GB-форматирование + ratio balance/start + WARN если startBalance > 50GB (DOUBLE_QUOTA_THRESHOLD_BYTES) с clientId

**Lessons Learned:**
- Не путать identity (Ed25519 pubkey) с payout wallet (Solana адрес). Ozero корректно разделяет.
- onRevoke ≠ stopVpn. Revoke значит external override → free process resources. Stop значит юзер вручную выключил VPN → процесс остаётся для UI/balance refresh.
- Каждое "это server-side, ничего нельзя" требует проверки 2-го уровня: показать архитектурную причину или предложить client-side investigation hook (diagnostic warn).

**Action Items:**
- После CI green на новый commit → tag v0.1.8
- При следующем repro ×2: запросить лог с фразой "startBalance=...GB > 50GB — подозрение" → подтвердить duplicate networkCreate vs накопленная квота
- При следующем repro #11: юзер запустит UR-app после Ozero stop → onRevoke kill отработает → URnetwork-app стартует чисто; если краш повторится — root глубже (libgojni state на диске)

### Memory Flush (01:22)

FLUSH_OK - Nothing worth saving from this session

### Session (post-compact) — v0.1.9 prep: URnetwork timeout + WARP UAPI + ByeDPI pin + UI redesign

**Context:** Юзер на v0.1.8 пожаловался: 1) URnetwork крашит (awaitReady timeout 15s), 2) WARP «работает медленно», 3) ByeDPI открывает Instagram но не YouTube с теми же стратегиями, что в ByeByeDPI оригинале. Плюс задачи: редизайн UI настроек ByeDPI без CMD, починить отсутствующие regions/cities в URnetwork picker, добавить 30-day chart в shared traffic и кэшировать balance для показа без подключения.

**Key Findings:**
- URnetwork timeout 15s маленький для P2P discovery на медленных сетях → bump 45s + progress лог каждые 15 опросов.
- WARP `WarpUapi.readState` искал сокет в legacy пути (корень), а после миграции на $dataDir/sockets/<tun>.sock читал null → stats всегда null → health monitor false DEGRADED → лишние recover триггеры. Фикс — использовать `WarpHandshakeUapi.findUapiSocket` (уже знает оба пути). Добавлен periodic boot.log stats.
- ByeDPI YouTube: pin был на `v0.17.3` (commit 7efde1b1, 2025-09-19), ByeByeDPI 1.7.5 использует `ba532298` (2026-03-26, 38 коммитов вперёд). YouTube QUIC/ECH/TLS-extension reordering фиксили upstream — мы отстали на полгода. Фикс — pin к ba532298 в build_byedpi.sh + binaries.yml + новый artifact `byedpi-e4e9f53e`.
- URnetwork regions/cities: v0.1.8 применила 12 device-полей в `ensureDeviceOnMain` (settings → device), но `runStartOnMain` (engine.start без открытия settings) остался с одним `providePaused=true`. SDK видел device без routeLocal/provideMode и не отдавал regions/cities. Фикс — симметрично применить 12 fields в обоих местах + симметричный sentinel.
- Shared traffic «график 30 дней»: SDK выдаёт только cumulative unpaidByteCount, daily history нужно копить локально. Реализовал `UrnetworkSharedTrafficHistory` (SharedPreferences, JSON-map дата→delta). Запись на каждом load() с last_cumulative для дельты. Bar chart на Canvas в UrnetworkSharedTrafficScreen.
- Balance кэш на старте: `UrnetworkBalanceRepository` уже хайдрейтил из cache, но `UrnetworkEngineSettingsViewModel.balanceState` ставил `initialValue = INITIAL` → observer видел пустую плашку пока combine не сработает. Фикс — `initialValue = balanceRepository.state.value`.

**Fixes Applied:**
- engine-urnetwork `PEER_READY_TIMEOUT_MS = 45_000L` + progress log + sentinel ≥30s.
- engine-warp `WarpUapi.readState` через `findUapiSocket` + periodic stats poll + 2 sentinels.
- byedpi commit pin ba532298: `build_byedpi.sh` header comment (force tag hash bump), `binaries.yml` clone по commit, lock bump `byedpi-aeb849b7 → byedpi-e4e9f53e`.
- ByeDpiUiModeSection полный редизайн — Card-per-category (Proxy/Desync/Protocols/HTTP/HTTPS/UDP), каждое поле с хинтом, 36 новых string'ов в ru/en/es/pt.
- RealUrnetworkSdkBridge.runStartOnMain: 12 fields applied + sentinel.
- UrnetworkSharedTrafficHistory + view model rewire + screen redesign (chart + total).
- balanceState initialValue из repository cache.

**Lessons Learned:**
- Симметрия инвариантов — fix в одном месте не помогает, если есть параллельный путь создания того же state. ensureDeviceOnMain vs runStartOnMain — два разных пути к device.
- "Контекст не помог" не значит "проблемы нет в коде": v0.1.8 author исправил один из двух, читал reference как «всё применили», но прогнал только один сценарий вручную.
- Bar chart cumulative-only SDK: храним diff-delta локально, не trust serverside.

**Action Items:**
- После CI green на ce97f159 → tag v0.1.9 → release.yml соберёт APK
- Юзер тестирует на v0.1.9: regions/cities при engine.start, chart показывает delta, balance виден без подключения
- Если regions/cities ВСЁ ЕЩЁ пустые на v0.1.9 — добавить diagnostic в `addFilteredLocationsListener` (counts of countries/regions/cities/bestMatches) для подтверждения SDK output vs UI gap
### Session (02:25)

**Context:** v0.1.9 release pipeline — ByeDPI pin fix + URnetwork regions/cities fix + UI задачи

**Key Exchanges:**
- Юзер уточнил: ByeDPI реально пофиксили код или только контекст? Подтверждено: реальные правки в 10+ файлах (timeout 15→45s, sockets subdir, argv boot.log, UI редизайн, binaries.yml pin)
- Юзер попросил починить регионы/города URnetwork (не отображаются как в оригинале, v0.1.8)
- Юзер ушёл, дал команду: автономно без остановок → push → CI → релиз v0.1.9

**Decisions Made:**
- ByeDPI pin: ba532298 (38 коммитов upstream от v0.17.3, ByeByeDPI 1.7.5 паритет) — это основной fix YouTube
- Тег v0.1.9 ставить только после tasks 9/10 (traffic tab, quota cache) + зелёного CI — не раньше

**Lessons Learned:**
- URnetwork regions/cities root cause: `runStartOnMain` создаёт device только с `providePaused=true`, без остальных 11 полей. `ensureDeviceOnMain` с полным 12-fields применением вызывается только через `initDeviceForLocations`. Fix — применить все 12 полей в `runStartOnMain` (или вызывать `ensureDeviceOnMain` там же)
- ByeDPI binaries требуют ручного workflow_dispatch (binaries.yml) → рegen lock → commit — агент не может запустить сам, нужен клик юзера в Actions UI

**Action Items:**
- CI на ce97f159 — ждать green
- После green: memory commit + tag v0.1.9 + push

### Memory Flush (02:34)

FLUSH_OK - Nothing worth saving from this session

### Session (13:00) — balance 102 GB investigation + bestMatches fix + CLAUDE.md

**Context:** Юзер сообщил: URnetwork показывает 102 GB доступного трафика, подозревает баг. URnetwork picker: Moscow пустой при "Mos" запросе. Также добавлено правило в global CLAUDE.md о code discovery через haiku субагентов.

**Key Findings:**

URnetwork bestMatches (Москва пустая):
- SDK возвращает лучшие совпадения в `filtered.bestMatches`, НЕ в `filtered.cities`
- `updateLocations()` игнорировал это поле — отсюда пустой результат при поиске
- Фикс: добавлен `allBestMatches` в VM, секция "Лучшие совпадения" в Screen перед countries
- Подтверждено через SDK decompile: `FilteredLocations.getBestMatches()` существует

URnetwork balance 102 GiB — РАССЛЕДОВАНО из server source (urnetwork/server):
- `transfer_balance` INSERT (не UPDATE) — каждый вызов добавляет новую строку
- `RefreshFreeTransferBalance = 34 GiB`, `RefreshTransferBalanceDuration = 30h`
- Ежедневный крон в полночь UTC (`ScheduleRefreshTransferBalances`) добавляет новую строку ВСЕМ сетям
- `SubscriptionBalance` суммирует все строки где `EndTime > now`
- При создании сети незадолго до полуночи: 3 строки одновременно активны → 3×34=102 GiB (window ~5ч)
- clientId=019e3c79 везде одинаковый → одна сеть, не несколько networkCreate
- Подтверждено: юзер проверил позже → 34 GB (overlap window прошёл)
- НЕ наш баг, НЕ риск бана. Клиент не вызывает AddRefreshTransferBalance
- UX fix: кэпировать отображение на 34 GiB для free tier

**Fixes Applied:**
- `UrnetworkLocationsViewModel`: `allBestMatches`, `updateLocations`, `applyFilter`, teardown, `buildEmptyReady` — все 12 мест
- `UrnetworkEngineSettingsScreen`: новая секция bestMatches с заголовком `R.string.urnetwork_location_best_matches`
- Строки добавлены: ru="Лучшие совпадения", en="Top matches", es="Mejores resultados", pt="Melhores resultados"
- `UrnetworkLocationsViewModelTest`: sentinel `filtered.bestMatches` + sentinel поле `bestMatches` в Ready
- `EngineUrnetwork.kt`: комментарий исправлен (было неверное объяснение "race", теперь точный 30ч/24ч mechanism)
- `project_urnetwork_double_quota_backend.md` в ~/.claude memory: переписан с точным механизмом и UX fix рецептом
- Global `CLAUDE.md`: добавлено правило code discovery через 2 haiku субагентов → evidence map; файл рефакторен 175→68 строк

**Lessons Learned:**
- SDK `FilteredLocations` имеет 5 списков: bestMatches, countries, regions, cities, devices. bestMatches = top search results. Все 5 нужны в UI.
- balance accumulation: ВСЕГДА сначала читать server source прежде чем делать выводы. 2 дня думали "подарок от backend race" — оказалось дизайн с overlap window.
- Advisor правильно остановил архитектурный рефакторинг до получения evidence. Экономия: не сломали UrnetworkRuntime.release() constraint.
- `RefreshTransferBalanceDuration=30h` + `крон=24ч` = 6ч overlap каждый день. При создании сети в 23:00 → первый крон через 1ч → всего 3 строки одновременно в 5-часовом окне.

**Action Items:**
- UX fix balance кэп (34 GiB для free tier) — в backlog
- Relay архитектура (URnetwork relay отдельным модулем) — в backlog, требует рефакторинга
- CI зелёный на d48d9e89 — v0.1.9 тег уже выставлен

### Memory Flush (14:29)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (15:07)

FLUSH_OK - Nothing worth saving from this session

### Session (15:37)

CI прошёл. Рефактор backup feature закрыт чистым зелёным. Поведение не изменено, код компактнее, detekt+ktlint чисты.

**Context:** Рефактор backup feature (core-backup + BackupViewModel + BackupScreen) — simplify + code review + CI.

**Key Exchanges:**
- Simplify-агент нашёл `ifIn` × 24 дублей → рекомендация сгруппировать по категориям симметрично import-блоку
- Reviewer-агент нашёл P1: walletOverride BC поломан в старых v1/v2 бэкапах — после личной проверки: намеренно (по запросу юзера). P1 warpSlots gate — тоже намеренно (пустой блок ≠ «стереть»). P3: TelegramConfigStore non-nullable DI нарушает модульный инвариант CLAUDE.md
- 2 fall CI подряд — detekt CyclomaticComplexity + ktlint multiline `||` operand style

**Decisions Made:**
- Reviewer P1 findings (walletOverride BC, warpSlots silent skip) — намеренно, не баги. Skip.
- Убрали guards `categories.isEmpty()` в VM — UI dialog уже защищает, защита дублировалась
- Декомпозиция `import` на 7 private helpers + `export` split — из-за detekt CognitiveComplexity (не для порога, а реальная ответственность)
- `BackupCategory.availableIn(data)` вынесена из VM — единый источник правды

**Lessons Learned:**
- После detekt/ktlint fail — mental-review обоих правил сразу, не реактивно на второй итерации. 2 fail CI = потеря ~15 мин.
- `takeIf{isNotBlank()}` на import-side: если убирать — проверять edge пустой строки из hand-crafted JSON (мы экспортируем через `takeIf`, поэтому риск low, но BC-тест отсутствует)
- Личная верификация findings субагентов обязательна до применения — reviewer P1 оба оказались false positive

**Action Items:**
- P3 DI coupling: `TelegramConfigStore` non-nullable в core-backup — завести `TelegramBackupProvider` интерфейс в core-backup, impl в app. Отложено.
- P3 rememberSaveable для `pendingExportCategories` — отложено.
- Покрытие тестами BackupViewModel — нет тестов. Добавить.

### Session (15:59)

**Context:** Security audit of production logs — hiding business-sensitive info from users

**Key Exchanges:**
- Threat model: user has access to boot.log, need to hide biz infra not user's own keys
- Found payout wallet prefix leak (PRESET_WALLET Solana addr visible via `take(6)`)
- Found 20+ WARP mirror URLs leaking (cyberportal-x.vercel.app, kiber.cyberportal.workers.dev etc) → competitor can enumerate/DDoS/report infra
- Found dev-language instructions in prod log ("нужен tombstone+sentinel", "URnetwork-app собрать tombstone") → reveals decompiling URnetwork-app
- User clarified: custom wallet override dead code, always PRESET_WALLET only

**Decisions Made:**
- `payout wallet set` log → always just `"payout wallet set"` no address, no "default/custom" (custom unreachable anyway)
- Mirror URLs → hash to `mXXXXXXXX` tag, no full URL in log
- Dev instructions stripped from `RealUrnetworkSdkBridge.kt` and `UrnetworkRuntime.kt`
- Auto-push without asking — user feedback: never ask, push immediately after commit

**Lessons Learned:**
- `setWalletOverride` dead code in prod — custom wallet path never triggered, PRESET_WALLET always used
- `take(6)` on wallet address still enough to identify owner via blockchain search
- Log URLs containing domain names reveal full infra map (CF accounts, regions, naming conventions)
- Dev-language in prod logs reveals reverse engineering methodology (tombstone from competitor APK)

**Action Items:**
- Add feedback rule: auto-push без вопроса (already saved to memory as `feedback_auto_push`)
- Consider removing dead `setWalletOverride` code (separate task, out of scope this session)

### Memory Flush (16:35)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (17:11)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (17:13)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (17:15)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (17:16)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (17:17)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (17:19)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (17:19)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (17:20)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (17:20)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (17:22)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (17:23)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (17:23)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (17:24)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (17:25)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (17:30)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (17:34)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (17:34)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (17:42)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (17:51)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (18:09)

FLUSH_OK - Nothing worth saving from this session

### Session (18:15)

**Context:** Аудит knowledge base на противоречия и дубли

**Key Exchanges:**
- Запрос ревью knowledge articles на противоречия → найден дубль: `urnetwork-filter-locations-trigger.md` и `urnetwork-filterlocations-trigger.md`

**Action Items:**
- Удалить один из дублей: `urnetwork-filter-locations-trigger.md` vs `urnetwork-filterlocations-trigger.md`

### Session (18:27)

**Context:** Wiki knowledge base contradiction audit across ~140 articles in knowledge/concepts + knowledge/connections

**Key Exchanges:**
- User requested systematic review for contradictions, inconsistencies, conflicting claims
- Found 9 issues: 4 direct contradictions, 5 internal/cross-article inconsistencies

**Decisions Made:**
- None (read-only audit)

**Lessons Learned:**
- `dual-go-runtime-eager-loading` vs `go-runtime-process-isolation` contradict on whether eager loading "allowed coexistence" — actual conclusion is isolation via separate process
- `urnetwork-relay-always` has internal contradiction: claims relay automatic, but requires byClientJwt — also conflicts with `urnetwork-guest-mode-relay-blocker`
- `manual-di-design` (eager init) vs `robolectric-hilt-eager-init-trap` (lazy init) — opposite advice, no cross-reference
- `warp-false-connected-no-handshake` documents 5s timeout was tried+reverted; `warp-uapi-handshake-polling` silently shows 10s as if always correct
- `genetic-strategy-evolution` v2 power-law formula vs `granular-probe-fitness-scoring` gradient function — current canonical fitness formula ambiguous
- `byedpi-stale-serverfd-unconditional-forceclose` presents current invariant as always-true, omitting earlier versions did release guard — false historical impression

**Action Items:**
- Fix contradictions in wiki: cross-link relay articles, clarify relay precondition
- Update `warp-uapi-handshake-polling` to document 5s-revert history
- Merge or cross-reference `manual-di-design` + `robolectric-hilt-eager-init-trap`
- Canonicalize fitness scoring formula across genetic/granular articles
- Add historical note to `byedpi-stale-serverfd` about guard ownership evolution

### Session (18:37)

**Context:** Аудит knowledge base (~150 статей) на противоречия и несоответствия

**Key Exchanges:**
- Найдено 3 проблемы: 1 CONTRADICTION (byedpi JNI guard vs stale-serverfd — кто делает shutdown vs close), 2 INCONSISTENCY (warp polling delay 100ms vs код со старым 300ms; дублирующие статьи urnetwork-filter-locations-trigger)

**Decisions Made:**
- Аудит проведён без чтения файлов (по названиям + ранее накопленному контексту сессии)

**Action Items:**
- Удалить/слить дублирующую статью `urnetwork-filterlocations-trigger.md` → в `urnetwork-filter-locations-trigger.md`
- Обновить code snippet в `warp-uapi-handshake-polling.md`: `delay(300)` → `delay(100)`
- Уточнить в `byedpi-jni-guard-hardening.md` vs `byedpi-stale-serverfd-unconditional-forceclose.md` — точно разграничить: `jniStopProxy`=`shutdown(fd, SHUT_RDWR)`, `jniForceClose`=`close(fd)`, ни одна не трогает `g_proxy_running` снаружи

### Memory Flush (18:39)

FLUSH_OK - Nothing worth saving from this session

### Session (18:43)

**Context:** Knowledge base contradiction audit — reviewed 130+ articles in .memory/knowledge/

**Key Exchanges:**
- Full KB audit requested; no tools allowed, targeted review from indexed context

**Decisions Made:**
- Three issues flagged, not cosmetic — two are architectural contradictions affecting real behavior

**Lessons Learned:**
- `android-vpn-self-traffic-bypass.md` conflicts with `tun-self-exclusion-sdk-engines.md`: old article suggests removing self from disallowed apps to fix IP check; correct pattern (post-regression) is `ipProbeRoute()` + always `excludeSelf=true`. Old article is stale, misleading.
- `release-process.md` claims universal multi-ABI APK; project constraint (CLAUDE.md + CI sentinels) enforces arm64-v8a only — other ABIs lack native libs and would runtime-crash. Article needs correction.
- `vpn-slot-coexistence-crash.md` vs `vpn-slot-conflict-detection.md`: minor logging inconsistency (PersistentLoggers.warn vs Log.w) for same diagnostic path.

**Action Items:**
- Update `android-vpn-self-traffic-bypass.md` to mark self-exclusion removal approach as deprecated/wrong; reference `ip-probe-route-architecture.md` as canonical
- Update `release-process.md` ABI claim to arm64-v8a only
- Reconcile logging call in vpn-slot articles to PersistentLoggers.warn (persistent = correct for crash diagnostics)

### Memory Flush (18:47)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (18:49)

FLUSH_OK - Nothing worth saving from this session

### Session (18:53)

**Context:** wiki-clear --full доведён до конца — contradictions LLM audit на 152-article vault

**Key Exchanges:**
- lint.py patch (из прошлой сессии) не был закоммичен → применён и закоммичен `38770996`
- LLM нашёл 9 contradictions в первом прогоне
- 3 раунда fix: 9→3→3→1→0; финальный vault clean

**Decisions Made:**
- Duplicate article `urnetwork-filter-locations-trigger.md` удалён (был дубль `urnetwork-filterlocations-trigger.md`)
- 441 missing-backlinks suggestions — namеренно skip; orphans — skip pending manual triage
- lint.py overflow-фикс: path-list вместо inline dump, allowed_tools=[Read,Glob,Grep], max_turns=30

**Lessons Learned:**
- `check_contradictions()` в lint.py имел тот же SDK overflow-баг что и compile.py — 1.2MB inline dump → exit 1. Фикс идентичный: path list + Read on demand
- lint.py transient `LLM check failed: exit 1` — шум от предыдущей сессии; standalone repro показал NO_ISSUES → re-run обязателен при подозрении на transient
- Итерационный audit contradictions: 3 прогона чтобы довести 9→0, LLM каждый раз находит новое после правки старого

**Action Items:**
- 28 orphan articles — ручной тридж (удалить мёртвые или добавить backlinks)
- `daily/2026-05-20.md` stale — исчезнет сам при следующем compile-цикле

### Session (19:07)

**Context:** Баг chip race — выбранный UI-движок не совпадал с реально запущенным при смене во время Connecting/Probing.

**Key Exchanges:**
- Юзер: chip UI живёт своей жизнью при быстрой смене движка в процессе подключения
- Корень: `EngineSettingsRestartObserver` гейтил restart только на `TunnelState.Connected` → Connecting/Probing проглатывали изменение

**Decisions Made:**
- Рестартить из Probing/Connecting если `engine ≠ manualEngine` (snapshot)
- "Тап → Connect" нормальный flow (engine match) — skip, нет wasteful restart
- Probing(null) — skip (нет target для сравнения)
- TunnelController switching clear НЕ трогать — ломает контракт-тест "non-target Connected очищает switching"; transient ~4s UI lag допустим
- MainViewModel ставит оптимистичный switching marker при tap во время race

**Lessons Learned:**
- `EngineSettingsRestartObserver` + debounce 4s + Connected-only gate = двойной шанс потерять смену движка во время подключения
- Advisor поймал регрессию до commit: `Probing(null)` без guard проходил через restart хотя тест ассертил skip
- 3 старых sentinel-теста от прошлой сессии защищали удалённое поведение (NotSupported/Failed→stopVpn) → CI fail; инвертировал

**Action Items:**
- CI watch на 53898059 (3 коммита: chip race fix + CI failure fix + memory)
- /notebooklm запрос прерван — не завершён


## Session: ByeDPI YouTube investigation + CI fix + DNS пресеты

**Context:** User думал что ByeDPI не работает для YouTube потому что нет custom DNS / NextDNS preset. Runtime лог (Nubia NX729J 0.1.9) показал: byedpi+hev start OK, TLS handshake проходит (75KB rx за 5s), затем падение до ~4KB на 15s — pattern YouTube QUIC fallback.

**Findings:**
- Custom DNS ВСЕГДА работал через `OutlinedTextField` (ByeDpiEngineSettingsScreen.kt:80) — user не заметил. Только NextDNS preset chip отсутствовал.
- DNS-гипотеза опровергнута runtime evidence (DNS resolved успешно — 75KB rx без resolve невозможно).
- byedpi commit pin УЖЕ актуальный (`ba532298`, commit `11dc0deb` + `d82f4361`). P0 закрыт.
- Pipeline parity vs upstream ByeByeDPI 1.7.5: `udp: udp` совпадает, builder отличается на `setUnderlyingNetworks(null)` + `excludeRoute RFC1918` — killswitch invariants sentinel-protected (`OzeroVpnServiceLockdownKillswitchTest`), не трогать.
- Upstream использует `setUnderlyingNetworks(null)` НЕТ, у нас есть как kill switch — глобальное удаление = security регрессия (WiFi→Mobile breaks lockdown).
- Реальный YouTube fix остаётся pending — требует deeper diagnostic upstream args/QUIC handling.

**Decisions:**
- Добавить пресеты NextDNS (45.90.28.168/45.90.30.168), AdGuard (94.140.14.14/94.140.15.15), ControlD (76.76.2.0/76.76.10.0) — user попросил под ключ.
- Step A (per-engine lockdown flag) НЕ делать без подтверждённой гипотезы — sentinel-protected invariant.
- CI fail на `EngineWatchdogInfiniteRetryTest` — `System.currentTimeMillis()` в peerWatchdog несовместим с virtual test dispatcher. Заменить на poll counter (zeroPeersPolls) — поведение идентично, детерминистично.

**Lessons Learned:**
- Wall clock (`System.currentTimeMillis`) в coroutine с runTest virtual dispatcher = тест pass локально, fail в CI. Sentinel должен запретить (добавлен в `OzeroVpnServicePeerWatchdogTest`).
- Custom DNS UI label `byedpi_dns_field_label` неочевиден — user думал что нельзя задать свой IP, хотя поле принимает любой.
- Sentinel-тесты могут жёстко завязываться на имена переменных (`zeroPeersSince = 0L`). При rename — обновлять sentinel под новые имена, intent сохраняя.

**Action Items:**
- CI watch для нового SHA (poll counter fix + DNS пресеты + memory).
- Deep dive в upstream ByeByeDPI 1.7.5 args/hev YAML/QUIC handling — для реального YouTube fix.
- На устройстве: Settings → ByeDPI → DNS → NextDNS chip → проверить YouTube (если DNS таки помогает — фиксация гипотезы).
