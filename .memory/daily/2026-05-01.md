# 2026-05-01 — pre-release fix wave для v0.0.2-1

## Что сделано

После diff-ревью feature → dev юзер запросил план фикса всех регрессий
перед merge → feature и tag v0.0.2-1.

5 atomic коммитов на ветке `claude/review-feature-branch-1dxJy` (база =
origin/feature):

1. `FIX(C3): Migration_4_5 runtime test — Robolectric SQLite`
   - `gradle/libs.versions.toml`: + room-testing, robolectric 4.13, junit4,
     junit-vintage-engine.
   - `core-storage/build.gradle.kts`: testImplementation deps,
     `testOptions.unitTests.isIncludeAndroidResources = true`.
   - `OzeroDatabase.MIGRATION_4_5` private → internal.
   - НОВЫЙ `MigrationFourToFiveRuntimeTest.kt` (6 runtime тестов через
     `FrameworkSQLiteOpenHelperFactory`).

2. `FIX(C1): preload settings/split в onCreate — убрать runBlocking из startVpn`
   - `OzeroVpnService`: `@Volatile cachedSettings`/`cachedSplitPackages`,
     `startSettingsCachePreload()` в onCreate (collect Flow + activePackages),
     refresh split в фоне при каждом startVpn. Удалены import runBlocking + first.
   - Sentinel-тесты в `OzeroVpnServiceLifecycleTest`: startVpn без
     runBlocking + onCreate с preload + collect + activePackages.

3. `FEAT(W7.1 UI): MainScreen badge для HealthMonitor DEGRADED`
   - `MainViewModel`: + healthMonitor ctor + healthStatus StateFlow.
   - `MainScreen`: бейдж "Соединение нестабильно" под TrafficStatsCard
     при `healthStatus == DEGRADED && state == Connected`.
   - strings.xml + values-en/strings.xml: `main_health_degraded`.
   - `MainScreenTestTags.HEALTH_DEGRADED_BADGE`.
   - `MainViewModelTest`: ctor с двумя args + healthStatusInitiallyUnknown.

4. `FIX(W9.1): LanguageSection only RU/EN до W9.2 — UX hygiene`
   - `LocaleApplier.SUPPORTED_TAGS = ["", "ru", "en"]`.
   - `SettingsScreen.LanguageSection`: filter по SUPPORTED_TAGS.
   - НОВЫЙ `LocaleApplierTest`: sentinel против регрессии.
   - 9 неподдерживаемых string keys оставлены — для W9.2.

5. `DOCS(plan): C1/C3/W7.1-UI/W9.1 closed pre-release v0.0.2-1`
   - `PLAN.md`: статус 42 closed / 9 pending. W7.1, W7.3, W7.4 → closed.
     Pre-squash gate секция обновлена. Добавлена `## v0.0.2-1 release readiness`.
   - `feature-branch-code-review-2026-05-01.md`: post-fix update сверху.

## Открытые вопросы / следующие шаги

- Локальный gradle недоступен (403 на dl.google.com, кэш пуст) — паттерн
  CLAUDE.md `feedback_no_local_tests`. Верификация только через CI.
- После push на claude/review-feature-branch-1dxJy → watch CI.
- При зелёном CI и одобрении юзера: ff-merge → origin/feature.
- Юзер потом делает manual smoke install-over-upgrade и tag v0.0.2-1.
