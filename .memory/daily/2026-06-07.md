# Daily Log: 2026-06-07
## Sessions
### Session
- Context: разбор свежих GitHub Actions run на `dev` по новому SHA, без опоры на старые статусы.
- Key exchanges:
  - Использован workflow: `gh run list` по новому SHA, затем `gh run watch` до terminal status, затем `gh run view --job --log-failed`.
  - Зафиксирован свежий run `27071720616` на SHA `c720f268` как ранний ориентир для CI-цикла.
  - Позже подтверждено, что текущие падения были не coverage-only, а unit-test failures, из-за чего JaCoCo-артефакты не публикуются.
- Decisions made:
  - Разбирать только самый свежий run по SHA после push.
  - Фиксить реальные contract/regression failures, а не brittle source asserts.
  - Перед публикацией памяти и логов исключать личные пути и host-specific details.
- Lessons learned:
  - Личные пути и machine-specific details нельзя сохранять в memory log.
  - Для duplicate subscription profiles нужен consumption of matched rows per stable key, иначе REPLACE затирает earlier rows.
### Session
- Context: локальная подготовка исправления duplicate profile matching и санитизация memory log.
- Key exchanges:
  - User requested not to push yet, only inspect the latest run and fix issues locally.
  - Latest run on SHA `86bc0bcf` still failed on unit-test compilation in `common-vpn`.
- Decisions made:
  - Replace stable-key matching with consumed matches per key.
  - Add regression coverage for duplicate stable-key profiles so the second match gets a fresh ID.
- Lessons learned:
  - Fresh CI evidence remains the source of truth; older runs can hide the active blocker.
