# Daily Log: 2026-06-07
## Sessions
### Session
- Context: разбор свежих GitHub Actions run на `dev` по новому SHA, без опоры на старые статусы.
- Key exchanges:
  - Использован workflow: `gh run list` по новому SHA, затем `gh run watch` до terminal status, затем `gh run view --job --log-failed`.
  - Зафиксирован свежий run `27071720616` на SHA `c720f268` как ранний ориентир для CI-цикла.
  - Позже подтверждено, что текущие падения были не coverage-only, а unit-test failures, из-за чего JaCoCo-артефакты не публикуются.
- Decisions made:
  - Разбирать только самый свежий run по SHA после push.
  - Фиксить реальные contract/regression failures, а не brittle source asserts.
  - Перед публикацией памяти и логов исключать личные пути и host-specific details.
- Lessons learned:
  - Личные пути и machine-specific details нельзя сохранять в memory log.
  - Для duplicate subscription profiles нужен consumption of matched rows per stable key, иначе REPLACE затирает earlier rows.
### Session
- Context: локальная подготовка исправления duplicate profile matching и санитизация memory log.
- Key exchanges:
  - User requested not to push yet, only inspect the latest run and fix issues locally.
  - Latest run on SHA `86bc0bcf` still failed on unit-test compilation in `common-vpn`.
- Decisions made:
  - Replace stable-key matching with consumed matches per key.
  - Add regression coverage for duplicate stable-key profiles so the second match gets a fresh ID.
- Lessons learned:
  - Fresh CI evidence remains the source of truth; older runs can hide the active blocker.
### Session (12:32)

**Context:** Работа над зелёным `dev` CI для Ozero: фиксы контрактных багов, добор покрытия тестами, подготовка и пуш изменений в `dev`.

**Key Exchanges:**
- Обсуждали, что `engine-warp` сначала выглядел как coverage-gap, но затем выявился реальный контрактный регресс в `WarpTurnOnResult` по parcel roundtrip.
- Было принято перевести parceling `ParcelFileDescriptor` на стандартный `writeParcelable/readParcelable` и усилить тесты `WarpTurnOnResultTest` проверкой `describeContents()`.
- Для `buildSrc` нашли отдельный branch-coverage gap и связанный риск падения `LockFileParser` на странных `artifacts` payload’ах; parser сделали безопаснее и добавили регрессионные тесты.
- Добавляли точечные тесты в `common-dns`, `common-net`, `core-backup` и другие модули, чтобы закрывать реальные missed branches, а не только happy-path.
- Перед пушем пользователь отдельно потребовал сначала починить локальный lint и затем исправить два прод-багa: late queued settings restarts в `RuntimeConfigRestartCoordinator` и fail-closed fallback без реального blocking TUN.

**Decisions Made:**
- Сначала чинить корневые regressions и только потом добирать coverage, чтобы не маскировать баги тестами.
- `WarpTurnOnResult` переводится на стандартный parcel protocol, потому что ручная запись/чтение `ParcelFileDescriptor` оказалась хрупкой для текущего AIDL-контракта.
- `LockFileParser` должен быть детерминированным и не падать на `ClassCastException` при нетипичном `artifacts` input.
- Для restart/fail-closed поведения нужно локализовать правки в правильных owning files в `app`, а не в `common-vpn`, и проверять атомарность drain/reset очереди и наличие реального blocking TUN.

**Lessons Learned:**
- Нельзя опираться только на coverage report: часть "misses" оказалась настоящими контрактными регрессиями.
- Парcel roundtrip и runtime restart logic лучше фиксировать через явные регрессионные тесты, иначе баги возвращаются молча.
- Перед пушем нужно проверять lint заранее, чтобы не создавать лишние CI-итерации и не тратить токены на повторные прогоны.

**Action Items:**
- Завершить локальный lint и исправить все найденные ошибки до следующего пуша.
- Довести фиксы `RuntimeConfigRestartCoordinator` и `ShutdownCoordinator` до корневого решения.
- Добавить/обновить регрессионные тесты для queued restart drain и fail-closed TUN fallback.
- Снова запустить CI по новому SHA и добить оставшийся `buildSrc` coverage-gap, если он ещё останется.

### Session (13:11)

**Context:** Работа над исправлением координатора и последующим циклом проверки через lint/тесты, затем переход к разбору свежих GitHub Actions логов и последовательному исправлению всех найденных проблем.

**Key Exchanges:**
- Обсуждали стратегию локальной валидации: вместо общего комбайна тестов предлагалось гонять модули по одному, чтобы быстрее видеть первый реальный фейл и чинить его сразу.
- Выяснилось, что локальные падения тестов на Windows в основном связаны не с логикой, а с блокировками `build/` и проблемами Gradle/daemon, то есть это ограничение окружения.
- На вопрос, стоит ли вообще чинить локальный прогон, был сделан вывод, что для поиска кодовых проблем надежнее опираться на GitHub Actions, а локально оставить только `ktlint/detekt` и точечные тесты.
- По прямому запросу было решено взять актуальные логи свежего GitHub Actions run на текущем `dev` SHA и разбирать все красные jobs по очереди, включая баги и пробелы в тестовом покрытии.

**Decisions Made:**
- Перейти от локального full-suite подхода к модульным прогонам или CI-логам, потому что локальная среда на Windows дает ложные фейлы из-за file-lock’ов.
- Использовать GitHub Actions как источник истины для текущего состояния ветки, чтобы не тратить время на нестабильные локальные ошибки окружения.
- Разбирать проблемы последовательно: сначала compile/style blockers, затем тестовые падения, затем coverage gates.

**Lessons Learned:**
- На Windows Gradle/Android build outputs могут ломать локальную проверку из-за `AccessDeniedException` и конфликтов с `build/` директориями, даже если код корректен.
- Для больших Android-проектов локальный “всё сразу” прогон часто хуже, чем CI или точечные тесты по модулю.
- Стабильная диагностика начинается с актуального SHA и свежих CI-run, иначе легко чинить уже неактуальные симптомы.

**Action Items:**
- Получить и разобрать все ошибки из свежего GitHub Actions run на последнем `dev` SHA.
- Исправлять проблемы последовательно по списку CI, включая кодовые баги и недостающие тесты.
- При необходимости дописывать тесты для закрытия coverage gap.

### Session (13:21)

**Context:** Разбирали и чинили массовые падения свежего CI run на одном SHA по нескольким модульным джобам, начиная с `dev`/`push` как источника истины.

**Key Exchanges:**
- Уточнили, что красными были не только `app`, но весь matrix: `buildSrc`, `common-vpn`, `core + common`, `engine-urnetwork + engine-byedpi`, `engine-warp`, `singbox + extra modules`.
- Зафиксировали подход: сначала собрать failed-логи свежего run, потом отделить compile/unit failures от coverage gates.
- Выяснилось, что часть падений была не в проде, а в тестах и контрактах: stale expectations, type inference, DataStore/state race, tolerant parser, parcel roundtrip.
- Согласовали, что сначала чинятся source-level failures, а coverage gates разруливаются только после возвращения unit tests в green.

**Decisions Made:**
- Основным источником истины взять `push`-run последнего SHA, а PR-run использовать для сверки.
- Сначала закрывать compile/unit failures, потому что coverage нельзя честно добивать на фоне красных тестов.
- Для FPTN store убрать stale initial state и синхронизировать in-memory snapshot с flow, чтобы UI и engine не видели старую конфигурацию.
- Для URnetwork store убрать сериализацию optional values через `JSONObject.NULL`, чтобы не ловить NPE и не оставлять мусор в cache.
- Для `common-vpn` обновить хрупкий source-based тест под уже введенный `hasBlockingTunForKillswitch()`.
- Для WARP перейти на явный parcel contract с marker-ами вместо зависимости от нестабильной Parcelable-сериализации.

**Lessons Learned:**
- `StateFlow` с initial default может давать stale read в CI и в runtime, если update не синхронизирует локальный snapshot.
- `JSONObject.NULL` в Kotlin/Java bridge может ломать non-null paths и лучше обходиться явными nullable/marker схемами.
- Source-assert тесты на строки кода быстро устаревают после рефакторинга и требуют обновления под фактический инвариант.
- В Android `Parcel`-контрактах лучше не опираться на deprecated/неявные сериализационные пути, особенно для nullable `ParcelFileDescriptor`.
- Для CI-расследования сначала нужно снять сигнатуры ошибок по всем jobs, иначе легко зациклиться на одном симптоме.

**Action Items:**
- Дождаться нового свежего `dev` run после пачки правок.
- Проверить, какие jobs остались красными, и отдельно добить coverage failures в `engines-core`, `common-dns`, `common-net`, `core-backup`, `singbox-config`, `engine-masterdns`, `engine-singbox`, `singbox-subscription`.
- После стабилизации собрать commit и пуш, не оставляя `.memory/` незакоммиченной.

### Session (13:34)

**Context:** Работали над доведением `dev` CI до зелёного: дописывали тесты для покрытия coverage-дыр и затем сделали push, чтобы снять свежий CI run по новому SHA.

**Key Exchanges:**
- Было решено не останавливаться на частичном покрытии и добивать ещё ветки тестами, потому что без свежего run нельзя честно утверждать, что CI станет зелёным.
- Зафиксировано, что основной фокус был на дешёвых pure-logic ветках: `common-dns`, `engines-core`, `common-net`, `engine-masterdns`, позже рассмотрен `engine-singbox`, но без слепого захода без нового evidence.
- После команды «Окей, пуш» было подтверждено, что нужно собрать ближайший рабочий push: закоммитить код вместе с `.memory`, запушить в `dev`, затем брать только свежий run с нового SHA как источник истины.
- Новый CI run упал на `ktlint + detekt`, при этом тестовые jobs даже не стартовали, потому что pipeline остановился на lint.
- Последнее состояние: нужен точный хвост лога job-а с конкретным файлом и правилом, чтобы исправлять причину, а не гадать по аннотациям.

**Decisions Made:**
- Не считать CI зелёным без свежего подтверждённого run после новых тестов.
- Добивать сначала дешёвые branch-ветки в чистой логике, а не лезть сразу в тяжёлую Android/service-инфраструктуру.
- Коммитить `.memory` вместе с рабочими изменениями, а не отдельно.
- После падения CI разбирать именно свежий run по новому SHA, а не старые логи.

**Lessons Learned:**
- Coverage нужно добирать не отдельными happy-path тестами, а ветками, которые реально закрывают branch points.
- `ktlint`/`detekt` могут остановить pipeline до запуска тестов, поэтому для зелёного CI нужно сначала убрать lint-failures.
- Для исправления lint важно смотреть конкретный файл и правило, а не только общий статус job-а.
- Свежий CI run по новому SHA важнее старых цифр и старых падений, иначе легко чинить не ту проблему.

**Action Items:**
- Получить точный хвост лога упавшего `ktlint + detekt` job-а.
- Исправить конкретный lint/detekt issue в названном файле.
- После фикса снова пушнуть и проверить свежий `dev` run.

### Session (14:03)

**Context:** Работали над CI-стопперами в репозитории, сначала локально добивали lint/detekt, затем переключились на анализ свежих failing jobs из последнего `dev` run и расширение тестового покрытия.

**Key Exchanges:**
- Пользователь настоял на локальном прогоне линта и detekt, затем отдельно потребовал закрепить правило: GitHub Actions-логи и сетевые проверки нужно сразу делать вне песочницы, не пытаясь сначала в sandbox.
- После локального `ktlintCheck detekt` был найден и устранён реальный blocker в `engine-fptn`: неиспользуемый `scope`, плюс параллельно была исправлена unsafe cast-цепочка в `buildSrc` parser.
- Пользователь отметил, что прогресс выглядит слабым, потому что снова падают те же группы test jobs в CI и текущий батч изменений слишком маленький.
- После просмотра свежего CI-run было зафиксировано, что падения затрагивают сразу несколько областей: `app`, `buildSrc`, `common-vpn`, `core + common`, `engine-urnetwork + engine-byedpi`, `engine-warp`, `singbox + extra modules`.
- При разборе логов выяснилось, что в `buildSrc` ломаются именно validation-тесты парсера, а не runtime, то есть изменения затронули контракт валидации.

**Decisions Made:**
- Для GitHub Actions и любых сетевых логов использовать только внепесочечный путь, без попыток сначала запускать это в sandbox.
- Не ограничиваться точечными фикcами по одному тесту, а разбирать и чинить пачку failing jobs за итерацию.
- В `buildSrc` вернуть строгую валидацию для `artifacts`, сохранив безопасный обход для root-map, чтобы починить validation-тесты без отката безопасного парсинга.

**Lessons Learned:**
- Локальный `ktlintCheck detekt` может быстро убрать класс blocker'ов, но после этого нередко вскрываются уже test-level regressions.
- Улучшение CI не всегда линейное: можно снять lint/detekt-барьер, но одновременно открыть регрессию в validation-тестах на соседнем слое.
- Для оценки прогресса важно смотреть не только на факт новых падений, а на то, на каком уровне пайплайна они происходят: переход от lint-blocker к тестовым регрессиям означает движение вперёд, даже если красных jobs пока много.

**Action Items:**
- Починить `buildSrc` validation-тесты парсера, сохранив безопасный YAML-парсинг.
- Добрать ещё покрытие и фиксы на соседних модулях, а не ограничиваться одним файлом или одной группой тестов.
- Дальше разбирать свежие failing job-логи пачкой и закрывать следующий широкий набор регрессий.

### Session (14:25)

**Context:** Пользователь добивал dev CI до зелёного: фиксировал реальные тестовые регрессии и добирал coverage в `buildSrc`, `engine-byedpi`, `engine-urnetwork`, `singbox-fmt` и смежных модулях.

**Key Exchanges:**
- Обсудили стратегию: сначала закрывать конкретные падения тестов, а затем добирать coverage-гейты пачкой в тех же модулях.
- Для `singbox-fmt` нашли причину: пустой query-параметр ломался из-за пустого сегмента `&&`; это признали реальным контрактным багом парсера и решили исправлять код вместе с тестом.
- Для `engine-byedpi` выяснили, что `--split -bad -K` проходит через слишком слабую проверку длинных опций; это было расценено как баг валидатора, а не тестовая ошибка.
- Для `engine-urnetwork` несколько раз уточняли источник падения: сначала race/serialization ambiguity, затем контракт cached locations; в итоге причина сузилась до несоответствия между сырым `next` и нормализованным persisted contract.
- Локальная валидация упиралась не только в код, но и в среду: Gradle пытался скачать wrapper и блокировался сетью, а затем `engine-urnetwork` ловил Windows file-lock на `build/intermediates`, после чего тесты всё же стали падать уже по содержимому.

**Decisions Made:**
- Закрывать явные регрессии и regression-тесты раньше, чем добирать чистый coverage.
- В `engine-urnetwork` сначала хранить актуальный снапшот в памяти и приводить его к нормализованному persisted contract, чтобы тесты проверяли именно сериализованное состояние.
- Для cache-полей в `engine-urnetwork` обновлять `latest` уже в capped-нормализованном виде, чтобы одновременно закрыть `max size` и roundtrip-ветки.
- `buildSrc`, `engine-byedpi` и `singbox-fmt` считались локально зелёными после правок, чтобы не тратить батч на уже закрытые участки.

**Lessons Learned:**
- `NoSuchElementException` в тестах часто означает не поломку логики, а несоответствие ожиданий реальному сериализуемому контракту.
- Для `engine-urnetwork` важнее опираться на persisted contract, чем на сырой промежуточный `next`, иначе roundtrip-тесты остаются хрупкими.
- `build/intermediates` на Windows может маскировать реальную регрессию file-lock шумом, поэтому сначала нужно исключить удержание файлов предыдущим процессом.
- Для парсеров и валидаторов лучше фиксировать именно наблюдаемое поведение, а не только поднимать coverage числами.

**Action Items:**
- Добить последний `engine-urnetwork` test blocker: компиляционную ошибку из-за конфликта имени `normalized`.
- Повторно прогнать `engine-urnetwork` после исправления имени helper-а.
- После закрытия `engine-urnetwork` снять свежий `dev` CI run.
- Затем добрать оставшиеся coverage-gates пачкой, а не по одному тесту.

### Session (14:58)

**Context:** Работали над доведением `dev` CI до зелёного, разбирая последовательные blockers в `singbox-process`, `engine-urnetwork`, `engine-warp` и связанных тестах/входных артефактах.

**Key Exchanges:**
- Было подтверждено, что `engine-urnetwork` и `engine-warp` уже починены локально: нормализация cached locations в `DataStoreUrnetworkConfigStore` и явный parcel-контракт в `WarpTurnOnResult`.
- Выяснилось, что падение `singbox-process` сначала выглядело как code-level compile blocker, но фактически зависело от CI bootstrap-артефактов `libbox.jar` и `go-stubs.jar`, которых не было в локальном дереве.
- После воспроизведения CI-подкладки `singbox-process` стал компилироваться, что подтвердило: проблема была в сборочном входе, а не в самом модуле.
- В `app:testDebugUnitTest` всплыл новый blocker: дубликат исходника `UrnetworkPreferredLocationConnector (1).kt`, вызывавший `redeclaration`.
- После устранения мусорного файла и повторного прогона выявилась пачка реальных failing tests, среди которых особенно заметны `UrnetworkLocationsViewModelTest`, `LoggingContract`, `SilentPackageInstaller`, `FormatUtils`, `RuntimeConfigRestartCoordinator`.
- Для `RuntimeConfigRestartCoordinatorTest` было установлено, что `TunnelController.onConnecting()` из `Idle` не стартует restart path, а `Intent` в тесте терял action из-за способа создания.
- Для `FormatUtils` выяснилось, что баг связан только с `Locale`, а не с самими единицами измерения.
- Для `LoggingContract` проблема оказалась в сканировании лишних non-source docs, включая `.codex/Контекст`.

**Decisions Made:**
- Не лечить `singbox-process` вслепую, а сначала воспроизвести CI bootstrap-окружение и отделить ложный red от реального кода.
- Считать отсутствие `libbox.jar/go-stubs.jar` именно build wiring issue, а не дефектом сервиса.
- Удалить дублирующийся `UrnetworkPreferredLocationConnector (1).kt` как репозиторный мусор, потому что он сам по себе ломал compile.
- Для `RuntimeConfigRestartCoordinator` чинить не только тест, но и контракт создания `Intent`, чтобы action сохранялся явно и поведение было детерминированным.
- В `FormatUtils` сохранять текущие русские суффиксы, меняя только locale-обработку, а не пользовательский контракт вывода.

**Lessons Learned:**
- CI может быть зелёным только в своей bootstrap-конфигурации; локальный workspace без нужных jar-артефактов даёт ложные compile failures.
- Дубликаты исходников с суффиксом `(1)` могут выглядеть как мелкий мусор, но полностью ломают компиляцию.
- В тестах на restart/state-machine важно проверять не только состояние, но и то, как создаётся `Intent`, иначе action может silently потеряться.
- Локальные `Locale`-ошибки часто маскируются под форматирование, хотя реальная проблема только в decimal separator.
- `LoggingContract` не должен сканировать служебные и документационные директории, если контракт касается source-only surface.

**Action Items:**
- Довести `RuntimeConfigRestartCoordinatorTest` до сценария, где restart реально идёт из явно connected state.
- Добить оставшиеся failing tests из `app:testDebugUnitTest`, особенно `UrnetworkLocationsViewModelTest`.
- Повторно прогнать `:app:testDebugUnitTest --continue` в CI-меченой конфигурации и собрать следующий terminal signal.
- Проверить рабочее дерево на нежелательные бинарники/артефакты после локального bootstrap.
- После стабилизации всех падений подготовить и прикрепить нужный commit/push вместе с `.memory/` изменениями.

### Session (15:21)

**Context:** Исправляли красный `dev` CI в `RuntimeConfigRestartCoordinatorTest`, затем планировали добивать остальные unit-тесты и покрытие.

**Key Exchanges:**
- Обсуждали, что тест не должен идти через reflection к private `suspend` helper; решили перевести его на публичный контракт `start(scope)` и реальный observer/runtime path.
- Выяснили, что проблема была не в бизнес-логике restart, а в тестовом окружении: `android.content.Intent` в JVM unit test через stub не вел себя как в runtime.
- Для стабилизации теста переключили его на Robolectric и реальный application context, а также привели SDK-конфигурацию к поддерживаемому значению.
- После этого тест дошёл до `startService`, и оставалось только синхронизировать ожидание с реальным `ACTION_RESTART_RUNTIME_CONFIG`.

**Decisions Made:**
- Отказаться от reflection для проверки private `suspend` helper и тестировать через публичный контракт, чтобы проверять реальное поведение очереди и restart flow.
- Использовать Robolectric вместо добавления production seam, чтобы не менять архитектуру только ради unit-теста.
- Зафиксировать `@Config(sdk = [34])`, потому что текущий Robolectric/runtime mismatch мешал выполнению теста.
- Не останавливаться на одном тесте: после его починки продолжить полный `:app:testDebugUnitTest --continue` для снятия следующих падений и расширения покрытия.

**Lessons Learned:**
- JVM unit tests с Android stub-классами могут падать раньше бизнес-кода, особенно на `Intent`/`Context`-поведении.
- Если тест не доходит до ожидаемого action, сначала нужно проверить test runtime и объектную модель, а не сразу production-логику.
- Диагностические asserts помогли отделить «не падает» от «не выполняет нужный путь», что сузило проблему до окружения и контракта action constant.

**Action Items:**
- Запустить полный `:app:testDebugUnitTest --continue` и снять следующий слой падений.
- Добить красный CI dev через дальнейшее расширение тестов, а не только через точечный фикс одного кейса.

### Session (15:39)

**Context:** Продолжалась стабилизация `:app` после падений unit-тестов и последующая доборка coverage через `JaCoCo`.
**Key Exchanges:**
- Сначала были устранены падения в `UrnetworkLocationsViewModelTest`: проблема оказалась не только в прод-логике, но и в тестовом scheduler mismatch между `Dispatchers.Main` и `runTest`.
- Затем был найден и исправлен реальный регресс в `UrnetworkLocationsViewModel`: после bootstrap и при обновлениях store экран не должен оставаться `NotConnected`, если SDK/device уже доступен.
- Отдельно было исправлено поведение config-store emissions, чтобы они не перетирали `Ready.selectedLocation` сразу после пользовательского выбора.
- После этого полный `:app:testDebugUnitTest --continue` стал зелёным: `1065/1065`.
- Далее был запущен `JaCoCo`; выяснилось, что unit-тесты зелёные, но coverage gate всё ещё сильно ниже порога: сначала около `0.65/0.54`, затем после добора тестов примерно `0.67/0.55`.
- Для роста покрытия были выбраны крупные непокрытые зоны, в частности `ApkDownloader`, где добавили runtime-тесты на success, HTTP error, size limit и `mkdirs()` failure.

**Decisions Made:**
- Сначала добивать причину падений в `UrnetworkLocationsViewModelTest`, а не маскировать их прод-фиксом.
- Фиксить `UrnetworkLocationsViewModel` на уровне owning layer, потому что там был реальный UI-regression, а не только тестовая нестабильность.
- Не останавливаться на зелёных unit-тестах и идти дальше в `JaCoCo`, потому что coverage gate остаётся красным.
- Добор покрытия вести по самым “жирным” непокрытым классам, а не дробить усилия на мелкие участки.

**Lessons Learned:**
- Совпадение зелёных unit-тестов не означает, что coverage gate близок к прохождению.
- В `UrnetworkLocationsViewModel` критично не давать `inactive/config-store` веткам перетирать уже достигнутый `Ready`.
- Для новых тестов `File` нельзя наследовать, поэтому failure-case для `ApkDownloader` пришлось строить через существующий файл как родительский путь.
- После устранения логических багов полезно сразу перепроверять coverage, чтобы не переоценить прогресс.

**Action Items:**
- Добить coverage по крупным непокрытым зонам из `jacocoTestReport.xml`, включая `SingboxEngineSettingsViewModel`, `BootDiagnostics`, `DefaultAppListProvider`, `LogcatReader`, `UrnetworkLocationsViewModelKt`, `AppListProviderKt`.
- Снова прогнать `:app:jacocoTestReport :app:jacocoTestCoverageVerification`.
- После зелёного `app` перейти к остальным required-модулям.
- Пуша ещё не было.

### Session (15:42)

**Context:** Продолжалась доводка `dev` до зеленого состояния: фиксы в `UrnetworkLocationsViewModel` и `RuntimeConfigRestartCoordinator`, добор unit/runtime coverage и подготовка к пушу на `dev` для прогона CI.

**Key Exchanges:**
- Был найден и исправлен баг, при котором `UrnetworkLocationsViewModel` мог откатывать `Ready` обратно в `NotConnected` после config-store emission без cache, хотя движок уже был running/device available.
- Было уточнено, что config update не должен затирать текущий пользовательский `selectedLocation`; collector изменили так, чтобы сохранять уже выбранный token.
- Полный `:app:testDebugUnitTest` стал зеленым: 1065/1065 тестов.
- Coverage gate `:app:jacocoTestCoverageVerification` остался красным; после `jacocoTestReport` выяснилось, что coverage все еще сильно ниже порога, примерно `lines 0.67`, `branches 0.55` при цели `0.95`.
- Для добора покрытия были выбраны самые “жирные” недопокрытые зоны, включая `BootDiagnostics`, `ApkDownloader`, `DefaultAppListProvider`, `LogcatReader`, `SingboxEngineSettingsViewModel`.
- Добавлены runtime-тесты для `ApkDownloader` на success, HTTP error, size limit и `mkdirs()` failure.
- Для `BootDiagnostics` начата отдельная ветка покрытия: `guard`, `guardUnit`, `installUncaughtHandler` с проверкой crashSink.
- Пользователь попросил попробовать уже сейчас запушить имеющееся на `dev`, чтобы запустить CI, и продолжать работу дальше по результатам.

**Decisions Made:**
- Сначала добивать реальные баги и покрытие в крупных непокрытых классах, а не повторять уже зеленые участки.
- Не останавливаться на успешном unit-pass: считать релевантным только состояние после `jacoco` gate.
- Пушить текущую рабочую точку на `dev`, чтобы получить свежий CI-прогон и не терять время в локальной изоляции.

**Lessons Learned:**
- Bootstrap/config emissions могут ломать уже установленный `Ready`, если collector не учитывает running/device available.
- Пользовательский выбор локации нужно защищать от последующих store emissions, иначе UI может мигать назад на store token.
- Зеленые unit-тесты сами по себе не означают готовность: реальный blocker оказался именно в coverage gate.
- Для ускорения покрытия полезно брать runtime-классы с большим количеством дешевых веток, а не только source sentinels.

**Action Items:**
- Запушить текущую рабочую точку на `dev`, чтобы запустить CI.
- Продолжить добор coverage по top missed classes из `jacocoTestReport.xml`.
- После нового CI-прогона собрать свежие логи падений и продолжить фиксы.

### Session (16:14)

**Context:** Работали над ускоренным добором unit/runtime-тестов для повышения `app` coverage, при этом отложили `lint` до самого конца перед пушем.

**Key Exchanges:**
- Уточнили, что локальный `jacoco` рост за час слишком медленный, и нужно брать сразу несколько крупных непокрытых зон, а не по одному экрану.
- Пользователь отдельно зафиксировал: `lint` запускать только один раз, перед пушем, когда все тесты уже добавлены.
- Был выбран более агрессивный coverage-подход: крупные ViewModel/service-классы добираются пачкой на основе свежего `jacocoTestReport.xml`.

**Decisions Made:**
- `lint` откладывается до финального этапа перед push, чтобы не тормозить цикл добавления тестов.
- Фокус смещён с точечных правок на пакетное покрытие самых больших пробелов в coverage.
- Для следующего шага решено сначала снять сигнатуры и существующие fakes, затем внести один большой patch с несколькими тестовыми классами.

**Lessons Learned:**
- По `app` coverage одиночные тестовые добавления дают слишком слабый эффект; нужен batch-режим по нескольким крупным целям сразу.
- Для тестов в этом проекте часто приходится учитывать особенности stub-окружения Android и API несовместимости Robolectric.
- При росте покрытия важно не застревать на линте раньше времени, если цель сейчас именно coverage.

**Action Items:**
- Снять сигнатуры и existing fakes для крупных targets: `SingboxEngineSettingsViewModel`, `RuntimeConfigRestartCoordinator`, `UrnetworkLocationsViewModel`, `BootDiagnostics/LogcatReader`, self-update хвосты.
- Подготовить один большой patch с несколькими runtime/unit-тестами сразу.
- После набора тестов прогнать весь новый пакет одним заходом и только потом возвращаться к `lint`.

### Session (17:05)

**Context:** Работали над добиванием покрытия тестами и закрытием CI-гейтов по `app`, с упором на крупные пробелы в JaCoCo и проблемный `RuntimeConfigRestartCoordinator`.

**Key Exchanges:**
- Обсуждали, что агентный targeted-подход реально помог: coverage по `app` вырос примерно с `0.69` до `0.77` по lines, branch coverage до `0.63`, но до порога `95%` это всё ещё далеко.
- Сообщалось, что у `RuntimeConfigRestartCoordinator` тесты проходят, но JaCoCo показывал `0 covered`; причиной подозревали Robolectric sandbox/instrumentation, поэтому был добавлен pure JVM MockK-вариант тестов.
- После сбоев sub-agent’ов с `403 Forbidden` было решено продолжать без агентов, inline, крупными пачками, а не по одному тесту.
- Пользователь отдельно попросил: не использовать агентов, быстрее закрывать все job’ы, затем прогнать lint, пофиксить проблемы, сделать commit/push и продолжить работу.

**Decisions Made:**
- Перешли от агентного режима к полностью ручной работе, потому что новые агентные запуски падали с `403`, а пользы от них уже не было.
- Сфокусировались на top gaps из JaCoCo, а не на случайных файлах, чтобы добивать покрытие по самым дорогим классам.
- Для `RuntimeConfigRestartCoordinator` выбрали путь с pure JVM тестами вместо дальнейшей ставки на Robolectric, чтобы coverage точно засчитывался.
- После добавления новой тестовой пачки решили сначала проверить compile/targeted tests, а уже потом идти в lint и фиксы.

**Lessons Learned:**
- JaCoCo может показывать `0 covered` даже при зелёных тестах, если класс выполняется в неподходящем окружении, поэтому нужно проверять не только pass/fail, но и факт зачёта coverage.
- Агентный подход полезен для быстрого расширения покрытия по разным зонам, но при проблемах с инфраструктурой лучше сразу перейти в ручной режим.
- Для роста coverage эффективнее работать крупными пачками по top missed classes, чем распыляться на точечные тесты.
- На текущем шаге основными зонами риска остаются `DefaultAppListProvider`, `StrategyScanService`, `AssetStrategyAssetSource`, `AppLogger`, `LogsViewModel`, `BootDiagnostics`, `SilentPackageInstaller`, `SingboxServiceProfileProbe`.

**Action Items:**
- Добить оставшиеся крупные пробелы покрытия в `app` крупными пачками тестов.
- Разобраться, почему часть тестов `RuntimeConfigRestartCoordinator` не учитывалась JaCoCo, и оставить только тот вариант, который реально даёт coverage.
- Прогнать локальный lint после текущей волны изменений.
- Исправить найденные lint-ошибки.
- Сделать commit и push после завершения текущей пачки.
- Продолжить работу по остальным обязательным job’ам после push.

### Session (17:38)

**Context:** Увеличение coverage в `app` за счет добавления дешевых тестов для `SingboxProbeService` и `SingboxEngineSettingsViewModel`, с постоянной проверкой полного `app`-прогона и Jacoco.

**Key Exchanges:**
- Добавили 4 теста в `SingboxProbeServiceTest` на `empty input`, `all failed`, `fastest selection + bean write`, `runtime-skip callbacks`.
- Полный `:app:testDebugUnitTest` сначала выявил 1 нестабильный failure в новом тесте, затем его убрали, оставив надежную проверку выбора fastest и записи bean.
- В `SingboxEngineSettingsViewModelCoverageTest` расширили coverage на `restore success/error`, `import-from-file success/error`, `ping empty/non-empty`, `auto-select empty/non-empty`.
- Для `ViewModel` пришлось переработать harness: мок `assets`, `groupSeeder`, `probe service`, `coVerify/coEvery`, валидный VLESS blob helper, `RecordingProfileProbe`.
- Несколько brittle assertions были удалены, когда выяснилось, что они завязаны на реализацию `Dispatchers.IO` и реальный `SingboxProbeService`, из-за чего тест становился нестабильным.
- Пользователь потребовал короткий ответ в формате “сколько было и сколько стало”, без лишних деталей.

**Decisions Made:**
- Оставлять только те asserts, которые надежно проверяют behavior и дают стабильный полный app test.
- Не тратить время на brittle probe-path внутри `ViewModel`-теста, если этот путь уже покрыт отдельным `SingboxProbeServiceTest`.
- Использовать полный `:app:testDebugUnitTest` + `:app:jacocoTestReport` как источник истины, а не targeted-прогоны.
- Продолжать добирать coverage по самым дешевым branch-веткам, но не идти дальше вслепую, если прирост не подтверждается в общем Jacoco.

**Lessons Learned:**
- Targeted green не гарантирует полезный прирост coverage; нужен полный app Jacoco.
- Для `runTest`-сценариев с реальным `Dispatchers.IO` и runtime-dependent probing легко получить нестабильность.
- Невалидный test blob может дать ложноположительное прохождение UI-state, не доходя до нужной branch.
- В Android/Robolectric-heavy коде часть логики может плохо засчитываться Jacoco, даже если тесты зеленые.

**Action Items:**
- Снять точные `lines/branches` “было -> стало” из свежего `jacocoTestReport.xml`.
- Сравнить реальный прирост coverage после последней пачки.
- Если прирост окажется слабым, не продолжать писать десятки Android-heavy тестов вслепую, а пересмотреть Jacoco/sourceSets/excludes или вынести логику в JVM-coverable helpers.

### Session (18:02)

**Context:** Завершали пакет работ по тестам, style-gate и публикации изменений в `dev` перед закрытием сессии.

**Key Exchanges:**
- Был найден и исправлен красный `DefaultAppListProviderRuntimeTest`: в Robolectric проверка на `null` после удаления пакета оказалась неверной из-за shadow cache, поэтому тест перевели на проверку контракта инвалидции кэша и перечитывания списка после broadcast.
- После этого targeted-набор прошёл зелёным, затем запускался полный app coverage, но пользователь остановил акцент на покрытии и попросил завершить сессию, прогнать линтер, пофиксить баги и запушить.
- CI style gate падал на `:app:detekt` из-за unused imports в старом `ApkDownloaderTest.kt` и новом `SingboxEngineSettingsViewModelCoverageTest.kt`; затем после исправления detekt упёрся в ktlint formatting issues.
- Были исправлены auto-format проблемы и вручную разбиты длинные строки в тестах и в `engine-urnetwork` (`DataStoreUrnetworkConfigStore.kt`), после чего `ktlintCheck detekt` стал зелёным.
- Перед коммитом проверяли staged diff, исключили `singbox-core/libs/libbox.jar`, проверили `.memory` на приватные пути и убедились в отсутствии AI-подписей.

**Decisions Made:**
- Не коммитить и не пушить `singbox-core/libs/libbox.jar`, оставить его untracked.
- Исправлять только реально найденные style issues, без расширения изменения на unrelated код.
- Закоммитить рабочий пакет вместе с `.memory`, затем пушить в `dev`, а не трогать `main`.

**Lessons Learned:**
- В Robolectric shadow package manager может сохранять старые артефакты, поэтому тесты должны проверять контракт кэша, а не буквальное удаление пакета.
- `detekt` и `ktlint` в CI нужно прогонять как отдельные gates: после исправления одного может всплыть второй.
- Для публичного репозитория важно отдельно проверять staged diff на приватные пути, AI-подписи и случайные бинарники.

**Action Items:**
- Сохранить факт успешного коммита и push: `62e91bcc TEST: Расширение покрытия app`.
- Зафиксировать, что в рабочем дереве остался только untracked `singbox-core/libs/libbox.jar`, который не включался в commit/push.

