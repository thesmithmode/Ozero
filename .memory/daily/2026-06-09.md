# Daily Log: 2026-06-09

## Sessions

## Memory Maintenance

### Session (11:09)

**Context:** Работа над Ozero: фикс P2 для Singbox selected profile fallback и массовое поднятие покрытия тестами по падающим CI-сегментам.

**Key Exchanges:**
- Пользователь уточнил активную цель: исправить P2 “Badge Resolve selected Singbox rows synchronously before fallback”, затем по последнему GitHub CI добить покрытие падающих сегментов до 95%+, без push до уверенности.
- Assistant подтвердил P2: fingerprint уже не падает к `BEAN_KEY`, но runtime config provider всё ещё зависел от flow-кэша и мог использовать stale DataStore blob до эмита Room.
- Пользователь явно разрешил локальные тестовые запуски, несмотря на проектное правило Ozero “локальные Gradle/test/lint запрещены”, чтобы ускорить работу.

**Decisions Made:**
- P2 фиксировать на owning layer `SingboxModule`: при miss выбранного профиля в `getAllFlow()` синхронно читать `profileDao.getById(selectedProfileId)` перед fingerprint/fallback; stale `BEAN_KEY` для selected id не использовать, missing row fail-closed fingerprint `0`.
- Не поднимать coverage фиктивными тестами runtime-heavy классов (`RealUrnetworkSdkBridge`, `SingboxEngine`, `EngineWarp`, `OzeroVpnService`), а добавлять targeted tests по reachable deterministic веткам.
- После разрешения пользователя локальные Gradle/test запуски допустимы для этой задачи и имеют приоритет над прежним локальным запретом.

**Lessons Learned:**
- Старый CI failure был не “несколько missed branches”: локальный app gate показал примерно line 0.81, branch 0.65; CI artifacts также показывали крупные провалы: `engine-urnetwork` около 23% branch/31% line, `engine-singbox` 42%/53%, `common-vpn` 53%/58%, `engine-fptn` 57%/51%.
- Для coverage evidence полезнее скачивать JaCoCo artifacts и читать XML counters/classes, чем полагаться на обрезанные raw logs.
- Ближайшие к быстрому улучшению deterministic modules: `common-dns`, `singbox-subscription`, `singbox-config`, `singbox-fmt`, `engines-core`.

**Action Items:**
- Исправить две регрессии после добавления тестов: compile error в `singbox-config` test и failing test в `engines-core`.
- Повторить coverage batch по `common-dns`, `singbox-fmt`, `singbox-config`, `singbox-subscription`, `engines-core`.
- После повторного gate перечитать свежие missed counters и продолжить добивать покрытие по фактическим hotspots.

### Session (11:35)

**Context:** Работа над Ozero: исправление P2 Singbox runtime fingerprint bug и массовое добивание test/coverage gaps до 95%.

**Key Exchanges:**
- Пользователь резко указал, что слишком много времени уходит на пустые прогоны тестов, и потребовал писать больше тестов пакетно для достижения 95% coverage.
- Ассистент изменил тактику: меньше широких Gradle-прогонов, больше пакетного написания тестов по Jacoco XML и targeted запусков.
- Пользователь запросил промежуточные итоги после примерно часа работы, оценку темпа и меры ускорения.

**Decisions Made:**
- Для Singbox runtime fingerprint выбран root fix: при cache miss выбранного профиля синхронно читать `profileDao.getById()`, не использовать stale `BEAN_KEY` как fallback.
- Для ускорения coverage recovery принято писать по 5-10 тестов за заход в одном модуле и запускать только конкретные test class через `--tests`; полный module test запускать только после стабилизации пачки.
- Приоритет coverage изменён: сначала чинить текущие failing app tests, затем добивать near-threshold modules (`common-dns`, `singbox-config`, `singbox-fmt`, `singbox-subscription`, `engines-core`), затем крупные модули по high-value classes из Jacoco.
- Для больших Android-service классов решено избегать хрупких source-sentinel tests, если можно тестировать чистую логику или existing coordinator harness.

**Lessons Learned:**
- `Build.MANUFACTURER`/`Build.MODEL` в Robolectric/runtime могут быть `null` несмотря на non-null сигнатуру; `RomCompat` должен использовать `.orEmpty()`.
- Kotlin не поддерживает `\f` в обычной строке; для таких ожиданий нужен raw literal или другой безопасный формат.
- Некоторые existing tests могут не попадать в Jacoco coverage из-за build/report mapping issue, поэтому для быстрого роста покрытия лучше выбирать классы, где Jacoco точно считает execution.
- Brittle sentinel tests на точную строку с indent ломаются от форматирования; лучше использовать структурный anchor внутри нужной функции.
- Существующие файлы с mojibake мешают patch-контексту; для новых тестов иногда быстрее создать отдельный ASCII-only файл.

**Action Items:**
- Починить 2 текущих failing app tests.
- Перепроверить coverage после стабилизации near-threshold modules.
- Продолжить high-value coverage работу по `StartSequenceCoordinator`, `TunBuilderHelper`, `EngineWatchdogCoordinator`, `Base58/auth`, `SingboxEngineSettingsViewModel`, `UrnetworkLocationsViewModel`.
- Не пушить текущий статус: тесты/coverage ещё красные и target 95% не доказан.

### Session (11:58)

**Context:** Ускорение coverage-цикла в Ozero: стабилизация flaky app tests и добивка near-threshold Gradle-модулей до 95%+ через targeted test runs.

**Key Exchanges:**
- Пользователь зафиксировал новый рабочий цикл: читать Jacoco/test XML, писать пачки по 5-10 тестов в одном модуле, запускать только конкретный test class через `--tests`, полный module test только после стабилизации.
- Приоритет изменён: сначала 2 failing app tests, затем near-threshold modules (`common-dns`, `singbox-config`, `singbox-fmt`, `singbox-subscription`, `engines-core`), потом high-value classes в крупных модулях.
- Assistant удалил flaky сценарии в `SingboxEngineSettingsViewModelCoverageTest`, потому что они зависели от coroutine timing/background callbacks и ломали targeted run.
- Для `singbox-fmt` добавлен пакет branch tests в `FmtAdditionalBranchCoverageTest`: IPv6 authority fallback, query pairs without `=`, truthy security aliases, base64 MIME/padding/blank ветки, displayName/default initializers.
- Targeted test classes по `singbox-fmt`, `singbox-config`, `singbox-subscription`, `engines-core`, `common-dns` прошли зелёно.

**Decisions Made:**
- Не стабилизировать timing-sensitive ViewModel tests через ожидание transient flags; удалить flaky тесты и при необходимости позже покрыть deterministic probe-service test.
- Не писать хрупкие source-sentinel tests для больших Android-service классов, если можно вынести чистую логику или использовать существующий coordinator harness.
- `common-dns` пока не трогать глубоко, потому что XML уже показывал 95% branch и 100% line.

**Lessons Learned:**
- `advanceUntilIdle` не гарантирует завершение работы через `Dispatchers.IO`; такие тесты могут оставлять фоновые callbacks после `resetMain()`.
- В targeted Kotlin/JUnit окружении list assertions могут быть хрупкими из-за разных list реализаций; надёжнее проверять `size` и конкретные значения.
- `UriCompat` fallback для IPv6 сохраняет host с квадратными скобками.
- Base64 decoder в `singbox-fmt` permissive: некоторые invalid-looking строки могут декодироваться в мусорную UTF-8 строку через fallback, поэтому invalid assertion нельзя строить на произвольных символах.

**Action Items:**
- Сделать checkpoint coverage verification только для near-threshold modules, чтобы понять, какие ещё нужно добивать.
- При необходимости добавить отдельный deterministic probe-service test вместо удалённого flaky auto-select ViewModel сценария.

### Session (12:45)

**Context:** Продолжалось повышение тестового покрытия Ozero на ветке `dev`, с фокусом на `common-vpn` и `engine-urnetwork`.

**Key Exchanges:**
- Пользователь разрешил пушить промежуточные результаты, не дожидаясь CI, чтобы самостоятельно смотреть статус тестов.
- Был выполнен промежуточный коммит `b34cb026` и push в `dev`; `main` не трогался.
- Untracked `singbox-core/libs/libbox.jar` сознательно не включался в коммит.

**Decisions Made:**
- Для `common-vpn` выбраны behavior-тесты watchdog вместо source-sentinel тестов, потому что они реально покрывают ветки и менее хрупкие.
- Для дальнейшего прироста покрытия выбраны deterministic/pure зоны без Android service/native зависимости: `TunInterfaceStats`, `UrnetworkConfigStore`, `UrnetworkApiHelper`, `DataStoreUrnetworkConfigStore`, auth identity.
- При проблемах с mojibake в существующем тестовом файле решено создать отдельный ASCII-only тестовый файл для дополнительных `TunInterfaceStats` веток.

**Lessons Learned:**
- `TunnelStats` требует обязательные поля `txPackets` и `rxPackets`; неполный construction ломает compile.
- Свежие coverage XML показали, что `common-vpn` и `engine-urnetwork` остаются исторически низкими, поэтому точечные 1-2 теста проблему не закроют.
- `Base58BranchCoverageTest`, новые `EngineWatchdogInfiniteRetryTest` behavior-тесты и `TunInterfaceStatsAdditionalCoverageTest` прошли targeted-прогоны.

**Action Items:**
- Дождаться результата targeted `UrnetworkConfigStoreTest`.
- Продолжить добивать deterministic branches в `engine-urnetwork` и `common-vpn`.
- Не забыть, что `.memory` должна идти вместе с ближайшим рабочим коммитом, а `singbox-core/libs/libbox.jar` остаётся untracked до отдельного решения.

### Session (13:34)

**Context:** Дочинка красного GitHub CI в Ozero после падения `ktlint + detekt`, плюс подготовка дополнительного test coverage для `engine-urnetwork`.

**Key Exchanges:**
- Пользователь указал, что GitHub CI всё ещё падает на `ktlint + detekt`, несмотря на предыдущие локальные исправления.
- Был взят свежий GitHub CI-сигнал: `dev` на `b806c514`, push run `27199320874`, PR run `27199322820`; остальные jobs были skipped из-за одного root job `ktlint + detekt`.
- Локально воспроизведена CI-style команда `./gradlew ktlintCheck detekt --stacktrace --continue`, чтобы получить точные причины вместо шумного GitHub log.
- После срочного требования пользователя был подготовлен push текущего исправленного пакета, при этом `singbox-core/libs/libbox.jar` оставлен untracked и не добавлялся.

**Decisions Made:**
- Для `detekt LargeClass` в `singbox-subscription` решено не снижать threshold и не удалять coverage, а разделить `SubscriptionParserBranchCoverageTest` на дополнительный test class.
- Два крупных YAML edge-теста перенесены в новый `SubscriptionParserYamlEdgeCoverageTest`, чтобы сохранить покрытие и пройти лимит `LargeClass` 600 строк.
- `ktlint` blank line исправлялся точечно в `RawUpdaterTest.kt` и затем в конце старого parser test class после удаления блока.
- Новые `engine-urnetwork` pure tests по `WindowType`, identity/Base58 не связывались с причиной GitHub падения, но планировались к включению в следующий commit при зелёном style.

**Lessons Learned:**
- GitHub CI-сигнал важнее локального targeted lint: если CI красный, нужно брать свежий failing run/job logs или воспроизводить ровно CI-style задачу.
- `gh` log может скрывать полезный report в шумном выводе; локальный запуск `ktlintCheck detekt --stacktrace --continue` и чтение report-файлов быстрее дают file:line.
- При добавлении coverage tests можно легко превысить `detekt LargeClass`; безопасный фикс — дробить test classes, а не ослаблять правило.
- После удаления большого блока из Kotlin test class ktlint может падать на лишнюю blank line у конца класса.

**Action Items:**
- Запушить исправленный пакет с split test class и ktlint fixes.
- После push мониторить новый GitHub CI до финального статуса и чинить новые падения по факту.

### Session (13:45)

Context: Чинили падение lint в `dev` и P1-регрессию восстановления persisted URnetwork config после recreation процесса.

Key Exchanges:
- Пользователь отменил push после lint-fix и добавил P1: `config()` в URnetwork после рестарта отдавал дефолтный in-memory `UrnetworkConfig()` вместо persisted DataStore state.
- Локальные Gradle/test/lint проверки не запускались из-за правила проекта; валидация ограничена diff/whitespace/staged sanity-check и должна завершаться через CI после push.

Decisions Made:
- Push не выполнять по прямой команде пользователя.
- Корневой фикс URnetwork: `DataStoreUrnetworkConfigStore.config()` должен читать `dataStore.data -> readConfig(...)`, а не `latest.asStateFlow()`, чтобы production callers вроде `EngineUrnetwork.start()` получали persisted credentials/location/performance settings сразу после recreation.
- Regression-test добавлен на сценарий нового `DataStoreUrnetworkConfigStore`, который вызывает `config().first()` без предварительного `update`.

Lessons Learned:
- В URnetwork persisted preferences ранее загружались только через `update()`, поэтому после process recreation `config().first()` мог вернуть defaults и спровоцировать новый guest/device JWT или default location/settings.
- Для ktlint в `singbox-config` multiline `apply { ... }` внутри аргументов builder-вызовов рискован; стабильнее выносить bean в `val`, затем передавать в builder.

Action Items:
- После разрешения пользователя запушить локальный коммит `a92fc6c3` в `dev`; CI проверит пользователь.
- Не трогать чужой untracked файл `singbox-core/libs/libbox.jar`.

### Session (14:09)

**Context:** Работа над Ozero: пауза coverage-задачи ради фикса P1/P2 и затем смена цели на доведение dev CI до зеленого без промежуточных push.

**Key Exchanges:**
- Пользователь сообщил P1: FPTN после recreation может читать дефолтный `FptnConfig()` из холодного cache и игнорировать сохраненные prefs при immediate start/sticky VPN service.
- Пользователь сообщил P2: в `.memory` была утечка прямого GitHub owner/repo вида `owner/repo#80`; требовалась санитизация.
- После фикса P1/P2 пользователь указал, что dev CI массово красный по многим jobs, значит coverage-добавления бесполезны, пока не устранен общий CI/root cause.
- Цель обновлена: довести dev CI до green; не пушить, пока нет уверенности, что все исправлено и coverage 95%+; сначала анализировать CI artifacts/XML и чинить пакетно.

**Decisions Made:**
- P1 исправлять на owning layer `DataStoreFptnConfigStore`: `currentConfig()` при холодном кеше должен один раз синхронно подтянуть первый snapshot из DataStore и обновить `latest`, чтобы `FptnEngine.buildManualConfig()` видел persisted token/server до подписчиков `config()`.
- Для P1 добавить regression test: записать FPTN prefs напрямую и вызвать `currentConfig()` до `config().first()`.
- P2 санитизировать не только указанную новую строку, а все найденные прямые `owner/repo` и GitHub URLs в `.memory`, заменив на нейтральные `PR #...`/`CI run`.
- Локальные Gradle/test/lint проверки не запускать из-за правил репо; валидация через CI artifacts/logs.
- Не делать промежуточные push; сначала собрать полный список failing tests/Jacoco violations, исправить пакетно, затем push только когда есть уверенность.

**Lessons Learned:**
- Массовое падение всех CI test jobs означает, что надо искать общий compile/config/test-infra blocker или coverage gate, а не продолжать слепо добавлять coverage.
- `ktlint + detekt` был зеленым, значит текущий массовый CI red не из-за форматирования.
- `buildSrc` имел конкретный failing test: `LockFileParserTest > parse lock with non list artifacts as empty list()` из-за `LockFileException`.
- CI test-report artifacts могут содержать HTML без `TEST-*.xml`; в таком случае failures нужно извлекать из HTML summaries/class pages и Gradle logs, а coverage из Jacoco XML/log violations.
- HTML grep по `failures` шумный, потому что Gradle class pages содержат такие блоки даже при 0 failures; надежнее смотреть summary/successRate.

**Action Items:**
- Продолжить парсинг скачанных CI artifacts: найти class pages с successRate <100%, Jacoco rule violations и реальные failing tests.
- Сначала починить текущие failing app tests, затем near-threshold modules: `common-dns`, `singbox-config`, `singbox-fmt`, `singbox-subscription`, `engines-core`.
- После этого добивать high-value classes: `StartSequenceCoordinator`, `TunBuilderHelper`, `EngineWatchdogCoordinator`, Base58/auth, `SingboxEngineSettingsViewModel`, `UrnetworkLocationsViewModel`.
- Избегать хрупких source-sentinel tests для больших Android-service классов, если можно вынести чистую логику или использовать existing coordinator harness.

### Memory Flush (14:32)

FLUSH_OK - Nothing worth saving from this session

### Session (17:12)

**Context:** Пользователь добивает `dev` CI до зеленого состояния и требует поднять coverage всех модулей до 95%+ без лишних широких Gradle прогонов и без push до уверенного полного фикса.

**Key Exchanges:**
- Пользователь несколько раз ускорил приоритет: сначала посмотреть текущие JaCoCo XML, найти модули ниже 95%, затем писать тесты пачками.
- Ассистент перестроил evidence map по последним JaCoCo XML и сообщил, что ниже 95% остаются `engine-fptn`, `engine-singbox`, `app`, `engine-warp`, `engine-byedpi`, `engine-masterdns`, `core-backup`.
- Ассистент выбрал `engine-singbox` как следующий high-payoff модуль из-за большого missed coverage в `SingboxEngine` и наличия reflection harness.
- Ассистент добавил пакет из 5 runtime-state тестов для `engine-singbox`: `attachTun` в chain mode, `attachTun` до start, `attachTun` без proxy очищает pending, `stop` обрабатывает remote exception, stats мапят `SingboxStats` в `EngineStats`.
- Следующим быстрым below-threshold модулем выбран `engine-masterdns`, где много branch miss в deploy mapping/parser helpers и их можно тестировать без сети через fake transport.

**Decisions Made:**
- Не пушить до уверенности, что все failing tests исправлены, coverage 95%+ и dev CI реально зеленый.
- Использовать цикл: читать JaCoCo/test XML, писать 5-10 тестов за заход в одном модуле, запускать конкретные test classes через `--tests`, полный module test только после стабилизации пачки.
- Не писать хрупкие source-sentinel tests для больших Android service classes, если можно вынести или покрыть чистую логику/coordinator harness.

**Lessons Learned:**
- Последний доступный coverage evidence указывает, что near-threshold modules почти закрыты, но крупные engine/app modules еще ниже 95%.
- `engine-singbox` уже имеет reflection harness, пригодный для реальных runtime branch tests без sentinel-подхода.

**Action Items:**
- Продолжить с `engine-masterdns`: написать пакет тестов для deploy mapping/parser helpers через fake transport.
- Затем снова читать JaCoCo XML и добивать оставшиеся below-threshold modules до 95%+.
- После стабилизации локальных targeted checks ждать/мониторить CI до финального статуса, не пушить раньше полной уверенности.

