# Daily Log: 2026-05-25

## Sessions

### Session: v0.2.1 install failure — root cause + fix

**Цель:** Найти причину "пакет недействителен" для v0.2.1 APK, починить, выпустить заново.

**Ложные гипотезы (отвергнуты):**
- V1 signing disabled → включили V1=true, ошибка осталась. apksigner verify показал V1=false для обоих APK — V1 signing был red herring
- R8 stripped manifest class → все classes в DEX (assertions добавлены, прошли). Не root cause
- INDEX.LIST от Objenesis/Kryo в APK → подозрительный артефакт, но не install blocker

**ROOT CAUSE: versionCode downgrade!**
- v0.2.0 APK: versionCode=1155, versionName="0.2.0"
- v0.2.1 APK: versionCode=618, versionName="0.1.11"
- Android отклоняет install с LOWER versionCode как "пакет недействителен"
- Причина регрессии: git history rewrite (удаление AI authorship via rebase+amend+force-push) уменьшило `git rev-list --count HEAD` с ~1155 до ~618
- versionName тоже регрессировал: `git describe --tags` подхватывает старый v0.1.11 тег (v0.2.0 тег не reachable после history rewrite)

**Фикс (9866f5db):**
- versionCode = max(commitCount + 1500, 2000) — offset + floor, безопасно выше v0.2.0 (1155)
- versionName = env OZERO_VERSION_NAME (из release.yml tag resolver), fallback git describe
- CI assertion: versionCode >= 2000 вместо > 1

**Также применены (588046f9, ранее):**
- enableV1Signing = true (не root cause, но совместимость)
- DEX assertions для всех manifest-классов (не root cause, но важный guard)

## Concepts

### versionCode regression after git history rewrite

`git rev-list --count HEAD` зависит от количества коммитов. History rewrite (rebase, amend, filter-branch, force-push) может УМЕНЬШИТЬ commit count. Это ломает versionCode монотонность → Android отклоняет install как downgrade.

Защита: versionCode = max(commitCount + OFFSET, FLOOR). FLOOR = последний известный versionCode + запас. CI assertion с жёстким порогом.
### Memory Flush (01:37)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (01:38)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (01:38)

**Context:** Fixing versionCode regression (618 < 1155) and versionName mismatch in Ozero release v0.2.1 APK; CI was pushed and user wants green CI → re-release v0.2.1.

**Key Exchanges:**
- Root cause: git history rewrite (AI authorship removal) reduced commit count → versionCode regressed from 1155 to 618; git describe picked old tag → versionName = "0.1.11" instead of "0.2.1"
- Fix applied: versionCode offset=1500 → dev=1210+1500>2000, safe above v0.2.0 (1155); added OZERO_VERSION_NAME env; strengthened assertion to >=2000

**Decisions Made:**
- Use fixed offset 1500 for versionCode so count > 2000 (above v0.2.0's 1155) — safe regardless of future history rewrites
- Delete bad v0.2.1 tag; re-release after CI green with corrected build

**Lessons Learned:**
- git history rewrite (rebase+amend+force-push for author cleanup) reduces commit count → versionCode regression if generated from `git rev-list --count`
- After any history rewrite: verify versionCode stays monotonically increasing before tagging release
- `apksigner verify` V1=false despite CERT.RSA present = corrupted signing (red herring in this case — real issue was versionCode)
- Add CI gate: assert versionCode >= previous_release_versionCode before APK assembly

**Action Items:**
- ~~Wait for CI green on dev after versionCode fix commit~~ → CI failed ktlint (build.gradle.kts lines 11/14 multi-line grouping parens)
- Fix applied (2cab7b29): вынести gitCommitCount в отдельную переменную → maxOf(gitCommitCount + offset, floor) single-line
- Re-tag v0.2.1 и trigger release после CI green

### Session (продолжение — ktlint fix)

**Проблема:** CI упал на :app:ktlintKotlinScriptCheck
- `app/build.gradle.kts:11:6 Missing newline after "("`
- `app/build.gradle.kts:14:59 Missing newline before ")"`
- Причина: multi-line grouping `(providers.exec { ... }.get()) + offset` внутри `maxOf()`

**Фикс (2cab7b29):**
- Вынести exec-блок в `val gitCommitCount: Int = providers.exec { ... }.get().toIntOrNull() ?: 1`
- Итоговая строка: `val gitVersionCode: Int = maxOf(gitCommitCount + versionCodeOffset, versionCodeFloor)`

