# Daily Log: 2026-05-09

## Sessions

## Memory Maintenance

### Memory Flush (09:27)

FLUSH_OK - Nothing worth saving from this session

### Session (09:37)

**Context:** Диагностика и фикс регрессии URnetwork в v0.0.7

**Key Exchanges:**
- Юзер: изучить diff между релизами v0.0.6 и v0.0.7, найти корень регрессии URnetwork
- Найдена регрессия: коммит `65e5b13` добавил `excludeSelf = false` в `establishTunForEngine`
- Предложено убрать параметр `excludeSelf` из API совсем — нет use-case для `false` ни в одном движке

**Decisions Made:**
- `excludeSelf = false` → регрессия: `Sdk.newDeviceLocalWithDefaults` не принимает socket protector callback → Go SDK не может protect свои сокеты → routing loop → URnetwork не коннектится
- Фикс: убрать `excludeSelf = false`, вернуть дефолт `true`
- Sentinel-тест закрепляет запрет на `excludeSelf = false`
- Тег `v0.0.7` пересоздан на фиксе
- Следующий шаг: убрать параметр `excludeSelf` из `TunBuilderConfigurator.apply` совсем — hardcode `true`, нет footgun

**Lessons Learned:**
- `excludeSelf = false` смертельно для движков без SDK-level socket protection (URnetwork Go SDK). Единственный способ дать SDK интернет — исключить app-пакет из своего TUN
- Параметр в API без валидного use-case = footgun. Лучше hardcode + нет параметра

**Action Items:**
- Рефакторинг `TunBuilderConfigurator.apply`: убрать параметр `excludeSelf` целиком, hardcode `true`; обновить sentinel-тест

### Memory Flush (10:34)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (10:52)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (11:29)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (11:41)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (11:59)

FLUSH_OK - Nothing worth saving from this session

### Session (12:27)

**Context:** CI maintenance + dependabot PR triage на Ozero dev

**Key Exchanges:**
- Maven Central вернул 403 на kotlin-stdlib-1.9.23 (transitive от gradle 8.9 kotlin-dsl). Transient infra, не регрессия
- 10 dependabot PRs проанализированы, категоризированы по риску
- Юзер подтвердил: спорные PRs не трогать ("не просто так использовали не самые новые версии")

**Decisions Made:**
- 4 safe PRs влиты в dev: appcompat 1.7.1 (#26), espresso 3.7.0 (#27), checkout v6 (#24), hadolint 3.3.0 (#23)
- 6 спорных PRs оставлены: kotlin 2.3 (#25), AGP 9.2 (#28), ktlint 14 (#29), upload-artifact v7 (#22), gh-release v3 (#21), cache v5 (#20) — юзер осознанно держит старые версии
- PRs target main, но применены на dev (наш flow dev→main)

**Lessons Learned:**
- `ByeDpiEngineTest > startSuccessWhenSocksPortReady()` — flaky. Был зелёным в предыдущих runs (25596700209, 25596517936), упал в новом. Нужен fix или stability improvement
- Dependabot PRs все target main — нужно вручную cherry-pick на dev
- Kotlin 2.3 bump высокий риск: K2 ломает ktlint baselines, требует синхронного compose-compiler bump, runTest+IO регрессии (из памяти feedback_dependency_bumps + feedback_runtest_while_true_init)

**Action Items:**
- Тег v0.0.8 переставить на HEAD dev когда CI зелёный
- Investigate flaky `ByeDpiEngineTest.startSuccessWhenSocksPortReady` — стабилизировать или пометить
- Rerun CI ещё в процессе на момент прерывания

### Session (12:32)

**Context:** Релиз v0.0.8 — фикс CI, мердж dependabot обновлений, тег, fast-forward main.

**Key Exchanges:**
- Юзер попросил релиз v0.0.8, потом отменил — CI красный, сначала починить
- Юзер предупредил про регрессии от прошлых фиксов
- Юзер попросил обзор dependabot веток → какие безопасны, какие спорны
- Юзер явно запросил merge dev→main **без squash** (все коммиты в main как есть)

**Decisions Made:**
- Maven Central 403 на kotlin-stdlib-1.9.23 = transient infra, не регрессия → cancel + повторный push
- 4 dependabot PR безопасны (#23 hadolint, #24 checkout v6, #26 appcompat 1.7.1, #27 espresso 3.7.0) → влиты в dev
- 6 PR спорные (#20 cache v5, #21 gh-release v3, #22 upload-artifact v7, #25 kotlin 2.3, #28 AGP 9.2, #29 ktlint 14) → не тронуты, обсудить отдельно
- ByeDpiEngineTest.startSuccessWhenSocksPortReady = flaky (green в предыдущих runs) → rerun, не фикс
- dev→main через fast-forward, не squash (по явной команде юзера — исключение из обычного workflow)

**Lessons Learned:**
- `actions/checkout@v4→v6`, `hadolint@v3.1.0→v3.3.0`, `appcompat 1.7.0→1.7.1`, `espresso 3.6.1→3.7.0` — безопасные минорные бампы, не ломают
- kotlin 2.3, AGP 9.2, ktlint 14 — потенциально breaking, требуют отдельного анализа
- ByeDpiEngineTest race condition между acceptSocks5InBackground и Socks5HandshakeProbe — intermittent, может потребовать фикс синхронизации позже
- Dependabot PR на target main бесполезны при dev-only workflow → применять патчи локально на dev

**Action Items:**
- Обсудить 6 спорных dependabot PR: cache v5, gh-release v3, upload-artifact v7, kotlin 2.3, AGP 9.2, ktlint 14
- Возможно стабилизировать ByeDpiEngineTest (race condition в acceptSocks5InBackground)

### Session (13:12)

**Context:** Debugging 4 production bugs in Ozero v0.0.8 release + CodeQL CI failure on main branch

**Key Exchanges:**
- User reported CodeQL failing on main (4 days running, schedule trigger). Root: `codeql.yml` missing URnetwork AAR download step that `ci.yml` has → `com.bringyour.sdk.*` unresolved
- User reported v0.0.8 broken: IP not showing, URnetwork engine broken, split tunnel empty on entry, split tunnel showing all apps when mode=ALL
- Log analysis (ozero.log) revealed engine-switch chain = root for 3/4 bugs: 7 startVpn calls in 30s, GoRuntimeGuard blocks WARP→URNETWORK switch, IP warmup never completes during rapid switching
- OzeroVpnService.kt touched 6+ times in 2 days = walking in circles around Go runtime conflict without fixing root
- 100% test coverage didn't catch any of these — unit tests on fakes miss behavioral/integration scenarios

**Decisions Made:**
- CodeQL fix: add AAR download step to `codeql.yml` matching `ci.yml` pattern
- Split tunnel: initial state should be `Loading` not `Content(apps=[])` to avoid empty flash
- Split tunnel mode=ALL: hide app list entirely (user preference over Karing's always-show design)
- Advisor called before substantive work — recommended treating engine-switch chain as single root cause
- Approach: sentinel tests first, then fixes (not symptom-patching)

**Lessons Learned:**
- CodeQL schedule runs on main can silently fail for days — push to dev doesn't trigger it, nobody notices
- `GoRuntimeGuard` was added as symptom-patch (633304f) but never solved the root: two Go runtimes (libam-go + libgojni) can't coexist without eager load in OzeroApp.onCreate
- Line/branch coverage ≠ behavioral coverage: FakeIpInfoProvider passes but real engine-switch race never tested
- IP warmup (8s delay) gets cancelled on every engine restart — rapid switching means fetch never fires
- Memory feedback added: `feedback_codeql_aar_dependency.md` for the silent CodeQL pattern

**Action Items:**
- S1: Split tunnel Loading state + controlled loadApps (was in progress when interrupted)
- S2: Split tunnel mode=ALL hides app list
- S3: IP fetch race during engine-switch chain
- S4: GoRuntimeGuard vs eager Go runtime load — resolve contradiction between guard logic and CLAUDE.md invariant
- Stress test: engine-switch chain (5 toggles in 30s)
- Integration test: WARP→URNETWORK without process kill
- All work stays on dev branch, no main touches without explicit command

### Memory Flush (13:36)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (13:46)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (14:22)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (14:51)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (14:51)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (15:56)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (16:19)

Проверку покрытия 95% выполняю сразу — не жду CI.

**Assistant:** 95% пороги настроены в `jacoco.gradle.kts`? Смотрю конфиг.

**Assistant:** Конфига `jacoco.gradle.kts` нет. Ищу где coverage конфигурирован.

**Assistant:** Coverage конфиг найден.

**Assistant:** Читаю coverage.gradle.kts.

**Assistant:** Пороги настроены: LINE/BRANCH/INSTRUCTION — все 0.95 (95%). Но нужно проверить что coverage реально считается и тесты не "пустые".

Лучший способ — посмотреть последний HTML-отчёт из CI артефактов или посмотреть на качество тестов вручную для критических модулей.

Проверю ByeDpiEngine тесты — самый свежий изменённый файл.

**Assistant:** Читаю `ByeDpiEngineTest`.

**Assistant:** Тест хороший — покрывает start success/failure/retry-logic/stats/stop. Но проверю: есть ли тест на `stop()` когда engine не запущен? Есть ли edge-cases на config? Смотрю дальше.

**Assistant:** Тесты достаточно полные для `ByeDpiEngine`. Но ключевой вопрос — насколько tight тесты для других критичных модулей. Смотрю `GoRuntimeGuard` — самый свежий добавленный инвариант.

**Assistant:** Читаю `GoRuntimeGuardTest`.

**Assistant:** `GoRuntimeGuardTest` — сильный. Покрывает acquire/release/double-acquire-block/concurrent-threads/reacquire-after-release. Это настоящие жёсткие тесты.

Смотрю `OzeroVpnService` — самый сложный модуль, 194 строки `runStartSequence`.

**Assistant:** Читаю `OzeroVpnServiceTest`.

**Assistant:** Тест `OzeroVpnService` — полный. Но хочу проверить `SplitTunnelViewModel` (недавно менялся).

**Assistant:** Читаю `SplitTunnelViewModelTest`.

**Assistant:** `SplitTunnelViewModelTest` имеет gap: нет теста на `handleIntent(SplitTunnelIntent.ToggleApp)` когда app уже `isBlocked=true` (reverse toggle). Нет теста на что происходит если `getInstalledApps()` бросает exception.

Добавляю missing тесты.

**Assistant:** Читаю `SplitTunnelViewModel` для понимания exception-пути.

**Assistant:** Добавляю 2 missing теста.

**Assistant:** Проверяю diff — всё ли правильно.

**Assistant:** CI для предыдущего push ещё крутится. Жду завершения перед новым коммитом (иначе запустится второй параллельный CI run зря).

**Assistant:** **Context:** Красный CI на Ozero — ktlint/detekt + флакующий тест ByeDpiEngine; аудит покрытия.

**Key Exchanges:**
- Юзер: почему за один CI run не видны все проблемы? — Ответ: `needs: [ktlint]` в pipeline → тест-jobs не стартуют при ktlint-фейле; разные runs = разные слои ошибок. `--continue` работает только внутри одного gradle run.
- Юзер: точно ли тесты 95% или "делают вид"? — Аудит GoRuntimeGuardTest/OzeroVpnServiceTest/SplitTunnelViewModelTest/ByeDpiEngineTest проведён.

**Decisions Made:**
- `acceptSocks5InBackground` в тест-хелпере убрать `repeat`-ограничение → сервер обслуживает все коннекты до закрытия сокета. Race condition в CI нагрузке: первый зонд (500ms timeout) отваливался, сервер выходил из loop, OS backlog принимал следующие коннекты без ответа → таймаут 5с → `StartResult.Failure`.

**Lessons Learned:**
- CI pipeline с `needs:` скрывает downstream-ошибки пока upstream красный → нельзя увидеть тест-провалы пока ktlint красный. Это структурное ограничение, не баг `--continue`.
- Mock-сервер с `repeat=N` в тестах = хрупкость под нагрузкой CI. Правило: mock-сервер должен обслуживать неограниченное число соединений до явного закрытия.

**Action Items:**
- `SplitTunnelViewModelTest` gap: нет теста на `ToggleApp` когда `isBlocked=true` (reverse toggle) + нет теста на exception из `getInstalledApps()`. Тесты подготовлены, ждут зелёного CI перед коммитом.

### Memory Flush (17:17)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (18:27)

FLUSH_OK - Nothing worth saving from this session

