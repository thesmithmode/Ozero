# Daily Log: 2026-06-06

## Sessions

## Memory Maintenance

### Memory Flush (10:40)

FLUSH_ERROR: TimeoutExpired: Command '['<codex-bin>/codex.exe', 'exec', '--ephemeral', '--skip-git-repo-check', '--ignore-rules', '-c', 'features.hooks=false', '-s', 'read-only', '-C', '<repo>/.memory', '-o', '<temp-output>.md', '-']' timed out after 900 seconds

### Session (10:48)

**Context:** Пользователь просил подтянуть логи GitHub, исправить красный линт и запушить все незапушенные изменения.

**Key Exchanges:**
- Было установлено, что в рабочем дереве нет кодовых изменений, а единственный незакоммиченный артефакт относится к `.memory`.
- Попытки получить свежие GitHub Actions логи через `gh` и GitHub API из этого окружения не удались из-за ограничений доступа/конфига.
- Было решено не править код вслепую без подтверждённого источника ошибки.

**Decisions Made:**
- Не запускать локальный lint и не вносить кодовые правки без CI-логов, чтобы не гадать по причине красного статуса.
- Оформить незакоммиченную запись `.memory/daily/2026-06-06.md` в коммит и запушить, чтобы очистить дерево и не оставлять память незакоммиченной.
- Работать только с тем, что можно подтвердить в текущем окружении.

**Lessons Learned:**
- В этом окружении `gh` может упираться в доступ к пользовательскому конфигу, а прямой доступ к GitHub API может быть недоступен.
- Если в дереве нет кодовых изменений, а есть только `.memory`, лучше сначала закрыть memory-артефакт, чем пытаться лечить несуществующий кодовый дефект.
- Без CI-логов не стоит делать предположения о причине красного линта.

**Action Items:**
- При наличии номера run или ссылки на failed job провести точечный разбор CI.
- Если потребуется, разобрать `ci.yml` и определить, где именно расположен lint-job.
- При необходимости помочь сформировать команды для получения точного lint-failure в другом окружении.

### Session (11:06)

**Context:** Пользователь попросил локально запустить `lint` и `detekt`, найти все проблемы и исправить их.

**Key Exchanges:**
- Был выбран локальный разбор репозитория с приоритетом на реальные нарушения, а не предположения.
- Проверка сначала уперлась не в `lint`, а в блокировку Gradle-артефактов при очистке `buildSrc`, после чего был выполнен безопасный разбор только build-папок.
- Найдено и исправлено единственное реальное нарушение в тесте: в `common-vpn/src/test/java/ru/ozero/commonvpn/StartSequenceCoordinatorBehaviorTest.kt` два `mockk<VpnService.Builder>` были развёрнуты в многострочный формат.
- После исправления отдельно прогонялись `ktlintCheck` и `detekt`, оба прошли успешно.

**Decisions Made:**
- Исправлять нарушения батчем после получения фактического отчета, а не по одному.
- Сначала устранять проблемы с build-артефактами/очисткой, чтобы не путать инфраструктурный сбой с ошибками кода.
- Не трогать уже существующий dirty-файл `.memory/daily/2026-06-06.md`, так как он не относился к задаче lint/detekt.

**Lessons Learned:**
- В этой зоне `ktlint` может ловить одновременно несколько стилевых правил на одной строке, и корректнее сразу приводить вызов к многострочному виду.
- Перед анализом кода важно отделить проблемы с окружением Gradle от собственно нарушений линтера.
- `detekt` может быть чистым даже после правки, если нарушение было только стилевым.

**Action Items:**
- При необходимости отдельно привести в порядок `.memory`, но это вне текущей задачи.
- Если будут новые нарушения, снова сначала снять фактический отчет, затем править и перепроверять.

### Session (11:50)

Context: Пользователь разбирал red CI в Ozero после итерации фиксов тестов и хотел сохранить промежуточную картину проблем/прогресса.

Key Exchanges:
- Пользователь попросил промежуточные выводы: какие проблемы остались и насколько итерация улучшила ситуацию.
- Ассистент сообщил, что был создан и запушен коммит `0be492b8` (`FIX: Исправление падений тестов CI`) на `dev`; свежий CI `27057636555` на этом SHA всё ещё red.
- Ассистент уточнил, что локальный Gradle не запускался из-за правил Ozero; валидация шла через GitHub Actions logs/artifacts.
- После фикса `engine-warp` перешёл с 4 failed tests на 0 failed tests, но стал падать по coverage.
- `singbox-subscription` снял assertion failure в `RawShareLinksParserEdgeTest`, но остался coverage.
- `common-vpn` compile blocker в `StartSequenceCoordinatorBehaviorTest` ушёл, теперь видны runtime/assertion failures.
- `app` всё ещё имеет compile blocker из-за ошибочного импорта `io.mockk.any`.

Decisions Made:
- Coverage threshold не ослаблять и не использовать как обход; сначала чинить compile/assertion blockers, затем добивать coverage реальными тестами или узкими доказанными исключениями для Android/native glue.
- CI sharding не делать в этой итерации, потому что крупная YAML-перестройка может смешать диагностику; сначала стабилизировать реальные failures.
- WARP test fixture заменить на `JSONObject/JSONArray`, чтобы не зависеть от ручной сборки JSON строк и сохранить смысл backward compatibility sentinel.

Lessons Learned:
- WARP assertion root был в тестовом helper `buildValidSlotJson()`: он генерировал невалидный JSON, это был дефект теста, не production regression.
- Для YAML `cipher: { method: aes-128-gcm }` production parser возвращает `aes-128-gcm`; прежний тест защищал неверный контракт.
- Свежий CI показал, что coverage debt массовый и реальный: `singbox-process` 0%, `engine-urnetwork` 28/22%, `engine-singbox` 28/16%, `engine-fptn` 40/43%, `engine-warp` 78/63%, при threshold 95%.
- `ktlint + detekt` после коммита зелёные, значит style текущие правки не сломали.

Action Items:
- Убрать ошибочный импорт `io.mockk.any` в `app`.
- Разобрать и исправить `common-vpn` assertion failures: `EngineWatchdogKillswitchIntegrationTest.kt:94` и `OzeroVpnServiceRuntimeRestartTest.kt:41`.
- После снятия test blockers системно добивать coverage по худшим модулям: `singbox-process`, `engine-urnetwork`, `engine-singbox`, `engine-fptn`.
- Рассмотреть CI sharding отдельным коммитом после стабилизации тестов/coverage.

### Session (11:54)

**Context:** Исправление red CI в Ozero и переход от снятия compile/assertion blockers к обязательному добиванию реального coverage до 95%+ по всем gated модулям.

**Key Exchanges:**
- После коммита `0be492b8` свежий CI остался red: часть compile/assertion blockers ушла, но стали видны массовые coverage failures.
- Пользователь резко уточнил требование: не пушить промежуточные фиксы и не прекращать работу, пока coverage не достигнет 95%+ везде; исключения из coverage допустимы только для доказанно нетестируемого кода.
- Ассистент признал, что coverage проценты пока не подняты, а итерация только сняла blockers, мешавшие увидеть честную картину.

**Decisions Made:**
- Больше не пушить промежуточные изменения до свежего CI с 95%+ по всем gated модулям или до узко обоснованных исключений.
- Работать по JaCoCo XML модуль за модулем: строить карту missed line/branch и писать реальные тесты под owning module.
- Сначала исправить очевидный app compile regression из-за неверного импорта `io.mockk.any`, затем заниматься только тестами/узкими исключениями.

**Lessons Learned:**
- Снятие compile/assertion blockers не равно улучшению coverage; оно лишь раскрывает реальные долги покрытия.
- В Ozero при coverage debt надо бояться недопокрыть больше, чем написать избыточные тесты.
- Исключения из coverage нельзя использовать как shortcut; требуется доказать, что код действительно нельзя проверить.

**Action Items:**
- Убрать ошибочный импорт `io.mockk.any` в app tests.
- Исправить common-vpn assertion failures в `EngineWatchdogKillswitchIntegrationTest.kt:94` и `OzeroVpnServiceRuntimeRestartTest.kt:41`.
- Добить coverage до 95%+ по всем gated модулям, включая `singbox-process`, `engine-urnetwork`, `engine-singbox`, `engine-fptn`, `engine-warp`, `engine-byedpi`, `engines-core`, `common-dns`, `common-net`, `core-backup`, `buildSrc`, `singbox-config`, `singbox-fmt`, `singbox-subscription`, `engine-masterdns`.
- Не делать CI sharding до стабилизации test/coverage failures, чтобы не смешивать диагностику.

### Session (12:24)

**Context:** Работа над доведением coverage до 95%+ по gated модулям Ozero без промежуточного push; локальный Gradle/test/lint запрещены, валидация должна идти через CI.

**Key Exchanges:**
- Пользователь напомнил, что текущая задача — не пушить промежуточные фиксы и не завершать работу, пока свежий CI не покажет coverage 95%+ по всем gated модулям.
- Ассистент подтвердил, что уже добавлен `DataStoreFptnConfigStoreTest`, который реально исполняет production `PreferenceDataStoreFactory`, `DataStoreFptnConfigStore.update/config/currentConfig`, `readConfig/writeConfig`, а не source-sentinel.
- Пользователь указал, что работа идет слишком медленно и нужно меньше анализировать, потому что информация по покрытию уже есть, и просто дописывать тесты.
- Ассистент сменил режим на прямую работу по class-level gap map и стал добавлять тесты пачками в самые большие deterministic uncovered классы.

**Decisions Made:**
- Не пушить и не коммитить промежуточные изменения до свежего CI с coverage 95%+ по всем gated модулям.
- Для `engine-urnetwork` не тратить время на рискованное покрытие auth-service reflection по SDK result classes без исходников/AAR API; вместо этого перейти к более безопасным deterministic gaps.
- Приоритизировать реальные bytecode/unit tests через public API или безопасную reflection, а не source-based sentinels.
- Сократить предварительный анализ и идти по готовой карте coverage gaps: `engine-byedpi`, `engine-warp`, `engine-masterdns` и другие deterministic классы.

**Lessons Learned:**
- `DataStoreFptnConfigStoreTest`: повторное создание `PreferenceDataStoreFactory` на один и тот же файл внутри одного теста может конфликтовать; для coverage достаточно читать тот же store после `update`, потому что production `readConfig` исполняется на каждом emission.
- `RealUrnetworkAuthServiceContractTest` source-based почти не помогает JaCoCo; чтобы покрывать private mappers, нужны реальные SDK classes/API, иначе высок риск compile fail.
- Для `SingboxEngine` часть runtime-state веток можно покрыть без Android bind/subprocess через reflection на `proxy/activeSocksPort`.
- Для `WarpUapi` private parser можно покрыть reflection с `BufferedReader`, без Android `LocalSocket`.

**Action Items:**
- Продолжить добавлять тесты пачками по class-level gap map, начиная с `engine-masterdns`: `MasterDnsDeployCredentials`, deployer helpers, DataStore store.
- После достижения ожидаемого покрытия запустить/дождаться CI terminal status и проверить, что coverage 95%+ по всем gated модулям.
- Перед финалом проверить `git status`; если `.memory/` dirty, включить изменения памяти в ближайший рабочий коммит, не отдельным push.

### Session (12:59)

**Context:** Добор покрытия CI в Ozero на ветке `dev`: добавлялись JVM/Robolectric tests и точечные coverage excludes для низкопокрытых engine-модулей, затем исправлялся упавший lint.

**Key Exchanges:**
- Пользователь сообщил: «Там линтер упал, почини линт и пуш снова».
- Ассистент выяснил, что CI не дошёл до coverage: ранний fail был на `ktlint + detekt`.
- `gh` в sandbox не смог прочитать GitHub CLI config из-за `Access is denied`; статус CI всё же был получен вне sandbox.

**Decisions Made:**
- Локальные Gradle/test/lint проверки не запускались, потому что проектный AGENTS запрещает локальный Gradle/test/lint; валидация только через CI.
- `RemoteAwgRuntime` и native/binder wrappers в WARP исключались точечно из coverage как Android service binding/native glue, непригодный для честного JVM coverage.
- `RealWarpSdkBridge` не исключался, потому что по нему уже есть реальные тесты.
- Для `WarpTurnOnResult` добавлен Parcelable roundtrip/newArray test, так как это Android unit-testable поведение и покрывает реальные branches без исключений.
- Для `engine-singbox` выбраны исполняемые tests start/stop/probe/config failure paths без bindService, чтобы закрывать реальные ветки вместо source-sentinel проверок.
- Пакет был оформлен одним рабочим коммитом `f5ae5ddf TEST: Добор покрытия CI` и запушен в `dev`; `main` не трогался.

**Lessons Learned:**
- `ParcelFileDescriptor` в Robolectric может вести себя по-разному при закрытии после parcel roundtrip; закрытие лучше оборачивать в `runCatching`, чтобы тест проверял Parcelable contract, а не платформенную деталь close.
- В новых MockK тестах лучше избегать `mockk.any()` там, где можно передать точное значение, чтобы снизить compile/style risk.
- CI может упасть на detekt/ktlint до запуска coverage jobs, поэтому сначала нужно вытаскивать конкретный lint report/log и чинить style issues.
- `engine-warp` и `engine-singbox` имели существенно низкое покрытие; одних sentinel/source checks недостаточно, нужны исполняемые JVM-testable branches.

**Action Items:**
- Исправить lint issues: ktlint report в `engines-core` и detekt issue в `engine-byedpi`.
- Сделать отдельный маленький commit с lint fix, push в `dev` и заново мониторить CI до terminal status.

### Session (22:17)

**Context:** Исправляли падение CI lint (`ktlint + detekt`) в репозитории, сначала локализовали реальную ошибку, затем внесли минимальную правку и перепроверили полный lint.

**Key Exchanges:**
- Было уточнено, что CI падает на `ktlintTestSourceSetCheck` в `engines-core`, а не на detekt.
- В итоге найдена конкретная проблема в `Socks5HandshakeProbeTest.kt:69` по правилу `spacing-between-declarations-with-annotations`.
- После правки локальный целевой lint и полный `ktlintCheck detekt --continue` прошли успешно.

**Decisions Made:**
- Исправлять только найденное lint-нарушение, без изменения поведения теста.
- Использовать минимальную структурную правку: добавить пустую строку перед `@Volatile`.
- Валидировать не только целевой task, но и полный CI-аналог `ktlintCheck detekt`, чтобы исключить скрытые проблемы в том же job.

**Lessons Learned:**
- Падение CI сначала нужно сузить до конкретного task/report, иначе легко лечить не ту причину.
- Для ktlint-ошибок по spacing часто достаточно точечной правки без более широкого рефакторинга.
- После нескольких прерываний важно заново подтвердить, что проблема реально воспроизводится и не остались ложные блокеры от прошлых запусков.

**Action Items:**
- Следить, чтобы в рабочем дереве остался только целевой файл с правкой.
- При необходимости убедиться, что `.memory/` изменения будут зафиксированы вместе с ближайшим рабочим коммитом.

