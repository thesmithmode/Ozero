# Daily Log: 2026-06-05
## Sessions
### Session 00:31
- Context: continued dev CI recovery after a runtime restart fix and reviewed earlier failures in the CI matrix.
- CI outcome at that point: one failing unit test in `app`, not a coverage gate.
- Finding `ObserveState`: confirmed a real compile issue because nested data classes do not get an implicit receiver for outer helper methods.
- Finding switching: confirmed a real lifecycle issue because successful restart on `Probing/Connecting/Connected` did not clear switching state.
- Decision: move observer helper functions to file-private scope, clear switching on successful restart, treat `TunnelState.Failed` as restart failure, and keep the tests aligned with the active contract.
### Session 11:56
- Context: investigated restart coalescing, Singbox config selection, and URI fallback parsing after code review comments.
- Key exchanges:
- Current restart logic uses a mutex-guarded queue instead of a boolean pending flag.
- `SingboxEngine.buildManualConfig()` must prefer the selected profile row over the cached DataStore blob.
- URI fallback parsing must decode `userinfo` consistently so percent-encoded credentials survive a failed `URI(raw)` parse.
- The restart queue needs a hard reset on abort/failed restart so later edits do not stay stuck behind a stale in-progress flag.
- Decisions made:
- Update source-sentinel tests to match the queue-based restart contract.
- Add regression coverage for Singbox row precedence and URI fallback decoding.
- Keep memory entries sanitized and free of user-specific CI URLs.
## Action Items
- Finish the code and test updates for the four review findings.
- Recheck CI to terminal state once the branch is pushed.
### Session (12:24)

**Context:** Исправляли регрессии в `SingboxEngine`, fallback-парсинге `userinfo` и сбросе очередей рестарта/abort, затем довели изменения до зелёного CI на `dev`.

**Key Exchanges:**
- Обсудили безопасный компромисс для `userinfo`: percent-decoding через `URLDecoder` после экранирования literal `+`, чтобы `+` сохранялся как символ, а `%40` и другие коды корректно декодировались.
- Выявили, что helper для очистки очереди должен быть `suspend`, иначе `withLock` не компилируется.
- Зафиксировали, что `SingboxEngine.buildManualConfig()` должен сначала брать blob выбранной строки профиля, и только потом падать на кеш `BEAN_KEY`.
- Отдельно отметили, что fallback-парсинг URI должен декодировать `userinfo`, чтобы `%40` превращался в `@`.
- Поймали важный хвост: source-sentinel был всё ещё завязан на старый `BEAN_KEY`-контракт и мог ломать CI даже при правильной реализации.
- Проверили, что CI-run `27006147176` на SHA `46b66db4d5ec84b23f635586d517977f35046d56` завершился success, включая `Tests — app` и `assembleDebug`.

**Decisions Made:**
- Добавить регрессии на `userinfo` decode, precedence выбранной Singbox-строки и очистку очереди при abort, чтобы зафиксировать новые контракты.
- Сузить слишком широкий sentinel, чтобы он проверял именно контракт сброса очереди, а не соседний код.
- Обновить `MainActivity` и `RuntimeConfigRestartCoordinator`, чтобы очередь рестартов явно сбрасывалась на abort и не возвращался stuck-state.
- Санитизировать `.memory`-лог, убрав user-specific CI ссылки.

**Lessons Learned:**
- `userinfo` в URI нельзя декодировать “как есть”: `+` нужно защищать отдельно, иначе теряется исходный символ.
- Sentinel-тесты должны быть строго scoped, иначе они начинают ловить соседние состояния и создают ложные падения.
- Если контракт меняется, нужно одновременно обновлять и код, и source-sentinel, иначе CI будет сигналить о старом ожидании вместо реальной ошибки.

**Action Items:**
- Держать новый контракт `selected row first, fallback second` как источник истины в `SingboxEngine`.
- Следить, чтобы сброс очереди при abort не откатывался в `MainActivity` и `RuntimeConfigRestartCoordinator`.
- Продолжать обновлять `.memory` вместе с рабочими изменениями и не оставлять memory-diff незакоммиченным.

### Session (12:24)

**Context:** Работали над доведением `dev` CI до зелёного состояния: искали причину падений, вносили правки в код и тестовые sentinel-контракты, затем дождались нового успешного CI run.

**Key Exchanges:**
- Было найдено, что в `SingboxEngine.buildManualConfig()` приоритет должен быть у выбранной строки профиля, а не у старого кеша `BEAN_KEY`.
- Обсуждали, что fallback-парсинг URI должен декодировать `userinfo`, чтобы `%40` превращался в `@`, и это нужно покрыть регрессионным тестом.
- Было выявлено, что очереди рестарта в `MainActivity` и `RuntimeConfigRestartCoordinator` должны сбрасываться при abort, иначе остаётся зависший `restartInProgress`.
- Отдельно отмечалось, что один sentinel был слишком широким и мог цеплять соседний код, поэтому его сузили до нужного блока.
- После пуша был найден свежий GitHub Actions run и CI довели до terminal `success`.

**Decisions Made:**
- Вносить минимальный, но полный набор правок сразу в код и тесты, чтобы CI ловил именно новые контракты, а не старые ожидания.
- Обновить sentinel-тесты вместе с кодом, чтобы избежать ложных падений на старом контракте.
- Зафиксировать sanitization memory-лога без user-specific ссылок и данных.

**Lessons Learned:**
- Если sentinel слишком широкий, он может валить тест не по целевому контракту, а из-за соседней логики.
- Для CI-фиксов важно проверять не только локальные изменения, но и свежий Actions run после пуша, иначе можно ошибочно принять промежуточное состояние за финальный результат.
- При смене контракта в коде нужно одновременно обновлять связанные fakes/sentinels, иначе CI будет продолжать падать на старых ожиданиях.

**Action Items:**
- Держать в памяти новый контракт: precedence выбранной строки в Singbox, decode fallback `userinfo`, сброс очереди рестарта на abort.
- При следующих изменениях вокруг restart-логики проверять, не возвращается ли stuck-state в очередях.
- Сохранять практику проверки свежего `dev` CI run после каждого значимого пуша.

### Session (13:07)

Context: Проводилось ревью изменений по нескольким секторам репозитория, с разбором рисков в `common-vpn`, `engine-warp`, `engine-urnetwork`, `singbox-*` и проверкой, есть ли существенные регрессии в `buildSrc/common-crypto`.

Key Exchanges:
- Было отмечено, что по сектору `singbox-*` субагент нашел несколько валидных регрессий, включая IPv6 fallback с квадратными скобками, недетерминированный выбор при равной latency и некорректный разбор Clash YAML `cipher`/`plugin-opts`.
- Ревью по `common-vpn` выявило критические проблемы в fail-closed поведении при ошибках старта и риск окна без blocking-TUN во время kill-switch рестарта.
- Дополнительно были найдены high/medium issues в runtime-restart очереди, late failure routing, сохранении WARP slot, мутирующих INI-заменах, шумном runtime fingerprint, legacy WARP DNS behavior, а также в `Urnetwork` state handling и thread-affinity callback-мутациях.
- По `buildSrc/common-crypto` явных high-confidence runtime regression не нашли, изменения выглядят как hardening и улучшение trust/coverage path.

Decisions Made:
- Ревью было разбито на сектора и отдельные субагенты, чтобы покрыть и code-path, и trust-path без раздувания списка findings.
- Для списка findings была выбрана сортировка по убыванию риска, чтобы сначала зафиксировать критические regressions.
- Было решено не расширять список искусственно, если по `buildSrc/common-crypto` нет убедительных runtime-рисков.

Lessons Learned:
- Fail-closed поведение и порядок shutdown/startup шагов критичны: небольшое изменение в coordinator может оставить сервис в живом, но ошибочном состоянии.
- Concurrent probing и очередь результатов требуют явного tie-break, иначе поведение становится недетерминированным между запусками.
- Fallback-парсеры для share-link и YAML должны нормализовать значения до канонического вида, иначе downstream конфиг получает невалидные host/method поля.
- Любая stateful UI/connector логика без ownership/refcount или без явного main-thread marshaling легко дает гонки и залипшие состояния.

Action Items:
- Подтвердить и, при необходимости, исправить findings в `common-vpn`, `engine-warp`, `engine-urnetwork` и `singbox-*`.
- Добавить регрессионные тесты под IPv6 fallback, deterministic probe tie-break и Clash YAML `cipher` map handling.
- Отдельно проверить, не скрываются ли еще риски в `buildSrc/common-crypto`, но без искусственного расширения scope.

### Session (13:09)

**Context:** Проводился обзор диффа по нескольким секторам репозитория с приоритетом на реальные регрессии и риски релиза, особенно в infra/buildSrc и common-vpn.

**Key Exchanges:**
- Было отмечено 2 критические регрессии в `buildSrc/src/main/kotlin/ozero.jacoco.gradle.kts`: занижение coverage gate ниже правила 95%/100% и исключение из покрытия trust-sensitive production paths.
- Дополнительно выявлены риски в `common-vpn`: потеря fail-closed поведения при старте, а также окно в kill-switch restart, где watchdog может неверно уйти в обычный stop.
- На уровне `app`, `engine-warp`, `engine-urnetwork`, `singbox` и `buildSrc` перечислены дополнительные HIGH/MEDIUM риски: зависание restart-ветки, ложные runtime failures, потеря `endpointList`, шумный fingerprint, смена DNS-поведения старых WARP-слотов, проблемы с cancel/resolve state в URnetwork, недетерминизм auto-select и хрупкость `DownloadBinaryTask`.
- Проверка была статической; локальные тесты не запускались.

**Decisions Made:**
- Главным риском релиза признан trust-gate, а не runtime: сначала нужно чинить JaCoCo-пороги и exclusions, затем разбирать runtime-пути.
- Среди runtime-рисков приоритет отдан `common-vpn` startup/kill-switch логике и WARP save/restart поведению, как наиболее опасным для пользовательского сценария.
- `DownloadBinaryTask` пока не квалифицирован как доказанный runtime-bug, но помечен как хрупкий error-path для наблюдения.

**Lessons Learned:**
- Coverage gate и exclusions в `buildSrc` могут сделать CI “зелёным” при реальной деградации безопасности и качества, если из покрытия исключены критичные production entry points.
- В startup/kill-switch цепочках важна fail-closed семантика и отсутствие промежутков, где watchdog может принять неверное решение.
- В `singbox` и `URnetwork` много регрессий связано не с падением приложения, а с детерминизмом, порядком данных и состояниями отмены/повтора.

**Action Items:**
- Вернуть единый coverage threshold 0.95/0.95 или оформить явный allowlist с отдельным review gate.
- Убрать из JaCoCo exclusions production trust paths, оставив только generated artifacts.
- Проверить и восстановить fail-closed поведение в `common-vpn` startup и kill-switch restart.
- Разобрать WARP save/restart логику: сохранение `endpointList`, чувствительность fingerprint и DNS-изменение для старых слотов.
- Отдельно проверить URnetwork resolve/cancel state и недетерминизм auto-select в `singbox`.

### Session (13:28)

**Context:** Работали над разбором большого code review и пошаговым исправлением подтверждённых багов в текущем коде относительно `v1.0.13`, с упором на проверку каждого дефекта перед фиксом.

**Key Exchanges:**
- Было решено не лечить все замечания сразу, а идти по одному подтверждённому багу за раз: проверить, есть ли реальный сломанный путь, затем фиксить и только после этого переходить дальше.
- Пользователь потребовал не пытаться “проглотить” все проблемы сразу, а выполнять цикл: один баг, проверка, фикс, следующий баг.
- В ходе работы были выделены и по очереди взяты в работу несколько конкретных проблем: потеря `endpointList` при `WarpEngineSettingsViewModel.onSaveEdit()`, зависание restart-очереди после исключения в runtime-restart, некорректный UI-сброс URnetwork при очистке кэша локаций, риск `message!!` в `DownloadBinaryTask`, а также некорректная обработка YAML scalar/map полей.

**Decisions Made:**
- Фиксировать только подтверждённые баги, а не потенциальные риски.
- Работать строго последовательно, по одному дефекту за раз.
- Для каждого исправления сразу добавлять или править регрессионный тест, чтобы не оставлять фиксы без проверки.
- Не менять поведение WARP fingerprint наугад, пока не понятен существующий контракт парсера/канонизатора.

**Lessons Learned:**
- При сохранении WARP-слота нельзя обнулять `endpointList`, иначе теряется пользовательское состояние.
- При очистке кэша URnetwork UI должен сбрасываться из `Ready` в `NotConnected`, а не показывать устаревшее состояние.
- `message!!` в обработке ошибок может сам стать источником падения и ломать recovery-путь.
- YAML-поля нельзя без разбора схлопывать в строковые представления, если downstream ожидает scalar-значения, иначе можно передать мусор в конфиг.

**Action Items:**
- Довести до конца последовательное исправление оставшихся подтверждённых багов из ревью.
- Проверить и при необходимости подправить тестовые контракты вокруг WARP fingerprint, URnetwork threading и YAML-парсинга.
- Продолжать фиксить только после верификации реального дефекта в коде.

### Session (13:50)

**Context:** Работа над серией точечных регрессий по ревью в Android/engine-коде с упором на подтверждённые баги и минимальные fixes.

**Key Exchanges:**
- Для регрессии с restart-логикой выбран runtime-тест через Kotlin reflection на приватный suspend-метод, чтобы проверить именно состояние очереди после exception, а не жизненный цикл корутины.
- Выяснилось, что reflection-хелпер в тесте должен корректно ждать suspend-результат; это было отдельно исправлено, чтобы тест не был ложноположительным.
- Подтверждён и закрыт баг: при exception в restart path очередь теперь сбрасывается, и следующий restart больше не уходит в бесконечный coalesce.
- Один пункт ревью про `RuntimeFailureRouter` признан спорным на фоне существующих контрактных тестов и временно отложен как невалидный/неподтверждённый.
- Для `SingboxProbeService` найден nondeterminism при равной latency: выбор зависел от completion order `ConcurrentLinkedQueue`, поэтому был введён детерминированный tie-break по исходному порядку входного списка.
- Для `UrnetworkPreferredLocationConnector` подтверждена проблема: `resolving` мог оставаться `true` после отмены, но баг ещё был признан спорным и не доведён до фикса в этом фрагменте.
- Исправлено преждевременное сохранение preferred location: запись в device/localState теперь происходит только после успешного `connect(match)`, а не после любой попытки.
- Отдельно закрыт баг, где `WarpEngineSettingsViewModel` терял `endpointList` при сохранении edit-слайда.
- Также исправлено: `UrnetworkLocationsViewModel` сбрасывает stale `Ready` в `NotConnected`, когда кэш локаций очищается.
- Исправлен `ClashYamlParser`: map-valued `cipher` больше не превращается в мусорный `k=v,...`, Shadowsocks method берётся корректно.

**Decisions Made:**
- Работать только по подтверждённым и проверяемым багам, не трогая спорные пункты ревью без твёрдой регрессии.
- Для проверки приватной suspend-логики использовать reflection в тесте, чтобы изолировать именно баг в restart-очереди.
- Для равных latency в probe selection выбрать детерминированный tie-break по исходному порядку, чтобы убрать зависимость от порядка завершения.
- Сохранять preferred location только на success, чтобы не закреплять ложное состояние при неуспешном connect.

**Lessons Learned:**
- Регрессии в suspend-логике лучше ловить прямым вызовом через reflection, если важен внутренний state transition.
- Любой nondeterminism из-за completion order надо нормализовать на уровне исходного входного порядка, а не пытаться “подловить” его тестом.
- Persist-сайд-эффекты должны быть привязаны к успешному результату, иначе легко зафиксировать ложное состояние.
- Несколько пунктов из ревью оказались спорными и не стоили изменения без дополнительной верификации.

**Action Items:**
- Продолжить разбор следующего отдельного пункта из ревью тем же режимом: один подтверждённый баг, один регрессионный тест, затем следующий.
- При необходимости отдельно вернуться к спорному `RuntimeFailureRouter` после проверки контракта и реального bypass-сценария.
- В дальнейшем учитывать, что локальные тесты по правилам репозитория не запускались.

### Session (13:56)

**Context:** Продолжалась пошаговая разборка и фиксация багов из большого code review для текущего diff 1.0.13, с упором на реальные регрессии и регрессионные тесты.

**Key Exchanges:**
- Исправлялся nondeterministic tie-break в `SingboxProbeService` для равной latency, чтобы выбор шёл по исходному порядку, а не по порядку завершения probe’ов.
- Уточнялось поведение persistence preferred location в URnetwork: запись в `device/localState` должна происходить только после успешного `connect(match)`, а не после любой попытки.
- Была проверена и закрыта отдельная регрессия WARP fingerprint: `slotId` не должен участвовать в equality, иначе одинаковый runtime-контент выглядит как изменение из-за storage identity.
- Исправлялся `DownloadBinaryTask`, чтобы исходная ошибка не подменялась `e.message!!` и не превращалась во вторичную NPE при `null`-message.
- Поднимался lifecycle-gap в URnetwork preferred-location: `resolving` может зависнуть, если job отменили до любого callback, и это было отмечено как cleanup-bug.
- В рамках ревью был проговорён подход: брать один подтверждённый баг за раз, сразу добавлять регрессионный тест и не трогать спорные пункты до проверки их контракта.

**Decisions Made:**
- Решено фиксить только доказуемые баги, а не спорные policy-изменения.
- Решено добавлять минимальный регрессионный тест на каждый исправленный баг, чтобы проверять именно сломанный порядок или контракт.
- Решено не считать баг закрытым до личной сверки изменённых фрагментов и тестового покрытия по конкретному сценарию.
- Решено не запускать локальные тесты, следуя правилам репозитория, и опираться на ревизию кода и контрактные проверки.

**Lessons Learned:**
- `slotId` как часть fingerprint equality создаёт ложные изменения и может триггерить лишние restarts, хотя runtime-контент не менялся.
- `!!` на `Throwable.message` опасен: при `null`-message ошибка может быть потеряна из-за вторичной поломки.
- Для lifecycle-кодов недостаточно ждать callback’а; нужен явный completion/cleanup path на случай отмены до первого события.
- Для таких багов важнее проверка порядка и контракта, чем просто наличие строк или вызовов.

**Action Items:**
- Дочитать приложенный ревью-текст целиком и сверить его со списком уже закрытых пунктов.
- Продолжить разбор оставшихся багов по одному, в том же режиме: анализ, точечный фикс, регрессионный тест, сверка.
- Обновлять TODO и помечать баги закрытыми только после реальной проверки.

### Session (14:46)

**Context:** Продолжалась пошаговая правка подтверждённых багов в `singbox`, `URnetwork` и `WARP` с опорой на реальные runtime-контракты и регрессии.

**Key Exchanges:**
- Подтвержден race в `SingboxEngineSettingsViewModel`: `testingProfileIds` как плоский set не держал пересекающиеся `probeAndAutoSelect()` callbacks, из-за чего spinner мог погаснуть раньше времени.
- Уточнён корень по `Singbox`: нужен refcount, а не простое добавление/удаление флага, чтобы UI-state отражал все активные probe-запуски.
- В `URnetwork` подтверждён отдельный баг: `selectedLocation` сохранялся слишком рано, до успешного `connect`, причём источник проблемы оказался не в `setPreferredLocation`, а в `connectTo()` и `connectBestAvailable()`.
- Для `WARP` проверено, что `WarpEndpointProber` сортирует результаты по RTT, поэтому исходный `endpointList` влияет только частично; `rawIniOverride` тоже может шуметь fingerprint’ом без реального изменения поведения.

**Decisions Made:**
- Для `Singbox` внедрён refcount через `ConcurrentHashMap<Long, AtomicInteger>` и добавлен regression-тест на overlap, чтобы spinner не гас до завершения всех callback’ов.
- Для `URnetwork` выбрано исправление “persist after success”: запись selection переносится внутрь `onSuccess`, чтобы не закреплять неудачный выбор до подтверждённого `connect`.
- Для `WARP` решено делать canonicalization, а не грубо вырезать поля fingerprint: нормализовать `rawIniOverride` и `endpointList`, чтобы убрать шум от форматирования и reorder, сохранив реальные изменения поведения.

**Lessons Learned:**
- В VM-level state для overlapping async flows нужен счётчик владения, а не boolean/set.
- Если state влияет на UI и persistence, ранняя запись до успешного network-action создаёт ложные пользовательские состояния и должна быть перенесена после подтверждения.
- Fingerprint’ы нельзя считать по сырому конфигу, если runtime сам нормализует или сортирует данные, иначе получаются лишние restart-trigger’ы от несемантических изменений.

**Action Items:**
- Довести проверку фикса `WARP` fingerprint: убедиться, что canonicalization действительно отличает runtime-change от whitespace/comment noise и reorder.
- Продолжать следующий отдельный баг из списка после подтверждения текущего `WARP` патча.

### Session (15:12)

**Context:** Продолжалась пошаговая зачистка подтверждённых багов по ревью текущего кода, с фокусом на WARP, coverage gate и thread-affinity в URnetwork.

**Key Exchanges:**
- Было подтверждено, что `WarpRuntimeFingerprint` уже исправлен: canonicalization больше не теряет смысловые raw-поля вроде `PresharedKey`, но по-прежнему игнорирует шум от formatting/reorder.
- Для WARP legacy-path выяснилось, что старые конфиги и слоты должны читать `doHProvider = SYSTEM`, а не молча подставлять Cloudflare.
- В `DownloadBinaryTask` проверка показала, что проблемный `message!!`-паттерн уже отсутствует, там используется `e.gradleMessage()` с fallback.
- Проверка coverage gate подтвердила, что текущий JaCoCo exclude-list скрывает детерминированную логику, и это совпадает с контрактным тестом.
- В `RealUrnetworkSdkBridge` найден живой runtime-риск: `addJwtRefreshListener` и `addProvideSecretKeysListener` писали в `LocalState` напрямую, без marshaling на main thread.

**Decisions Made:**
- WARP legacy `doHProvider` нужно фиксить на уровне чтения legacy-конфига и legacy-slot, чтобы не ломать уже сохранённые данные и не менять default для нового UI.
- Fingerprint canonicalization принято строить через parsed known config плюс нормализованные raw-extras, чтобы убрать шум, но не потерять смысловые поля.
- Coverage exclusions нужно убирать точечно только для запрещённых контрактом masks, не трогая generated/boilerplate excludes.
- Callback mutations в URnetwork должны идти через `Dispatchers.Main.immediate`, потому что прямые `LocalState` writes нарушают thread-affinity контракт.

**Lessons Learned:**
- Если raw-конфиг не парсится, такой тест не доказывает canonicalization шума, а лишь сравнивает сырые строки.
- При проверке оставшихся пунктов ревью важно сначала подтверждать реальный живой path в коде, а не чинить уже закрытый сценарий.
- Для coverage masks длинные списки лучше разбирать по конкретным строкам, иначе легко снести лишнее или не увидеть остатки.

**Action Items:**
- Довести чистку JaCoCo exclude-list до контрактного состояния и подтвердить, что запрещённые masks больше не скрываются.
- Продолжить исправление thread-affinity в `RealUrnetworkSdkBridge`, убедившись, что все SDK callbacks мутируют state только через main thread.
- После каждого подтверждённого фикса держать регрессионный тест рядом с изменением и проверять, что он действительно покрывает нужную ветку.

### Session (15:47)

**Context:** Работали над ревью-рисками и coverage-дыру в `buildSrc`/JaCoCo exclusions, последовательно снимая лишние исключения и закрепляя это тестами и контрактами.

**Key Exchanges:**
- Сняли несколько `MasterDns`-масок из `ozero.jacoco.gradle.kts`, чтобы production wrapper/contract больше не скрывались из coverage gate.
- Убрали stale-исключения вроде `UrnetworkAuthService$DefaultImpls` и `InMemoryUrnetworkConfigStore`, так как они либо уже покрыты общей маской, либо выглядят устаревшими.
- Выявили, что `core-backup` содержит deterministic production-логику, а не временные артефакты, и решили закрыть её тестами вместо сохранения excludes.
- Добавили `AppBackupSerializerTest.kt` с round-trip, encrypted/plain flow, `BackupCategory` и JSON helper coverage.
- Убрали backup-маски из `ozero.jacoco.gradle.kts` и обновили `JacocoExcludesContractTest`, чтобы эти классы не вернулись в exclusions.
- Следующим кандидатом были `HttpUrlConnectionClient` и `RawUpdater`: проверили, что у них уже есть прямые тесты, значит их тоже можно выводить из excludes.

**Decisions Made:**
- Снимать exclusions только после подтверждения, что класс живёт в `src/main` и уже имеет тестовое покрытие или может быть закрыт минимальным тестом без риска.
- Для `core-backup` выбран тестовый путь, а не оставление масок, потому что слой оказался чистым deterministic code.
- `coroutine-generated refresh-helper` не трогать без отдельной проверки, чтобы не перепутать generated glue с реальным production-кодом.

**Lessons Learned:**
- Coverage-дыры здесь часто прячутся не в “сложной” логике, а в production-helper слоях, которые ошибочно маскируют как glue.
- Перед удалением mask лучше подтверждать наличие прямых тестов или строить минимальный deterministic test, иначе легко снести нужное исключение.
- Контрактный тест на exclusions полезен как защита от возврата уже закрытых символов в JaCoCo config.

**Action Items:**
- Продолжить сверку оставшихся exclusions в `ozero.jacoco.gradle.kts` по одному подтверждённому слою за раз.
- Зафиксировать, сколько из пользовательского списка уже реально закрыто после актуальной проверки дерева.
- Проверить, не остались ли рядом ещё production-классы с прямыми тестами, которые всё ещё скрыты в excludes.
### Session (16:34)

**Context:** Работали над серией фиксов для CI и затем получили новый P2-баг в WARP edit flow, где редактирование импортированного slot не сбрасывает `endpointList` при смене endpoint preset.

**Key Exchanges:**
- Обсуждали, что новый баг возникает в edit path: `endpointList` сохраняется при смене endpoint, хотя дальше `EngineWarp.resolveActive()` продолжает предпочитать `slot.endpointList` перед `config.peerEndpoint`.
- Было отмечено, что после сохранения UI показывает новый endpoint, но при следующем start/restart движок всё ещё пробует старый список и может откатываться на него.
- Пользователь отдельно сообщил, что линт снова упал после предыдущего пакета исправлений.

**Decisions Made:**
- Решено чинить на owning layer в save path: при изменении endpoint поля нужно очищать или заменять `endpointList`, чтобы не сохранялся устаревший список и не ломался следующий запуск.
- Новый баг принят как P2 и будет исследоваться с прицелом на точечный fix в WARP edit flow, а не на симптом в `resolveActive()`.

**Lessons Learned:**
- Для WARP сохранение UI-состояния и runtime-выбор endpoint должны быть синхронизированы, иначе старый `endpointList` продолжает выигрывать у нового `peerEndpoint`.
- Повторяющиеся падения линта требуют отдельно проверять форматные/локальные проблемы в новом патче, не смешивая их с логикой фикса.

**Action Items:**
- Найти save path для импортированного WARP slot и изменить логику обновления `endpointList` при смене endpoint preset.
- Перепроверить линт после внесения правки, чтобы не повторить предыдущий цикл CI-failure.

### Session (16:34)
Context: fixed WARP edit flow so imported slot endpoint lists no longer survive a user-changed endpoint preset.
Key exchange: if the edited endpoint matches the original slot endpoint, keep the imported `endpointList`; otherwise replace it with a singleton list for the new endpoint so runtime probing cannot keep using stale candidates.
Action item: watch the next dev CI run for format/regression fallout after the change.
