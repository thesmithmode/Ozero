<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+: тип FGS для OzeroVpnService = specialUse (см. common-vpn manifest). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Android 14+: тип FGS для StrategyScanService = dataSync. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Android 13 (API 33+): runtime-permission, без неё system-tray notification VPN не покажет -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 8+ (API 26+): self-update через PackageInstaller — иначе install upd APK падает -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!--
        опциональный whitelist от Doze. Без permission'а system отказывает в
        ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS Intent'е (SecurityException).
        Prompt показывается один раз — VPN работает и без него, но с Doze possible drops.
    -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!--
        Android 11+ (API 30+): без QUERY_ALL_PACKAGES getInstalledApplications возвращает
        обрезанный список, и пользователь не может добавить системные сервисы / background-only
        приложения в split-tunnel allowlist/blocklist. VPN-юзкейс — Google Play разрешает
        QUERY_ALL_PACKAGES для VPN чтобы дать пользователю полный контроль над per-app routing.
    -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />

    <application
        android:name=".OzeroApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ozero"
        android:usesCleartextTraffic="false"
        android:enableOnBackInvokedCallback="true"><!-- Predictive Back на Android 14+ -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".selfupdate.UpdateInstallResultReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="ru.ozero.app.UPDATE_INSTALL_RESULT" />
            </intent-filter>
        </receiver>

        <!-- отключаем default WorkManager auto-init, используем custom Configuration.Provider в OzeroApp -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- FileProvider для экспорта crash log из filesDir/crashes/ -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <service
            android:name=".OzeroQuickTile"
            android:exported="true"
            android:icon="@drawable/ozero_logo_white"
            android:label="Ozero VPN"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- AppCompat per-app locale persistence на API 24-32 (на API 33+ работает системный LocaleManager). -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <service
            android:name=".warp.WarpEngineService"
            android:process=":engine_warp"
            android:exported="false" />

        <service
            android:name="ru.ozero.singboxprocess.SingboxEngineService"
            android:process=":engine_singbox"
            android:exported="false" />

        <service
            android:name=".ui.strategy.StrategyScanService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

    </application>

</manifest>
